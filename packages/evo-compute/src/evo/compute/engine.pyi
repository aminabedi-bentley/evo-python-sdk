#  Copyright © 2026 Bentley Systems, Incorporated
#  Licensed under the Apache License, Version 2.0 (the "License");
#  you may not use this file except in compliance with the License.
#  You may obtain a copy of the License at
#      http://www.apache.org/licenses/LICENSE-2.0
#  Unless required by applicable law or agreed to in writing, software
#  distributed under the License is distributed on an "AS IS" BASIS,
#  WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
#  See the License for the specific language governing permissions and
#  limitations under the License.

# AUTO-GENERATED BY `python -m evo.compute._stubgen generate`. DO NOT EDIT.
#
# Catalogue snapshot: GET /compute/orgs/{org_id}/tasks?details=true
# Captured: 2026-09-17
#
# The runtime is always live, so tasks published after the snapshot still run --
# they are simply not statically known until the snapshot is refreshed. Use
# `ComputeClient.arun(topic, task, parameters)`, or `SyncComputeClient.run(...)`,
# to reach them.

from typing import Any, Literal, Protocol
from uuid import UUID

from evo.common import IContext
from evo.objects import ObjectMetadata, ObjectReference
from evo.objects.typed import BaseObject, DownloadedObject
from typing_extensions import NotRequired, TypedDict

from .outputs import ResultNode, SyncResultNode, SyncTaskResult, TaskResult
from .overrides.geostatistics.kriging import (
    BlockDiscretisation,
    Filter,
    KrigingResult,
    OrdinaryKriging,
    SearchNeighborhood,
    SimpleKriging,
    _ObjectInput,
    _SourceInput,
    _TargetInput,
)
from .overrides.geostatistics.kriging import KrigingOverride as _GeostatisticsKriging
from .tasks.common.source_target import AnyTypedAttribute

__all__ = ["ComputeClient", "SyncComputeClient"]

ObjectInput = str | UUID | BaseObject | DownloadedObject | ObjectMetadata | ObjectReference
"""Any handle reference resolution can derive a geoscience object URL from."""

AttributeInput = str | AnyTypedAttribute
"""A typed attribute, a JMESPath expression, or a bare attribute name."""

class _FileUrl(Protocol):
    """Anything exposing the URL directly, such as a file resource."""

    url: Any

class _FileWithMetadata(Protocol):
    """Anything exposing it through metadata, such as an upload result."""

    metadata: _FileUrl

FileInput = str | _FileUrl | _FileWithMetadata
"""A File API URL, or a handle carrying one."""

class ConverterDetectConverterResultConverterOption(ResultNode):
    """A converter task that can handle a particular file extension."""

    converter_task: str
    """The compute task URL for the converter (e.g. 'https://example.com/compute/orgs/{org_id}/converter/obj-import')."""
    description: str
    """A human-readable description of what the converter task does."""
    schema: str
    """The geoscience object schema the converter produces (e.g. 'triangle-mesh')."""

class ConverterDetectConverterResultFileConverterInfo(ResultNode):
    """Result for a single file, indicating which converters can handle it."""

    converters: list[ConverterDetectConverterResultConverterOption]
    """The converter task(s) that can handle this file."""
    extension: str
    """The lowercased file extension including the leading dot, or empty string."""
    file_ref: str
    """The file reference URL including version."""
    filename: str
    """The filename from file metadata."""
    supported: bool
    """Whether at least one converter exists for this file extension."""

class ConverterDetectConverterResult(TaskResult):
    """Results for the detect-converter task."""

    files: list[ConverterDetectConverterResultFileConverterInfo]
    """One result per input file, in the same order."""

class SyncConverterDetectConverterResultConverterOption(SyncResultNode):
    """A converter task that can handle a particular file extension."""

    converter_task: str
    """The compute task URL for the converter (e.g. 'https://example.com/compute/orgs/{org_id}/converter/obj-import')."""
    description: str
    """A human-readable description of what the converter task does."""
    schema: str
    """The geoscience object schema the converter produces (e.g. 'triangle-mesh')."""

class SyncConverterDetectConverterResultFileConverterInfo(SyncResultNode):
    """Result for a single file, indicating which converters can handle it."""

    converters: list[SyncConverterDetectConverterResultConverterOption]
    """The converter task(s) that can handle this file."""
    extension: str
    """The lowercased file extension including the leading dot, or empty string."""
    file_ref: str
    """The file reference URL including version."""
    filename: str
    """The filename from file metadata."""
    supported: bool
    """Whether at least one converter exists for this file extension."""

class SyncConverterDetectConverterResult(SyncTaskResult):
    """Results for the detect-converter task."""

    files: list[SyncConverterDetectConverterResultFileConverterInfo]
    """One result per input file, in the same order."""

class ConverterDgnImportTarget(TypedDict):
    """Target specifying where to create or update the imported geoscience object. Contains 'reference' (File API URL) and optional 'overwrite' (boolean, only valid for path references)."""

    description: NotRequired[str | None]
    """Description to put into the target object."""
    overwrite: NotRequired[bool]
    """Whether to overwrite the existing object."""
    reference: ObjectInput
    """Reference to the targeted object."""
    tags: NotRequired[dict[str, str]]
    """Tags to put into the target object."""

class ConverterDgnImportResultResultObject(ResultNode):
    """The geoscience object created from the input DGN file."""

    description: str | None
    """The description of the geoscience object."""
    name: str
    """The name of the geoscience object."""
    reference: str
    """Reference to a geoscience object."""
    schema_id: str
    """The ID of the Geoscience Object schema."""

class ConverterDgnImportResult(TaskResult):
    """Results for the DGN import compute task."""

    object: ConverterDgnImportResultResultObject
    """The geoscience object created from the input DGN file."""

class SyncConverterDgnImportResultResultObject(SyncResultNode):
    """The geoscience object created from the input DGN file."""

    description: str | None
    """The description of the geoscience object."""
    name: str
    """The name of the geoscience object."""
    reference: str
    """Reference to a geoscience object."""
    schema_id: str
    """The ID of the Geoscience Object schema."""

class SyncConverterDgnImportResult(SyncTaskResult):
    """Results for the DGN import compute task."""

    object: SyncConverterDgnImportResultResultObject
    """The geoscience object created from the input DGN file."""

class ConverterErsImportTarget(TypedDict):
    """Target specifying where to create or update the Regular2DGrid object. Contains 'reference' (File API URL) and optional 'overwrite' (boolean, only valid for path references)."""

    description: NotRequired[str | None]
    """Description to put into the target object."""
    overwrite: NotRequired[bool]
    """Whether to overwrite the existing object."""
    reference: ObjectInput
    """Reference to the targeted object."""
    tags: NotRequired[dict[str, str]]
    """Tags to put into the target object."""

class ConverterErsImportResultResultObject(ResultNode):
    """The Regular2DGrid object created from the input file."""

    description: str | None
    """The description of the geoscience object."""
    name: str
    """The name of the geoscience object."""
    reference: str
    """Reference to a geoscience object."""
    schema_id: str
    """The ID of the Geoscience Object schema."""

class ConverterErsImportResult(TaskResult):
    """Results for the ERS import compute task."""

    object: ConverterErsImportResultResultObject
    """The Regular2DGrid object created from the input file."""

class SyncConverterErsImportResultResultObject(SyncResultNode):
    """The Regular2DGrid object created from the input file."""

    description: str | None
    """The description of the geoscience object."""
    name: str
    """The name of the geoscience object."""
    reference: str
    """Reference to a geoscience object."""
    schema_id: str
    """The ID of the Geoscience Object schema."""

class SyncConverterErsImportResult(SyncTaskResult):
    """Results for the ERS import compute task."""

    object: SyncConverterErsImportResultResultObject
    """The Regular2DGrid object created from the input file."""

class ConverterGeosoftGridImportTarget(TypedDict):
    """Target specifying where to create or update the Regular2DGrid object. Contains 'reference' (File API URL) and optional 'overwrite' (boolean, only valid for path references)."""

    description: NotRequired[str | None]
    """Description to put into the target object."""
    overwrite: NotRequired[bool]
    """Whether to overwrite the existing object."""
    reference: ObjectInput
    """Reference to the targeted object."""
    tags: NotRequired[dict[str, str]]
    """Tags to put into the target object."""

class ConverterGeosoftGridImportResultResultObject(ResultNode):
    """The Regular2DGrid object created from the input Geosoft Grid file."""

    description: str | None
    """The description of the geoscience object."""
    name: str
    """The name of the geoscience object."""
    reference: str
    """Reference to a geoscience object."""
    schema_id: str
    """The ID of the Geoscience Object schema."""

class ConverterGeosoftGridImportResult(TaskResult):
    """Results for the Geosoft Grid import compute task."""

    object: ConverterGeosoftGridImportResultResultObject
    """The Regular2DGrid object created from the input Geosoft Grid file."""

class SyncConverterGeosoftGridImportResultResultObject(SyncResultNode):
    """The Regular2DGrid object created from the input Geosoft Grid file."""

    description: str | None
    """The description of the geoscience object."""
    name: str
    """The name of the geoscience object."""
    reference: str
    """Reference to a geoscience object."""
    schema_id: str
    """The ID of the Geoscience Object schema."""

class SyncConverterGeosoftGridImportResult(SyncTaskResult):
    """Results for the Geosoft Grid import compute task."""

    object: SyncConverterGeosoftGridImportResultResultObject
    """The Regular2DGrid object created from the input Geosoft Grid file."""

class ConverterGeosoftSurfaceImportTarget(TypedDict):
    """Target specifying where to create or update the GeologicalModelMeshes object. Contains 'reference' (File API URL) and optional 'overwrite' (boolean, only valid for path references)."""

    description: NotRequired[str | None]
    """Description to put into the target object."""
    overwrite: NotRequired[bool]
    """Whether to overwrite the existing object."""
    reference: ObjectInput
    """Reference to the targeted object."""
    tags: NotRequired[dict[str, str]]
    """Tags to put into the target object."""

class ConverterGeosoftSurfaceImportResultResultObject(ResultNode):
    """The GeologicalModelMeshes object created from the input file."""

    description: str | None
    """The description of the geoscience object."""
    name: str
    """The name of the geoscience object."""
    reference: str
    """Reference to a geoscience object."""
    schema_id: str
    """The ID of the Geoscience Object schema."""

class ConverterGeosoftSurfaceImportResult(TaskResult):
    """Results for the Geosoft Surface import compute task."""

    object: ConverterGeosoftSurfaceImportResultResultObject
    """The GeologicalModelMeshes object created from the input file."""

class SyncConverterGeosoftSurfaceImportResultResultObject(SyncResultNode):
    """The GeologicalModelMeshes object created from the input file."""

    description: str | None
    """The description of the geoscience object."""
    name: str
    """The name of the geoscience object."""
    reference: str
    """Reference to a geoscience object."""
    schema_id: str
    """The ID of the Geoscience Object schema."""

class SyncConverterGeosoftSurfaceImportResult(SyncTaskResult):
    """Results for the Geosoft Surface import compute task."""

    object: SyncConverterGeosoftSurfaceImportResultResultObject
    """The GeologicalModelMeshes object created from the input file."""

class ConverterGeosoftVoxelImportTarget(TypedDict):
    """Target specifying where to create or update the Regular3DGrid | Tensor3DGrid object. Contains 'reference' (Geoscience Object API URL) and optional 'overwrite' (boolean, only valid for path references)."""

    description: NotRequired[str | None]
    """Description to put into the target object."""
    overwrite: NotRequired[bool]
    """Whether to overwrite the existing object."""
    reference: ObjectInput
    """Reference to the targeted object."""
    tags: NotRequired[dict[str, str]]
    """Tags to put into the target object."""

class ConverterGeosoftVoxelImportResultResultObject(ResultNode):
    """The Regular3DGrid or Tensor3DGrid object created from the input Geosoft Voxel file."""

    description: str | None
    """The description of the geoscience object."""
    name: str
    """The name of the geoscience object."""
    reference: str
    """Reference to a geoscience object."""
    schema_id: str
    """The ID of the Geoscience Object schema."""

class ConverterGeosoftVoxelImportResult(TaskResult):
    """Results for the Geosoft Voxel import compute task."""

    object: ConverterGeosoftVoxelImportResultResultObject
    """The Regular3DGrid or Tensor3DGrid object created from the input Geosoft Voxel file."""

class SyncConverterGeosoftVoxelImportResultResultObject(SyncResultNode):
    """The Regular3DGrid or Tensor3DGrid object created from the input Geosoft Voxel file."""

    description: str | None
    """The description of the geoscience object."""
    name: str
    """The name of the geoscience object."""
    reference: str
    """Reference to a geoscience object."""
    schema_id: str
    """The ID of the Geoscience Object schema."""

class SyncConverterGeosoftVoxelImportResult(SyncTaskResult):
    """Results for the Geosoft Voxel import compute task."""

    object: SyncConverterGeosoftVoxelImportResultResultObject
    """The Regular3DGrid or Tensor3DGrid object created from the input Geosoft Voxel file."""

class ConverterImageImportTarget(TypedDict):
    """Target specifying where to create or update the Regular2DGrid object. Contains 'reference' (Geoscience Object API URL) and optional 'overwrite' (boolean, only valid for path references)."""

    description: NotRequired[str | None]
    """Description to put into the target object."""
    overwrite: NotRequired[bool]
    """Whether to overwrite the existing object."""
    reference: ObjectInput
    """Reference to the targeted object."""
    tags: NotRequired[dict[str, str]]
    """Tags to put into the target object."""

class ConverterImageImportResultValues(ResultNode):
    """Continuous cell attribute containing imported image pixel values."""

    name: str
    """The name of the output attribute."""
    reference: str
    """Reference to the attribute in the geoscience object."""

class ConverterImageImportResultResultObject(ResultNode):
    """The Regular2DGrid object created from the input file."""

    description: str | None
    """The description of the geoscience object."""
    name: str
    """The name of the geoscience object."""
    reference: str
    """Reference to a geoscience object."""
    schema_id: str
    """The ID of the Geoscience Object schema."""
    values: ConverterImageImportResultValues
    """Continuous cell attribute containing imported image pixel values."""

class ConverterImageImportResult(TaskResult):
    """Results for the image import compute task."""

    object: ConverterImageImportResultResultObject
    """The Regular2DGrid object created from the input file."""

class SyncConverterImageImportResultValues(SyncResultNode):
    """Continuous cell attribute containing imported image pixel values."""

    name: str
    """The name of the output attribute."""
    reference: str
    """Reference to the attribute in the geoscience object."""

class SyncConverterImageImportResultResultObject(SyncResultNode):
    """The Regular2DGrid object created from the input file."""

    description: str | None
    """The description of the geoscience object."""
    name: str
    """The name of the geoscience object."""
    reference: str
    """Reference to a geoscience object."""
    schema_id: str
    """The ID of the Geoscience Object schema."""
    values: SyncConverterImageImportResultValues
    """Continuous cell attribute containing imported image pixel values."""

class SyncConverterImageImportResult(SyncTaskResult):
    """Results for the image import compute task."""

    object: SyncConverterImageImportResultResultObject
    """The Regular2DGrid object created from the input file."""

class ConverterObjImportTarget(TypedDict):
    """Target specifying where to create or update the Triangle Mesh object. Contains 'reference' (File API URL) and optional 'overwrite' (boolean, only valid for path references)."""

    description: NotRequired[str | None]
    """Description to put into the target object."""
    overwrite: NotRequired[bool]
    """Whether to overwrite the existing object."""
    reference: ObjectInput
    """Reference to the targeted object."""
    tags: NotRequired[dict[str, str]]
    """Tags to put into the target object."""

class ConverterObjImportResultResultObject(ResultNode):
    """The Triangle Mesh object created from the input file."""

    description: str | None
    """The description of the geoscience object."""
    name: str
    """The name of the geoscience object."""
    reference: str
    """Reference to a geoscience object."""
    schema_id: str
    """The ID of the Geoscience Object schema."""

class ConverterObjImportResult(TaskResult):
    """Results for the OBJ import compute task."""

    object: ConverterObjImportResultResultObject
    """The Triangle Mesh object created from the input file."""

class SyncConverterObjImportResultResultObject(SyncResultNode):
    """The Triangle Mesh object created from the input file."""

    description: str | None
    """The description of the geoscience object."""
    name: str
    """The name of the geoscience object."""
    reference: str
    """Reference to a geoscience object."""
    schema_id: str
    """The ID of the Geoscience Object schema."""

class SyncConverterObjImportResult(SyncTaskResult):
    """Results for the OBJ import compute task."""

    object: SyncConverterObjImportResultResultObject
    """The Triangle Mesh object created from the input file."""

class ConverterShpImportTarget(TypedDict):
    """Target specifying where to create or update the imported geoscience object. Contains 'reference' (File API URL) and optional 'overwrite' (boolean, only valid for path references)."""

    description: NotRequired[str | None]
    """Description to put into the target object."""
    overwrite: NotRequired[bool]
    """Whether to overwrite the existing object."""
    reference: ObjectInput
    """Reference to the targeted object."""
    tags: NotRequired[dict[str, str]]
    """Tags to put into the target object."""

class ConverterShpImportResultResultObject(ResultNode):
    """The geoscience object created from the input file(s)."""

    description: str | None
    """The description of the geoscience object."""
    name: str
    """The name of the geoscience object."""
    reference: str
    """Reference to a geoscience object."""
    schema_id: str
    """The ID of the Geoscience Object schema."""

class ConverterShpImportResult(TaskResult):
    """Results for the SHP import compute task."""

    object: ConverterShpImportResultResultObject
    """The geoscience object created from the input file(s)."""

class SyncConverterShpImportResultResultObject(SyncResultNode):
    """The geoscience object created from the input file(s)."""

    description: str | None
    """The description of the geoscience object."""
    name: str
    """The name of the geoscience object."""
    reference: str
    """Reference to a geoscience object."""
    schema_id: str
    """The ID of the Geoscience Object schema."""

class SyncConverterShpImportResult(SyncTaskResult):
    """Results for the SHP import compute task."""

    object: SyncConverterShpImportResultResultObject
    """The geoscience object created from the input file(s)."""

class ConverterUbcImportTarget(TypedDict):
    """Target specifying where to create or update the Tensor Grid object. Contains 'reference' (File API URL) and optional 'overwrite' (boolean, only valid for path references)."""

    description: NotRequired[str | None]
    """Description to put into the target object."""
    overwrite: NotRequired[bool]
    """Whether to overwrite the existing object."""
    reference: ObjectInput
    """Reference to the targeted object."""
    tags: NotRequired[dict[str, str]]
    """Tags to put into the target object."""

class ConverterUbcImportResultResultObject(ResultNode):
    """The Tensor Grid object created from the input file(s)."""

    description: str | None
    """The description of the geoscience object."""
    name: str
    """The name of the geoscience object."""
    reference: str
    """Reference to a geoscience object."""
    schema_id: str
    """The ID of the Geoscience Object schema."""

class ConverterUbcImportResult(TaskResult):
    """Results for the UBC import compute task."""

    object: ConverterUbcImportResultResultObject
    """The Tensor Grid object created from the input file(s)."""

class SyncConverterUbcImportResultResultObject(SyncResultNode):
    """The Tensor Grid object created from the input file(s)."""

    description: str | None
    """The description of the geoscience object."""
    name: str
    """The name of the geoscience object."""
    reference: str
    """Reference to a geoscience object."""
    schema_id: str
    """The ID of the Geoscience Object schema."""

class SyncConverterUbcImportResult(SyncTaskResult):
    """Results for the UBC import compute task."""

    object: SyncConverterUbcImportResultResultObject
    """The Tensor Grid object created from the input file(s)."""

class ConverterVtkImportResultResultObjectsItem(ResultNode):
    """A Geoscience Object that was extracted from the input files"""

    description: str | None
    """The description of the geoscience object."""
    name: str
    """The name of the geoscience object."""
    reference: str
    """Reference to a geoscience object."""
    schema_id: str
    """The ID of the Geoscience Object schema."""

class ConverterVtkImportResult(TaskResult):
    """Results for the VTK import compute task"""

    objects: list[ConverterVtkImportResultResultObjectsItem]
    """List of Geoscience objects that was extracted from the input files."""

class SyncConverterVtkImportResultResultObjectsItem(SyncResultNode):
    """A Geoscience Object that was extracted from the input files"""

    description: str | None
    """The description of the geoscience object."""
    name: str
    """The name of the geoscience object."""
    reference: str
    """Reference to a geoscience object."""
    schema_id: str
    """The ID of the Geoscience Object schema."""

class SyncConverterVtkImportResult(SyncTaskResult):
    """Results for the VTK import compute task"""

    objects: list[SyncConverterVtkImportResultResultObjectsItem]
    """List of Geoscience objects that was extracted from the input files."""

class ConverterXyzImportTarget(TypedDict):
    """Target specifying where to create or update the Pointset object. Contains 'reference' (File API URL) and optional 'overwrite' (boolean, only valid for path references)."""

    description: NotRequired[str | None]
    """Description to put into the target object."""
    overwrite: NotRequired[bool]
    """Whether to overwrite the existing object."""
    reference: ObjectInput
    """Reference to the targeted object."""
    tags: NotRequired[dict[str, str]]
    """Tags to put into the target object."""

class ConverterXyzImportResultResultObject(ResultNode):
    """The Pointset object created from the input file."""

    description: str | None
    """The description of the geoscience object."""
    name: str
    """The name of the geoscience object."""
    reference: str
    """Reference to a geoscience object."""
    schema_id: str
    """The ID of the Geoscience Object schema."""

class ConverterXyzImportResult(TaskResult):
    """Results for the XYZ import compute task."""

    object: ConverterXyzImportResultResultObject
    """The Pointset object created from the input file."""

class SyncConverterXyzImportResultResultObject(SyncResultNode):
    """The Pointset object created from the input file."""

    description: str | None
    """The description of the geoscience object."""
    name: str
    """The name of the geoscience object."""
    reference: str
    """Reference to a geoscience object."""
    schema_id: str
    """The ID of the Geoscience Object schema."""

class SyncConverterXyzImportResult(SyncTaskResult):
    """Results for the XYZ import compute task."""

    object: SyncConverterXyzImportResultResultObject
    """The Pointset object created from the input file."""

class CptToBoreholeCptReaderTargetDownholeCollection(TypedDict):
    """DownHoleCollection object to which the parsed results will be written."""

    description: NotRequired[str | None]
    """Description to put into the target object."""
    overwrite: NotRequired[bool]
    """Whether to overwrite the existing object."""
    reference: ObjectInput
    """Reference to the targeted object."""
    tags: NotRequired[dict[str, str]]
    """Tags to put into the target object."""

class CptToBoreholeCptReaderResultResultDownholeCollection(ResultNode):
    """The Downhole Collection object created or updated from the CPT file."""

    description: str | None
    """The description of the geoscience object."""
    name: str
    """The name of the geoscience object."""
    reference: str
    """Reference to a geoscience object."""
    schema_id: str
    """The ID of the Geoscience Object schema."""

class CptToBoreholeCptReaderResultResultMessagesItem(ResultNode):
    """A message produced by the task during execution."""

    code: Literal[100, 101, 102, 103, 104]
    """Integer code that uniquely identifies this message type within its task. Allows consumers to handle specific messages without relying on text matching."""
    level: Literal["info", "warning", "error"]
    """The level of the message, e.g., 'info', 'warning', 'error'."""
    message: str
    """The content of the message."""
    object_id: str | None
    """Optional identifier of the object (e.g., hole ID) this message relates to. None for global task-level messages."""

class CptToBoreholeCptReaderResult(TaskResult):
    """Result of the cpt_reader task."""

    downhole_collection: CptToBoreholeCptReaderResultResultDownholeCollection
    """The Downhole Collection object created or updated from the CPT file."""
    messages: list[CptToBoreholeCptReaderResultResultMessagesItem]
    """List of messages generated during the CPT reading process."""

class SyncCptToBoreholeCptReaderResultResultDownholeCollection(SyncResultNode):
    """The Downhole Collection object created or updated from the CPT file."""

    description: str | None
    """The description of the geoscience object."""
    name: str
    """The name of the geoscience object."""
    reference: str
    """Reference to a geoscience object."""
    schema_id: str
    """The ID of the Geoscience Object schema."""

class SyncCptToBoreholeCptReaderResultResultMessagesItem(SyncResultNode):
    """A message produced by the task during execution."""

    code: Literal[100, 101, 102, 103, 104]
    """Integer code that uniquely identifies this message type within its task. Allows consumers to handle specific messages without relying on text matching."""
    level: Literal["info", "warning", "error"]
    """The level of the message, e.g., 'info', 'warning', 'error'."""
    message: str
    """The content of the message."""
    object_id: str | None
    """Optional identifier of the object (e.g., hole ID) this message relates to. None for global task-level messages."""

class SyncCptToBoreholeCptReaderResult(SyncTaskResult):
    """Result of the cpt_reader task."""

    downhole_collection: SyncCptToBoreholeCptReaderResultResultDownholeCollection
    """The Downhole Collection object created or updated from the CPT file."""
    messages: list[SyncCptToBoreholeCptReaderResultResultMessagesItem]
    """List of messages generated during the CPT reading process."""

class CptToBoreholeCreateClustersResultResultDownholeCollection(ResultNode):
    """Reference to the DownholeCollection object that was created or updated which will hold the results of clusters creation."""

    description: str | None
    """The description of the geoscience object."""
    name: str
    """The name of the geoscience object."""
    reference: str
    """Reference to a geoscience object."""
    schema_id: str
    """The ID of the Geoscience Object schema."""

class CptToBoreholeCreateClustersResultResultInertiaSilhouetteScore(ResultNode):
    """Chart data for the inertia and silhouette score clustering diagnostics."""

    cluster_counts: list[int]
    """Candidate cluster counts evaluated by the clustering algorithm."""
    inertias: list[float]
    """KMeans inertia values for each candidate cluster count."""
    optimal_number_of_clusters: int
    """Optimal number of clusters selected by the clustering algorithm."""
    silhouette_scores: list[float]
    """Silhouette scores for each candidate cluster count."""

class CptToBoreholeCreateClustersResultResultMessagesItem(ResultNode):
    """A message produced by the task during execution."""

    code: Literal[300, 301, 302, 305, 307, 309, 310]
    """Integer code that uniquely identifies this message type within its task. Allows consumers to handle specific messages without relying on text matching."""
    level: Literal["info", "warning", "error"]
    """The level of the message, e.g., 'info', 'warning', 'error'."""
    message: str
    """The content of the message."""
    object_id: str | None
    """Optional identifier of the object (e.g., hole ID) this message relates to. None for global task-level messages."""

class CptToBoreholeCreateClustersResult(TaskResult):
    """Result of the "Create clusters" task."""

    downhole_collection: CptToBoreholeCreateClustersResultResultDownholeCollection
    """Reference to the DownholeCollection object that was created or updated which will hold the results of clusters creation."""
    inertia_silhouette_score: CptToBoreholeCreateClustersResultResultInertiaSilhouetteScore | None
    """Chart data for the inertia and silhouette score clustering diagnostics."""
    messages: list[CptToBoreholeCreateClustersResultResultMessagesItem]
    """List of messages generated during the clustering process."""

class SyncCptToBoreholeCreateClustersResultResultDownholeCollection(SyncResultNode):
    """Reference to the DownholeCollection object that was created or updated which will hold the results of clusters creation."""

    description: str | None
    """The description of the geoscience object."""
    name: str
    """The name of the geoscience object."""
    reference: str
    """Reference to a geoscience object."""
    schema_id: str
    """The ID of the Geoscience Object schema."""

class SyncCptToBoreholeCreateClustersResultResultInertiaSilhouetteScore(SyncResultNode):
    """Chart data for the inertia and silhouette score clustering diagnostics."""

    cluster_counts: list[int]
    """Candidate cluster counts evaluated by the clustering algorithm."""
    inertias: list[float]
    """KMeans inertia values for each candidate cluster count."""
    optimal_number_of_clusters: int
    """Optimal number of clusters selected by the clustering algorithm."""
    silhouette_scores: list[float]
    """Silhouette scores for each candidate cluster count."""

class SyncCptToBoreholeCreateClustersResultResultMessagesItem(SyncResultNode):
    """A message produced by the task during execution."""

    code: Literal[300, 301, 302, 305, 307, 309, 310]
    """Integer code that uniquely identifies this message type within its task. Allows consumers to handle specific messages without relying on text matching."""
    level: Literal["info", "warning", "error"]
    """The level of the message, e.g., 'info', 'warning', 'error'."""
    message: str
    """The content of the message."""
    object_id: str | None
    """Optional identifier of the object (e.g., hole ID) this message relates to. None for global task-level messages."""

class SyncCptToBoreholeCreateClustersResult(SyncTaskResult):
    """Result of the "Create clusters" task."""

    downhole_collection: SyncCptToBoreholeCreateClustersResultResultDownholeCollection
    """Reference to the DownholeCollection object that was created or updated which will hold the results of clusters creation."""
    inertia_silhouette_score: SyncCptToBoreholeCreateClustersResultResultInertiaSilhouetteScore | None
    """Chart data for the inertia and silhouette score clustering diagnostics."""
    messages: list[SyncCptToBoreholeCreateClustersResultResultMessagesItem]
    """List of messages generated during the clustering process."""

class CptToBoreholePublishTargetDownholeCollection(TypedDict):
    """The geoscience object where the published DownholeCollection is written as a new object."""

    description: NotRequired[str | None]
    """Description to put into the target object."""
    overwrite: NotRequired[bool]
    """Whether to overwrite the existing object."""
    reference: ObjectInput
    """Reference to the targeted object."""
    tags: NotRequired[dict[str, str]]
    """Tags to put into the target object."""

class CptToBoreholePublishClusterMetadata(TypedDict):
    """Human-assigned name and colour for a single cluster."""

    color: list[int]
    """RGB colour of the cluster as three integers in the range 0-255."""
    name: str
    """Human-assigned name for the cluster."""

class CptToBoreholePublishResultResultDownholeCollection(ResultNode):
    """Reference to the newly published DownholeCollection geoscience object."""

    description: str | None
    """The description of the geoscience object."""
    name: str
    """The name of the geoscience object."""
    reference: str
    """Reference to a geoscience object."""
    schema_id: str
    """The ID of the Geoscience Object schema."""

class CptToBoreholePublishResultResultMessagesItem(ResultNode):
    """A message produced by the task during execution."""

    code: Literal[601, 602, 603, 604, 605]
    """Integer code that uniquely identifies this message type within its task. Allows consumers to handle specific messages without relying on text matching."""
    level: Literal["info", "warning", "error"]
    """The level of the message, e.g., 'info', 'warning', 'error'."""
    message: str
    """The content of the message."""
    object_id: str | None
    """Optional identifier of the object (e.g., hole ID) this message relates to. None for global task-level messages."""

class CptToBoreholePublishResult(TaskResult):
    """Result of the publish task."""

    downhole_collection: CptToBoreholePublishResultResultDownholeCollection
    """Reference to the newly published DownholeCollection geoscience object."""
    messages: list[CptToBoreholePublishResultResultMessagesItem]
    """List of messages generated during the publish process."""

class SyncCptToBoreholePublishResultResultDownholeCollection(SyncResultNode):
    """Reference to the newly published DownholeCollection geoscience object."""

    description: str | None
    """The description of the geoscience object."""
    name: str
    """The name of the geoscience object."""
    reference: str
    """Reference to a geoscience object."""
    schema_id: str
    """The ID of the Geoscience Object schema."""

class SyncCptToBoreholePublishResultResultMessagesItem(SyncResultNode):
    """A message produced by the task during execution."""

    code: Literal[601, 602, 603, 604, 605]
    """Integer code that uniquely identifies this message type within its task. Allows consumers to handle specific messages without relying on text matching."""
    level: Literal["info", "warning", "error"]
    """The level of the message, e.g., 'info', 'warning', 'error'."""
    message: str
    """The content of the message."""
    object_id: str | None
    """Optional identifier of the object (e.g., hole ID) this message relates to. None for global task-level messages."""

class SyncCptToBoreholePublishResult(SyncTaskResult):
    """Result of the publish task."""

    downhole_collection: SyncCptToBoreholePublishResultResultDownholeCollection
    """Reference to the newly published DownholeCollection geoscience object."""
    messages: list[SyncCptToBoreholePublishResultResultMessagesItem]
    """List of messages generated during the publish process."""

class CptToBoreholeStratifyTargetDownholeCollection(TypedDict):
    """DownHoleCollection object to which results are written."""

    description: NotRequired[str | None]
    """Description to put into the target object."""
    overwrite: NotRequired[bool]
    """Whether to overwrite the existing object."""
    reference: ObjectInput
    """Reference to the targeted object."""
    tags: NotRequired[dict[str, str]]
    """Tags to put into the target object."""

class CptToBoreholeStratifyPerHoleParameters(TypedDict):
    """Parameters for each CPT/hole to be stratified."""

    custom_number_of_layers: NotRequired[int | None]
    """Custom number of layers to use for the stratification of the CPT/hole."""
    filtering_method: NotRequired[Literal["no_filtering", "inverse_filtering"]]
    """Filtering method to use for the classification of the CPT/hole."""
    hole_id: str
    """Identifier for the CPT/hole to be classified and stratified."""
    layering_tolerance: NotRequired[float]
    """Layering tolerance to use for the stratification of the CPT/hole."""
    maximum_number_of_layers: NotRequired[int]
    """Maximum number of layers to use for the stratification of the CPT/hole."""
    minimum_layer_thickness: NotRequired[float]
    """Minimum layer thickness to use for the stratification of the CPT/hole."""
    name: NotRequired[str | None]
    """A new name for the CPT/hole (this will change the hole_id to the desired name)."""
    net_area_ratio: NotRequired[float | None]
    """Cone area ratio for this CPT/hole."""
    stratification_method: NotRequired[Literal["average", "std_dev", "machine_learning"]]
    """Stratification method to use for the stratification of the CPT/hole."""
    water_depth: NotRequired[float | None]
    """Water depth at the location of this CPT/hole."""

class CptToBoreholeStratifyResultResultDownholeCollection(ResultNode):
    """Reference to the DownholeCollection object that was created or updated which will hold the results of the stratification and classification."""

    description: str | None
    """The description of the geoscience object."""
    name: str
    """The name of the geoscience object."""
    reference: str
    """Reference to a geoscience object."""
    schema_id: str
    """The ID of the Geoscience Object schema."""

class CptToBoreholeStratifyResultResultMessagesItem(ResultNode):
    """A message produced by the task during execution."""

    code: Literal[200, 201, 203, 204, 205, 206, 207, 208, 209]
    """Integer code that uniquely identifies this message type within its task. Allows consumers to handle specific messages without relying on text matching."""
    level: Literal["info", "warning", "error"]
    """The level of the message, e.g., 'info', 'warning', 'error'."""
    message: str
    """The content of the message."""
    object_id: str | None
    """Optional identifier of the object (e.g., hole ID) this message relates to. None for global task-level messages."""

class CptToBoreholeStratifyResultNormalizedCostPoint(ResultNode):
    """One point on the normalized cost variation chart."""

    cost: float
    """Normalized cost value for the candidate number of layers."""
    number_of_layers: int
    """Candidate number of layers used for the normalized cost calculation."""

class CptToBoreholeStratifyResultNormalizedCostVariationEntry(ResultNode):
    """Normalized cost variation chart data for one hole."""

    object_id: str
    """Identifier of the object (meaning the hole id, e.g. "CPT_1") this normalized cost variation entry relates to."""
    optimal_number_of_layers: int
    """Optimal number of layers selected by the machine learning stratification method."""
    points: list[CptToBoreholeStratifyResultNormalizedCostPoint]
    """Chart points for the normalized cost variation curve."""

class CptToBoreholeStratifyResult(TaskResult):
    """Result of the stratify task."""

    downhole_collection: CptToBoreholeStratifyResultResultDownholeCollection
    """Reference to the DownholeCollection object that was created or updated which will hold the results of the stratification and classification."""
    messages: list[CptToBoreholeStratifyResultResultMessagesItem]
    """List of messages generated during the stratification process."""
    normalized_cost_variation: list[CptToBoreholeStratifyResultNormalizedCostVariationEntry]
    """Per-hole normalized cost variation data for holes stratified with the machine learning method."""

class SyncCptToBoreholeStratifyResultResultDownholeCollection(SyncResultNode):
    """Reference to the DownholeCollection object that was created or updated which will hold the results of the stratification and classification."""

    description: str | None
    """The description of the geoscience object."""
    name: str
    """The name of the geoscience object."""
    reference: str
    """Reference to a geoscience object."""
    schema_id: str
    """The ID of the Geoscience Object schema."""

class SyncCptToBoreholeStratifyResultResultMessagesItem(SyncResultNode):
    """A message produced by the task during execution."""

    code: Literal[200, 201, 203, 204, 205, 206, 207, 208, 209]
    """Integer code that uniquely identifies this message type within its task. Allows consumers to handle specific messages without relying on text matching."""
    level: Literal["info", "warning", "error"]
    """The level of the message, e.g., 'info', 'warning', 'error'."""
    message: str
    """The content of the message."""
    object_id: str | None
    """Optional identifier of the object (e.g., hole ID) this message relates to. None for global task-level messages."""

class SyncCptToBoreholeStratifyResultNormalizedCostPoint(SyncResultNode):
    """One point on the normalized cost variation chart."""

    cost: float
    """Normalized cost value for the candidate number of layers."""
    number_of_layers: int
    """Candidate number of layers used for the normalized cost calculation."""

class SyncCptToBoreholeStratifyResultNormalizedCostVariationEntry(SyncResultNode):
    """Normalized cost variation chart data for one hole."""

    object_id: str
    """Identifier of the object (meaning the hole id, e.g. "CPT_1") this normalized cost variation entry relates to."""
    optimal_number_of_layers: int
    """Optimal number of layers selected by the machine learning stratification method."""
    points: list[SyncCptToBoreholeStratifyResultNormalizedCostPoint]
    """Chart points for the normalized cost variation curve."""

class SyncCptToBoreholeStratifyResult(SyncTaskResult):
    """Result of the stratify task."""

    downhole_collection: SyncCptToBoreholeStratifyResultResultDownholeCollection
    """Reference to the DownholeCollection object that was created or updated which will hold the results of the stratification and classification."""
    messages: list[SyncCptToBoreholeStratifyResultResultMessagesItem]
    """List of messages generated during the stratification process."""
    normalized_cost_variation: list[SyncCptToBoreholeStratifyResultNormalizedCostVariationEntry]
    """Per-hole normalized cost variation data for holes stratified with the machine learning method."""

class DriverDriverGeneralComputeResult(TaskResult):
    """Results of the general compute task."""

    output: str
    """The result of the computation."""

class SyncDriverDriverGeneralComputeResult(SyncTaskResult):
    """Results of the general compute task."""

    output: str
    """The result of the computation."""

class GeophysicsGravityTerrainCorrectionTerrainLayer(TypedDict):
    """Parameters for the terrain layer, including density and reference to elevation attribute. In the absence of a specified water layer, the terrain represents the topography. If a water layer is provided, the terrain represents the elevation of the top of the solid earth (the base of the water layer)."""

    density: float
    """Density of the layer [kg/m^3]"""
    elevation: AttributeInput
    """Reference to an attribute of a 2D grid representing the elevations of a layer defining a stratigraphic surface. Elevations [m] correspond to the same third coordinate axis as used in the observation points."""

class GeophysicsGravityTerrainCorrectionWaterLayer(TypedDict):
    """Optional: Parameters for a water layer to be used in the model. If provided, the terrain layer represents the elevation of the top of the solid earth (the base of the water layer). At locations without water, terrain and water elevations should be identical. If not provided, no water layer is modeled."""

    density: float
    """Density of the layer [kg/m^3]"""
    elevation: AttributeInput
    """Reference to an attribute of a 2D grid representing the elevations of a layer defining a stratigraphic surface. Elevations [m] correspond to the same third coordinate axis as used in the observation points."""

class GeophysicsGravityTerrainCorrectionResultResultTargetTerrainCorrection(ResultNode):
    """Attribute containing the terrain correction associated with each location at the input observation points. The terrain correction corresponds to the vertical gravity component. Unit is in m/s² and corresponds to the coordinate system of the observation points where z-axis is positive upwards. This means that the terrain correction values should be multiplied by -1 if applied to gravity observations assuming a z-axis positive downwards."""

    name: str
    """The name of the output attribute."""
    reference: str
    """Reference to the attribute in the geoscience object."""

class GeophysicsGravityTerrainCorrectionResultResultTarget(ResultNode):
    """The object containing the computed terrain correction values."""

    description: str | None
    """The description of the geoscience object."""
    name: str
    """The name of the geoscience object."""
    reference: str
    """Reference to a geoscience object."""
    schema_id: str
    """The ID of the Geoscience Object schema."""
    terrain_correction: GeophysicsGravityTerrainCorrectionResultResultTargetTerrainCorrection
    """Attribute containing the terrain correction associated with each location at the input observation points. The terrain correction corresponds to the vertical gravity component. Unit is in m/s² and corresponds to the coordinate system of the observation points where z-axis is positive upwards. This means that the terrain correction values should be multiplied by -1 if applied to gravity observations assuming a z-axis positive downwards."""

class GeophysicsGravityTerrainCorrectionResult(TaskResult):
    """Results of the terrain correction task."""

    target: GeophysicsGravityTerrainCorrectionResultResultTarget
    """The object containing the computed terrain correction values."""

class SyncGeophysicsGravityTerrainCorrectionResultResultTargetTerrainCorrection(SyncResultNode):
    """Attribute containing the terrain correction associated with each location at the input observation points. The terrain correction corresponds to the vertical gravity component. Unit is in m/s² and corresponds to the coordinate system of the observation points where z-axis is positive upwards. This means that the terrain correction values should be multiplied by -1 if applied to gravity observations assuming a z-axis positive downwards."""

    name: str
    """The name of the output attribute."""
    reference: str
    """Reference to the attribute in the geoscience object."""

class SyncGeophysicsGravityTerrainCorrectionResultResultTarget(SyncResultNode):
    """The object containing the computed terrain correction values."""

    description: str | None
    """The description of the geoscience object."""
    name: str
    """The name of the geoscience object."""
    reference: str
    """Reference to a geoscience object."""
    schema_id: str
    """The ID of the Geoscience Object schema."""
    terrain_correction: SyncGeophysicsGravityTerrainCorrectionResultResultTargetTerrainCorrection
    """Attribute containing the terrain correction associated with each location at the input observation points. The terrain correction corresponds to the vertical gravity component. Unit is in m/s² and corresponds to the coordinate system of the observation points where z-axis is positive upwards. This means that the terrain correction values should be multiplied by -1 if applied to gravity observations assuming a z-axis positive downwards."""

class SyncGeophysicsGravityTerrainCorrectionResult(SyncTaskResult):
    """Results of the terrain correction task."""

    target: SyncGeophysicsGravityTerrainCorrectionResultResultTarget
    """The object containing the computed terrain correction values."""

class GeophysicsRegular2dGridToTrimeshSourceInput(TypedDict):
    """Source grid and attribute"""

    attribute: AttributeInput
    """Scalar attribute to use as Z values for the triangle mesh"""
    object: ObjectInput
    """Input regular 2D grid containing the scalar attribute to use as Z values"""

class GeophysicsRegular2dGridToTrimeshTarget(TypedDict):
    """Where to create the output Triangle Mesh object"""

    description: NotRequired[str | None]
    """Description to put into the target object."""
    overwrite: NotRequired[bool]
    """Whether to overwrite the existing object."""
    reference: ObjectInput
    """Reference to the targeted object."""
    tags: NotRequired[dict[str, str]]
    """Tags to put into the target object."""

class GeophysicsRegular2dGridToTrimeshSettings(TypedDict):
    """Optional settings for mesh generation"""

    drop_nan_patches: NotRequired[bool]
    """Exclude triangles that touch NaN attribute values"""
    flip_triangles: NotRequired[bool]
    """Reverse triangle winding order (flip normals)"""
    triMeshVersion: NotRequired[Literal["2.1", "2.2"]]
    """Triangle mesh schema version for output payload. Use 2.1 for Oasis Montaj compatibility or 2.2 for latest schema."""
    use_centroids: NotRequired[bool]
    """Add a centroid vertex to each quad and split into 4 triangles instead of 2, producing a smoother surface at the cost of twice as many triangles"""

class GeophysicsRegular2dGridToTrimeshResultResultOutput(ResultNode):
    """Reference to the created Triangle Mesh object."""

    description: str | None
    """The description of the geoscience object."""
    name: str
    """The name of the geoscience object."""
    reference: str
    """Reference to a geoscience object."""
    schema_id: str
    """The ID of the Geoscience Object schema."""

class GeophysicsRegular2dGridToTrimeshResultSummary(ResultNode):
    """Summary statistics for the generated triangle mesh."""

    has_non_manifold_edges: bool
    """Always false — splitting a regular grid into triangles cannot produce edges shared by more than 2 triangles"""
    has_unused_vertices: bool
    """Whether the mesh has orphan vertices not referenced by any triangle"""
    num_triangles: int
    """Number of triangles in the generated mesh"""
    num_vertices: int
    """Number of vertices in the mesh vertex array"""

class GeophysicsRegular2dGridToTrimeshResult(TaskResult):
    """Results of the regular-2d-grid-to-trimesh task."""

    has_nan: bool
    """True if the source attribute contains NaN values."""
    is_rotated: bool
    """True if the source grid has a non-identity rotation."""
    nan_count: int
    """Number of NaN values in the source attribute."""
    output: GeophysicsRegular2dGridToTrimeshResultResultOutput | None
    """Reference to the created Triangle Mesh object."""
    summary: GeophysicsRegular2dGridToTrimeshResultSummary | None
    """Summary statistics for the generated triangle mesh."""

class SyncGeophysicsRegular2dGridToTrimeshResultResultOutput(SyncResultNode):
    """Reference to the created Triangle Mesh object."""

    description: str | None
    """The description of the geoscience object."""
    name: str
    """The name of the geoscience object."""
    reference: str
    """Reference to a geoscience object."""
    schema_id: str
    """The ID of the Geoscience Object schema."""

class SyncGeophysicsRegular2dGridToTrimeshResultSummary(SyncResultNode):
    """Summary statistics for the generated triangle mesh."""

    has_non_manifold_edges: bool
    """Always false — splitting a regular grid into triangles cannot produce edges shared by more than 2 triangles"""
    has_unused_vertices: bool
    """Whether the mesh has orphan vertices not referenced by any triangle"""
    num_triangles: int
    """Number of triangles in the generated mesh"""
    num_vertices: int
    """Number of vertices in the mesh vertex array"""

class SyncGeophysicsRegular2dGridToTrimeshResult(SyncTaskResult):
    """Results of the regular-2d-grid-to-trimesh task."""

    has_nan: bool
    """True if the source attribute contains NaN values."""
    is_rotated: bool
    """True if the source grid has a non-identity rotation."""
    nan_count: int
    """Number of NaN values in the source attribute."""
    output: SyncGeophysicsRegular2dGridToTrimeshResultResultOutput | None
    """Reference to the created Triangle Mesh object."""
    summary: SyncGeophysicsRegular2dGridToTrimeshResultSummary | None
    """Summary statistics for the generated triangle mesh."""

class GeostatisticsBreakTiesSource(TypedDict):
    """Data in which ties will be broken."""

    attribute: AttributeInput
    """The known values, used to estimate values at unknown locations."""
    object: ObjectInput
    """Object that contains the data in which ties will be broken."""

class GeostatisticsBreakTiesTargetAttributeCreate(TypedDict):
    name: str
    """The name of the attribute to create."""
    operation: Literal["create"]
    """The operation to perform on the attribute."""

class GeostatisticsBreakTiesTargetAttributeUpdate(TypedDict):
    operation: Literal["update"]
    """The operation to perform on the attribute."""
    reference: AttributeInput
    """Reference to an existing attribute to update."""

class GeostatisticsBreakTiesTarget(TypedDict):
    """Object and attribute where the results will be written."""

    attribute: (
        GeostatisticsBreakTiesTargetAttributeCreate | GeostatisticsBreakTiesTargetAttributeUpdate | AttributeInput
    )
    """The attribute to write the results to."""
    object: ObjectInput
    """Object where the results will be written."""

class GeostatisticsBreakTiesNeighborhoodEllipsoidEllipsoidRanges(TypedDict):
    """The ranges of the ellipsoid"""

    major: float
    """The major axis length of the ellipsoid"""
    minor: float
    """The minor axis length of the ellipsoid"""
    semi_major: float
    """The semi major axis length of the ellipsoid"""

class GeostatisticsBreakTiesRotation(TypedDict):
    """The rotation of the ellipsoid"""

    dip: NotRequired[float]
    """second rotation, about the x-axis, in degrees"""
    dip_azimuth: NotRequired[float]
    """first rotation, about the z-axis, in degrees"""
    pitch: NotRequired[float]
    """third rotation, about the z-axis, in degrees"""

class GeostatisticsBreakTiesEllipsoid(TypedDict):
    """The ellipsoid, to search for points within"""

    ellipsoid_ranges: GeostatisticsBreakTiesNeighborhoodEllipsoidEllipsoidRanges
    """The ranges of the ellipsoid"""
    rotation: GeostatisticsBreakTiesRotation
    """The rotation of the ellipsoid"""

class GeostatisticsBreakTiesBreakTiesSearch(TypedDict):
    """
    Search parameters.

    In this break ties algorithm, the final value of each tied point is influenced by the mean of nearby points.
    The search parameters determines which nearby points to use.
    """

    ellipsoid: GeostatisticsBreakTiesEllipsoid
    """The ellipsoid, to search for points within"""
    max_samples: int
    """The maximum number of neighbors to find for each tied point."""
    min_samples: NotRequired[int]
    """The minimum number of samples to use for each evaluation point."""

class GeostatisticsBreakTiesResultAttribute(ResultNode):
    """Attribute containing the break ties result."""

    name: str
    """The name of the output attribute."""
    reference: str
    """Reference to the attribute in the geoscience object."""

class GeostatisticsBreakTiesResultTarget(ResultNode):
    """The target that was created or updated."""

    attribute: GeostatisticsBreakTiesResultAttribute
    """Attribute containing the break ties result."""
    description: str | None
    """The description of the geoscience object."""
    name: str
    """The name of the geoscience object."""
    reference: str
    """Reference to a geoscience object."""
    schema_id: str
    """The ID of the Geoscience Object schema."""

class GeostatisticsBreakTiesResult(TaskResult):
    """Result of the break ties task."""

    message: str
    """A message that says what happened in the task."""
    target: GeostatisticsBreakTiesResultTarget
    """The target that was created or updated."""

class SyncGeostatisticsBreakTiesResultAttribute(SyncResultNode):
    """Attribute containing the break ties result."""

    name: str
    """The name of the output attribute."""
    reference: str
    """Reference to the attribute in the geoscience object."""

class SyncGeostatisticsBreakTiesResultTarget(SyncResultNode):
    """The target that was created or updated."""

    attribute: SyncGeostatisticsBreakTiesResultAttribute
    """Attribute containing the break ties result."""
    description: str | None
    """The description of the geoscience object."""
    name: str
    """The name of the geoscience object."""
    reference: str
    """Reference to a geoscience object."""
    schema_id: str
    """The ID of the Geoscience Object schema."""

class SyncGeostatisticsBreakTiesResult(SyncTaskResult):
    """Result of the break ties task."""

    message: str
    """A message that says what happened in the task."""
    target: SyncGeostatisticsBreakTiesResultTarget
    """The target that was created or updated."""

class GeostatisticsConditionalTurningBandsNeighborhoodEllipsoidEllipsoidRanges(TypedDict):
    """The ranges of the ellipsoid"""

    major: float
    """The major axis length of the ellipsoid"""
    minor: float
    """The minor axis length of the ellipsoid"""
    semi_major: float
    """The semi major axis length of the ellipsoid"""

class GeostatisticsConditionalTurningBandsRotation(TypedDict):
    """The rotation of the ellipsoid"""

    dip: NotRequired[float]
    """second rotation, about the x-axis, in degrees"""
    dip_azimuth: NotRequired[float]
    """first rotation, about the z-axis, in degrees"""
    pitch: NotRequired[float]
    """third rotation, about the z-axis, in degrees"""

class GeostatisticsConditionalTurningBandsEllipsoid(TypedDict):
    """
    The ellipsoid, to search for points within
    Alias: anisotropy
    """

    ellipsoid_ranges: GeostatisticsConditionalTurningBandsNeighborhoodEllipsoidEllipsoidRanges
    """The ranges of the ellipsoid"""
    rotation: GeostatisticsConditionalTurningBandsRotation
    """The rotation of the ellipsoid"""

class GeostatisticsConditionalTurningBandsNeighborhood(TypedDict):
    """Search neighborhood for conditioning."""

    ellipsoid: GeostatisticsConditionalTurningBandsEllipsoid
    """
    The ellipsoid, to search for points within
    Alias: anisotropy
    """
    max_samples: int
    """
    The maximum number of samples to use for each evaluation point.
    Alias: max-samples
    """
    min_samples: NotRequired[int]
    """
    The minimum number of samples to use for each evaluation point.
    Alias: min-samples
    """

class GeostatisticsConditionalTurningBandsBlockDiscretization(TypedDict):
    """Sub-block discretization used for support correction within each grid block."""

    nx: NotRequired[int]
    """Number of subdivisions in the x direction"""
    ny: NotRequired[int]
    """Number of subdivisions in the y direction"""
    nz: NotRequired[int]
    """Number of subdivisions in the z direction"""

class GeostatisticsConditionalTurningBandsFilterCondition(TypedDict):
    """
    A leaf filter condition applied to one attribute of the filtered object.

    Exactly one operator field must be provided.

    - ``in``: Membership test. Provide string category names for
        ``CategoryAttribute`` objects or integers for integer-coded indices.
    - ``not_in``: Exclusion test.
    - ``equal``, ``not_equal``: Numeric equality/inequality.
    - ``greater_than``, ``greater_than_or_equal_to``, ``less_than``,
        ``less_than_or_equal_to``: Numeric comparisons on numeric attributes.
    """

    attribute: AttributeInput
    """Attribute used to filter locations."""
    operator: Literal[
        "in",
        "not_in",
        "equal",
        "not_equal",
        "greater_than",
        "greater_than_or_equal_to",
        "less_than",
        "less_than_or_equal_to",
    ]
    """The filter operator. Use 'in' or 'not_in' for category/membership tests (pair with 'values'). Use 'equal', 'not_equal', 'greater_than', 'greater_than_or_equal_to', 'less_than', or 'less_than_or_equal_to' for numeric comparisons (pair with 'threshold')."""
    threshold: NotRequired[float | None]
    """Numeric threshold for comparison. Required when operator is 'equal', 'not_equal', 'greater_than', 'greater_than_or_equal_to', 'less_than', or 'less_than_or_equal_to'."""
    type: NotRequired[Literal["condition"]]
    """Identifies this as a leaf filter condition."""
    values: NotRequired[list[str] | None]
    """Category values to match. Required when operator is 'in' or 'not_in'. Use strings for string/category-name attributes; use integers for integer-coded attributes."""

class GeostatisticsConditionalTurningBandsAnyOfFilter(TypedDict):
    """A composite filter that passes locations satisfying ANY child filter (OR logic)."""

    filters: list[
        GeostatisticsConditionalTurningBandsFilterCondition
        | GeostatisticsConditionalTurningBandsAllOfFilter
        | GeostatisticsConditionalTurningBandsAnyOfFilter
    ]
    """At least one of these filters must be satisfied (OR logic)."""
    type: NotRequired[Literal["any_of"]]
    """Identifies this as an OR composite filter."""

class GeostatisticsConditionalTurningBandsAllOfFilter(TypedDict):
    """A composite filter that passes locations satisfying ALL child filters (AND logic)."""

    filters: list[
        GeostatisticsConditionalTurningBandsFilterCondition
        | GeostatisticsConditionalTurningBandsAllOfFilter
        | GeostatisticsConditionalTurningBandsAnyOfFilter
    ]
    """All of these filters must be satisfied (AND logic)."""
    type: NotRequired[Literal["all_of"]]
    """Identifies this as an AND composite filter."""

class GeostatisticsConditionalTurningBandsFilter(TypedDict):
    """Optional filter to apply to the target grid. This allows the simulation to only be applied to a subset of grid locations, defined by filtering an attribute on the target grid."""

    where: (
        GeostatisticsConditionalTurningBandsFilterCondition
        | GeostatisticsConditionalTurningBandsAllOfFilter
        | GeostatisticsConditionalTurningBandsAnyOfFilter
    )
    """A filter expression defining which locations to include. Use a FilterCondition for a single predicate, AllOfFilter for AND logic, AnyOfFilter for OR logic, or nest them to build compound predicates."""

class GeostatisticsConditionalTurningBandsSourceAnyOfFilter(TypedDict):
    """A composite filter that passes locations satisfying ANY child filter (OR logic)."""

    filters: list[
        GeostatisticsConditionalTurningBandsFilterCondition
        | GeostatisticsConditionalTurningBandsSourceAllOfFilter
        | GeostatisticsConditionalTurningBandsSourceAnyOfFilter
    ]
    """At least one of these filters must be satisfied (OR logic)."""
    type: NotRequired[Literal["any_of"]]
    """Identifies this as an OR composite filter."""

class GeostatisticsConditionalTurningBandsSourceAllOfFilter(TypedDict):
    """A composite filter that passes locations satisfying ALL child filters (AND logic)."""

    filters: list[
        GeostatisticsConditionalTurningBandsFilterCondition
        | GeostatisticsConditionalTurningBandsSourceAllOfFilter
        | GeostatisticsConditionalTurningBandsSourceAnyOfFilter
    ]
    """All of these filters must be satisfied (AND logic)."""
    type: NotRequired[Literal["all_of"]]
    """Identifies this as an AND composite filter."""

class GeostatisticsConditionalTurningBandsSourceFilter(TypedDict):
    """Optional filter to apply to the source object. This allows the simulation to use only a subset of the source data, defined by filtering an attribute on the source object."""

    where: (
        GeostatisticsConditionalTurningBandsFilterCondition
        | GeostatisticsConditionalTurningBandsSourceAllOfFilter
        | GeostatisticsConditionalTurningBandsSourceAnyOfFilter
    )
    """A filter expression defining which locations to include. Use a FilterCondition for a single predicate, AllOfFilter for AND logic, AnyOfFilter for OR logic, or nest them to build compound predicates."""

class GeostatisticsConditionalTurningBandsResultSimulations(ResultNode):
    """Attribute containing the block simulation results."""

    name: str
    """The name of the output attribute."""
    reference: str
    """Reference to the attribute in the geoscience object."""

class GeostatisticsConditionalTurningBandsResultResultTarget(ResultNode):
    """The target grid with simulation results."""

    description: str | None
    """The description of the geoscience object."""
    name: str
    """The name of the geoscience object."""
    reference: str
    """Reference to a geoscience object."""
    schema_id: str
    """The ID of the Geoscience Object schema."""
    simulations: GeostatisticsConditionalTurningBandsResultSimulations | None
    """Attribute containing the block simulation results."""

class GeostatisticsConditionalTurningBandsResult(TaskResult):
    """Results of the conditional turning bands compute task."""

    target: GeostatisticsConditionalTurningBandsResultResultTarget
    """The target grid with simulation results."""

class SyncGeostatisticsConditionalTurningBandsResultSimulations(SyncResultNode):
    """Attribute containing the block simulation results."""

    name: str
    """The name of the output attribute."""
    reference: str
    """Reference to the attribute in the geoscience object."""

class SyncGeostatisticsConditionalTurningBandsResultResultTarget(SyncResultNode):
    """The target grid with simulation results."""

    description: str | None
    """The description of the geoscience object."""
    name: str
    """The name of the geoscience object."""
    reference: str
    """Reference to a geoscience object."""
    schema_id: str
    """The ID of the Geoscience Object schema."""
    simulations: SyncGeostatisticsConditionalTurningBandsResultSimulations | None
    """Attribute containing the block simulation results."""

class SyncGeostatisticsConditionalTurningBandsResult(SyncTaskResult):
    """Results of the conditional turning bands compute task."""

    target: SyncGeostatisticsConditionalTurningBandsResultResultTarget
    """The target grid with simulation results."""

class GeostatisticsConsimNeighborhoodEllipsoidEllipsoidRanges(TypedDict):
    """The ranges of the ellipsoid"""

    major: float
    """The major axis length of the ellipsoid"""
    minor: float
    """The minor axis length of the ellipsoid"""
    semi_major: float
    """The semi major axis length of the ellipsoid"""

class GeostatisticsConsimRotation(TypedDict):
    """The rotation of the ellipsoid"""

    dip: NotRequired[float]
    """second rotation, about the x-axis, in degrees"""
    dip_azimuth: NotRequired[float]
    """first rotation, about the z-axis, in degrees"""
    pitch: NotRequired[float]
    """third rotation, about the z-axis, in degrees"""

class GeostatisticsConsimEllipsoid(TypedDict):
    """
    The ellipsoid, to search for points within
    Alias: anisotropy
    """

    ellipsoid_ranges: GeostatisticsConsimNeighborhoodEllipsoidEllipsoidRanges
    """The ranges of the ellipsoid"""
    rotation: GeostatisticsConsimRotation
    """The rotation of the ellipsoid"""

class GeostatisticsConsimNeighborhood(TypedDict):
    """Search neighborhood for conditioning."""

    ellipsoid: GeostatisticsConsimEllipsoid
    """
    The ellipsoid, to search for points within
    Alias: anisotropy
    """
    max_samples: int
    """
    The maximum number of samples to use for each evaluation point.
    Alias: max-samples
    """
    min_samples: NotRequired[int]
    """
    The minimum number of samples to use for each evaluation point.
    Alias: min-samples
    """

class GeostatisticsConsimBlockDiscretization(TypedDict):
    """Sub-block discretization used for support correction within each grid block."""

    nx: NotRequired[int]
    """Number of subdivisions in the x direction"""
    ny: NotRequired[int]
    """Number of subdivisions in the y direction"""
    nz: NotRequired[int]
    """Number of subdivisions in the z direction"""

class GeostatisticsConsimDistributionTailExtrapolationLower(TypedDict):
    """Power model for extrapolating the lower tail of the distribution. Optional."""

    min: float
    """Minimum extent of tail, must be less than the minimum value in the data."""
    power: float
    """Denominator of the exponent for the power model (between 0 exclusive and 1 inclusive)."""

class GeostatisticsConsimDistributionTailExtrapolationUpper(TypedDict):
    """Power model for extrapolating the upper tail of the distribution."""

    max: float
    """Maximum extent of tail, must be greater than the maximum value in the data."""
    power: float
    """Denominator of the exponent for the power model (between 0 exclusive and 1 inclusive)."""

class GeostatisticsConsimTailExtrapolation(TypedDict):
    """Tail extrapolation"""

    lower: GeostatisticsConsimDistributionTailExtrapolationLower | None
    """Power model for extrapolating the lower tail of the distribution. Optional."""
    upper: GeostatisticsConsimDistributionTailExtrapolationUpper
    """Power model for extrapolating the upper tail of the distribution."""

class GeostatisticsConsimContinuousDistributionParameters(TypedDict):
    """Parameters for a continuous distribution used for normal-score transformation."""

    tail_extrapolation: GeostatisticsConsimTailExtrapolation | None
    """Tail extrapolation"""
    weights: NotRequired[AttributeInput | None]
    """Optional weights attribute for weighted distribution"""

class GeostatisticsConsimFilterCondition(TypedDict):
    """
    A leaf filter condition applied to one attribute of the filtered object.

    Exactly one operator field must be provided.

    - ``in``: Membership test. Provide string category names for
        ``CategoryAttribute`` objects or integers for integer-coded indices.
    - ``not_in``: Exclusion test.
    - ``equal``, ``not_equal``: Numeric equality/inequality.
    - ``greater_than``, ``greater_than_or_equal_to``, ``less_than``,
        ``less_than_or_equal_to``: Numeric comparisons on numeric attributes.
    """

    attribute: AttributeInput
    """Attribute used to filter locations."""
    operator: Literal[
        "in",
        "not_in",
        "equal",
        "not_equal",
        "greater_than",
        "greater_than_or_equal_to",
        "less_than",
        "less_than_or_equal_to",
    ]
    """The filter operator. Use 'in' or 'not_in' for category/membership tests (pair with 'values'). Use 'equal', 'not_equal', 'greater_than', 'greater_than_or_equal_to', 'less_than', or 'less_than_or_equal_to' for numeric comparisons (pair with 'threshold')."""
    threshold: NotRequired[float | None]
    """Numeric threshold for comparison. Required when operator is 'equal', 'not_equal', 'greater_than', 'greater_than_or_equal_to', 'less_than', or 'less_than_or_equal_to'."""
    type: NotRequired[Literal["condition"]]
    """Identifies this as a leaf filter condition."""
    values: NotRequired[list[str] | None]
    """Category values to match. Required when operator is 'in' or 'not_in'. Use strings for string/category-name attributes; use integers for integer-coded attributes."""

class GeostatisticsConsimAnyOfFilter(TypedDict):
    """A composite filter that passes locations satisfying ANY child filter (OR logic)."""

    filters: list[GeostatisticsConsimFilterCondition | GeostatisticsConsimAllOfFilter | GeostatisticsConsimAnyOfFilter]
    """At least one of these filters must be satisfied (OR logic)."""
    type: NotRequired[Literal["any_of"]]
    """Identifies this as an OR composite filter."""

class GeostatisticsConsimAllOfFilter(TypedDict):
    """A composite filter that passes locations satisfying ALL child filters (AND logic)."""

    filters: list[GeostatisticsConsimFilterCondition | GeostatisticsConsimAllOfFilter | GeostatisticsConsimAnyOfFilter]
    """All of these filters must be satisfied (AND logic)."""
    type: NotRequired[Literal["all_of"]]
    """Identifies this as an AND composite filter."""

class GeostatisticsConsimFilter(TypedDict):
    """Optional filter to apply to the target grid. This allows the simulation to only be applied to a subset of grid locations, defined by filtering an attribute on the target grid."""

    where: GeostatisticsConsimFilterCondition | GeostatisticsConsimAllOfFilter | GeostatisticsConsimAnyOfFilter
    """A filter expression defining which locations to include. Use a FilterCondition for a single predicate, AllOfFilter for AND logic, AnyOfFilter for OR logic, or nest them to build compound predicates."""

class GeostatisticsConsimConSimMaterialCategory(TypedDict):
    """
    Material category for loss calculation.

    If both cutoff_grade and metal_price are zero, this category is treated as waste.
    """

    cutoff_grade: float
    """Cutoff grade for the category"""
    label: str
    """Label for the material category"""
    metal_price: float
    """Price of the metal per unit"""

class GeostatisticsConsimLossCalculationTargetAttributeCreate(TypedDict):
    name: str
    """The name of the attribute to create."""
    operation: Literal["create"]
    """The operation to perform on the attribute."""

class GeostatisticsConsimLossCalculationTargetAttributeUpdate(TypedDict):
    operation: Literal["update"]
    """The operation to perform on the attribute."""
    reference: AttributeInput
    """Reference to an existing attribute to update."""

class GeostatisticsConsimLossCalculation(TypedDict):
    """Settings for loss calculations."""

    material_categories: list[GeostatisticsConsimConSimMaterialCategory]
    """Ordered list of material categories, from lowest to highest cutoff grade"""
    metal_recovery_fraction: float
    """Fraction of metal expected to be recovered during processing"""
    mining_ore_cost: float
    """Mining cost per unit of ore material"""
    mining_waste_cost: float
    """Mining cost per unit of waste material"""
    processing_cost: float
    """Processing cost per unit of material"""
    target_attribute: (
        GeostatisticsConsimLossCalculationTargetAttributeCreate
        | GeostatisticsConsimLossCalculationTargetAttributeUpdate
        | AttributeInput
    )
    """The target attribute that will be created or updated with loss calculation results."""

class GeostatisticsConsimValidationReportContextItem(TypedDict):
    """A single item of contextual information to be included in the validation report."""

    label: str
    """Label of the context item, which will appear on the report."""
    value: str
    """Value of the context item."""

class GeostatisticsConsimReportContext(TypedDict):
    """Context for the validation report."""

    details: list[GeostatisticsConsimValidationReportContextItem]
    """Key-value pairs of details to appear on the report."""
    title: str
    """Title that will appear on the report."""

class GeostatisticsConsimReportMeanThresholds(TypedDict):
    """Thresholds for the mean comparison in the validation report."""

    acceptable: float
    """Threshold for the percentage difference between the reference and simulated means that is considered acceptable."""
    marginal: float
    """Threshold for the percentage difference between the reference and simulated means that is considered marginal."""

class GeostatisticsConsimSourceAnyOfFilter(TypedDict):
    """A composite filter that passes locations satisfying ANY child filter (OR logic)."""

    filters: list[
        GeostatisticsConsimFilterCondition | GeostatisticsConsimSourceAllOfFilter | GeostatisticsConsimSourceAnyOfFilter
    ]
    """At least one of these filters must be satisfied (OR logic)."""
    type: NotRequired[Literal["any_of"]]
    """Identifies this as an OR composite filter."""

class GeostatisticsConsimSourceAllOfFilter(TypedDict):
    """A composite filter that passes locations satisfying ALL child filters (AND logic)."""

    filters: list[
        GeostatisticsConsimFilterCondition | GeostatisticsConsimSourceAllOfFilter | GeostatisticsConsimSourceAnyOfFilter
    ]
    """All of these filters must be satisfied (AND logic)."""
    type: NotRequired[Literal["all_of"]]
    """Identifies this as an AND composite filter."""

class GeostatisticsConsimSourceFilter(TypedDict):
    """Optional filter to apply to the source object. This allows the simulation to use only a subset of the source data, defined by filtering an attribute on the source object."""

    where: (
        GeostatisticsConsimFilterCondition | GeostatisticsConsimSourceAllOfFilter | GeostatisticsConsimSourceAnyOfFilter
    )
    """A filter expression defining which locations to include. Use a FilterCondition for a single predicate, AllOfFilter for AND logic, AnyOfFilter for OR logic, or nest them to build compound predicates."""

class GeostatisticsConsimResultLinks(ResultNode):
    """Links related to the simulation task."""

    dashboard: str | None
    """Link to the validation dashboard."""

class GeostatisticsConsimResultResultTargetLossCalculationAttribute(ResultNode):
    """Attribute containing the loss calculation results."""

    name: str
    """The name of the output attribute."""
    reference: str
    """Reference to the attribute in the geoscience object."""

class GeostatisticsConsimResultResultTargetMeanAboveCutoffAttributesItem(ResultNode):
    """Attribute containing the mean of values above a cutoff."""

    cutoff: float
    """The cutoff value that this attribute represents."""
    name: str
    """The name of the output attribute."""
    reference: str
    """Reference to the attribute in the geoscience object."""

class GeostatisticsConsimResultResultTargetPointSimulations(ResultNode):
    """Attribute containing the point simulation results."""

    name: str
    """The name of the output attribute."""
    reference: str
    """Reference to the attribute in the geoscience object."""

class GeostatisticsConsimResultResultTargetPointSimulationsNormalScore(ResultNode):
    """Attribute containing the point simulation results in normal score space."""

    name: str
    """The name of the output attribute."""
    reference: str
    """Reference to the attribute in the geoscience object."""

class GeostatisticsConsimResultResultTargetProbabilityAboveCutoffAttributesItem(ResultNode):
    """Attribute containing the probability of being above a cutoff value."""

    cutoff: float
    """The cutoff value that this attribute represents."""
    name: str
    """The name of the output attribute."""
    reference: str
    """Reference to the attribute in the geoscience object."""

class GeostatisticsConsimResultQuantile(ResultNode):
    """Attribute containing a quantile of the simulation results."""

    name: str
    """The name of the output attribute."""
    quantile: float
    """Quantile of the simulations, that this attribute represents."""
    reference: str
    """Reference to the attribute in the geoscience object."""

class GeostatisticsConsimResultSimulations(ResultNode):
    """Attribute containing the simulation results."""

    name: str
    """The name of the output attribute."""
    reference: str
    """Reference to the attribute in the geoscience object."""

class GeostatisticsConsimResultResultTargetSimulationsNormalScore(ResultNode):
    """Attribute containing the simulation results in normal score space."""

    name: str
    """The name of the output attribute."""
    reference: str
    """Reference to the attribute in the geoscience object."""

class GeostatisticsConsimResultMaximum(ResultNode):
    """Attribute containing the maximum of the simulations."""

    name: str
    """The name of the output attribute."""
    reference: str
    """Reference to the attribute in the geoscience object."""

class GeostatisticsConsimResultMean(ResultNode):
    """Attribute containing the mean of the simulations."""

    name: str
    """The name of the output attribute."""
    reference: str
    """Reference to the attribute in the geoscience object."""

class GeostatisticsConsimResultMinimum(ResultNode):
    """Attribute containing the minimum of the simulations."""

    name: str
    """The name of the output attribute."""
    reference: str
    """Reference to the attribute in the geoscience object."""

class GeostatisticsConsimResultVariance(ResultNode):
    """Attribute containing the variance of the simulations."""

    name: str
    """The name of the output attribute."""
    reference: str
    """Reference to the attribute in the geoscience object."""

class GeostatisticsConsimResultResultTargetSummaryAttributes(ResultNode):
    """Attribute containing summary statistics of the simulation results."""

    max: GeostatisticsConsimResultMaximum
    """Attribute containing the maximum of the simulations."""
    mean: GeostatisticsConsimResultMean
    """Attribute containing the mean of the simulations."""
    min: GeostatisticsConsimResultMinimum
    """Attribute containing the minimum of the simulations."""
    variance: GeostatisticsConsimResultVariance
    """Attribute containing the variance of the simulations."""

class GeostatisticsConsimResultResultTarget(ResultNode):
    """The target grid with simulation results."""

    description: str | None
    """The description of the geoscience object."""
    loss_calculation_attribute: GeostatisticsConsimResultResultTargetLossCalculationAttribute | None
    """Attribute containing the loss calculation results."""
    mean_above_cutoff_attributes: list[GeostatisticsConsimResultResultTargetMeanAboveCutoffAttributesItem]
    """List of attributes containing mean above cutoff values."""
    name: str
    """The name of the geoscience object."""
    point_simulations: GeostatisticsConsimResultResultTargetPointSimulations | None
    """Attribute containing the point simulation results."""
    point_simulations_normal_score: GeostatisticsConsimResultResultTargetPointSimulationsNormalScore | None
    """Attribute containing the point simulation results in normal score space."""
    probability_above_cutoff_attributes: list[GeostatisticsConsimResultResultTargetProbabilityAboveCutoffAttributesItem]
    """List of attributes containing probability above cutoff values."""
    quantile_attributes: list[GeostatisticsConsimResultQuantile]
    """List of attributes containing quantiles of the simulation results."""
    reference: str
    """Reference to a geoscience object."""
    schema_id: str
    """The ID of the Geoscience Object schema."""
    simulations: GeostatisticsConsimResultSimulations | None
    """Attribute containing the simulation results."""
    simulations_normal_score: GeostatisticsConsimResultResultTargetSimulationsNormalScore | None
    """Attribute containing the simulation results in normal score space."""
    summary_attributes: GeostatisticsConsimResultResultTargetSummaryAttributes
    """Attribute containing summary statistics of the simulation results."""

class GeostatisticsConsimResultResultValidationReport(ResultNode):
    """Link to the validation report file."""

    name: str
    """The name of the file, typically the terminal segment of the path."""
    reference: str
    """Reference to a file."""

class GeostatisticsConsimResultValidationSummary(ResultNode):
    """Summary of the validation results."""

    mean: float
    """Mean of the simulated values."""
    reference_mean: float
    """Mean of the input values."""

class GeostatisticsConsimResult(TaskResult):
    """Results of the conditional simulation compute task."""

    links: GeostatisticsConsimResultLinks
    """Links related to the simulation task."""
    target: GeostatisticsConsimResultResultTarget
    """The target grid with simulation results."""
    validation_report: GeostatisticsConsimResultResultValidationReport | None
    """Link to the validation report file."""
    validation_summary: GeostatisticsConsimResultValidationSummary | None
    """Summary of the validation results."""

class SyncGeostatisticsConsimResultLinks(SyncResultNode):
    """Links related to the simulation task."""

    dashboard: str | None
    """Link to the validation dashboard."""

class SyncGeostatisticsConsimResultResultTargetLossCalculationAttribute(SyncResultNode):
    """Attribute containing the loss calculation results."""

    name: str
    """The name of the output attribute."""
    reference: str
    """Reference to the attribute in the geoscience object."""

class SyncGeostatisticsConsimResultResultTargetMeanAboveCutoffAttributesItem(SyncResultNode):
    """Attribute containing the mean of values above a cutoff."""

    cutoff: float
    """The cutoff value that this attribute represents."""
    name: str
    """The name of the output attribute."""
    reference: str
    """Reference to the attribute in the geoscience object."""

class SyncGeostatisticsConsimResultResultTargetPointSimulations(SyncResultNode):
    """Attribute containing the point simulation results."""

    name: str
    """The name of the output attribute."""
    reference: str
    """Reference to the attribute in the geoscience object."""

class SyncGeostatisticsConsimResultResultTargetPointSimulationsNormalScore(SyncResultNode):
    """Attribute containing the point simulation results in normal score space."""

    name: str
    """The name of the output attribute."""
    reference: str
    """Reference to the attribute in the geoscience object."""

class SyncGeostatisticsConsimResultResultTargetProbabilityAboveCutoffAttributesItem(SyncResultNode):
    """Attribute containing the probability of being above a cutoff value."""

    cutoff: float
    """The cutoff value that this attribute represents."""
    name: str
    """The name of the output attribute."""
    reference: str
    """Reference to the attribute in the geoscience object."""

class SyncGeostatisticsConsimResultQuantile(SyncResultNode):
    """Attribute containing a quantile of the simulation results."""

    name: str
    """The name of the output attribute."""
    quantile: float
    """Quantile of the simulations, that this attribute represents."""
    reference: str
    """Reference to the attribute in the geoscience object."""

class SyncGeostatisticsConsimResultSimulations(SyncResultNode):
    """Attribute containing the simulation results."""

    name: str
    """The name of the output attribute."""
    reference: str
    """Reference to the attribute in the geoscience object."""

class SyncGeostatisticsConsimResultResultTargetSimulationsNormalScore(SyncResultNode):
    """Attribute containing the simulation results in normal score space."""

    name: str
    """The name of the output attribute."""
    reference: str
    """Reference to the attribute in the geoscience object."""

class SyncGeostatisticsConsimResultMaximum(SyncResultNode):
    """Attribute containing the maximum of the simulations."""

    name: str
    """The name of the output attribute."""
    reference: str
    """Reference to the attribute in the geoscience object."""

class SyncGeostatisticsConsimResultMean(SyncResultNode):
    """Attribute containing the mean of the simulations."""

    name: str
    """The name of the output attribute."""
    reference: str
    """Reference to the attribute in the geoscience object."""

class SyncGeostatisticsConsimResultMinimum(SyncResultNode):
    """Attribute containing the minimum of the simulations."""

    name: str
    """The name of the output attribute."""
    reference: str
    """Reference to the attribute in the geoscience object."""

class SyncGeostatisticsConsimResultVariance(SyncResultNode):
    """Attribute containing the variance of the simulations."""

    name: str
    """The name of the output attribute."""
    reference: str
    """Reference to the attribute in the geoscience object."""

class SyncGeostatisticsConsimResultResultTargetSummaryAttributes(SyncResultNode):
    """Attribute containing summary statistics of the simulation results."""

    max: SyncGeostatisticsConsimResultMaximum
    """Attribute containing the maximum of the simulations."""
    mean: SyncGeostatisticsConsimResultMean
    """Attribute containing the mean of the simulations."""
    min: SyncGeostatisticsConsimResultMinimum
    """Attribute containing the minimum of the simulations."""
    variance: SyncGeostatisticsConsimResultVariance
    """Attribute containing the variance of the simulations."""

class SyncGeostatisticsConsimResultResultTarget(SyncResultNode):
    """The target grid with simulation results."""

    description: str | None
    """The description of the geoscience object."""
    loss_calculation_attribute: SyncGeostatisticsConsimResultResultTargetLossCalculationAttribute | None
    """Attribute containing the loss calculation results."""
    mean_above_cutoff_attributes: list[SyncGeostatisticsConsimResultResultTargetMeanAboveCutoffAttributesItem]
    """List of attributes containing mean above cutoff values."""
    name: str
    """The name of the geoscience object."""
    point_simulations: SyncGeostatisticsConsimResultResultTargetPointSimulations | None
    """Attribute containing the point simulation results."""
    point_simulations_normal_score: SyncGeostatisticsConsimResultResultTargetPointSimulationsNormalScore | None
    """Attribute containing the point simulation results in normal score space."""
    probability_above_cutoff_attributes: list[
        SyncGeostatisticsConsimResultResultTargetProbabilityAboveCutoffAttributesItem
    ]
    """List of attributes containing probability above cutoff values."""
    quantile_attributes: list[SyncGeostatisticsConsimResultQuantile]
    """List of attributes containing quantiles of the simulation results."""
    reference: str
    """Reference to a geoscience object."""
    schema_id: str
    """The ID of the Geoscience Object schema."""
    simulations: SyncGeostatisticsConsimResultSimulations | None
    """Attribute containing the simulation results."""
    simulations_normal_score: SyncGeostatisticsConsimResultResultTargetSimulationsNormalScore | None
    """Attribute containing the simulation results in normal score space."""
    summary_attributes: SyncGeostatisticsConsimResultResultTargetSummaryAttributes
    """Attribute containing summary statistics of the simulation results."""

class SyncGeostatisticsConsimResultResultValidationReport(SyncResultNode):
    """Link to the validation report file."""

    name: str
    """The name of the file, typically the terminal segment of the path."""
    reference: str
    """Reference to a file."""

class SyncGeostatisticsConsimResultValidationSummary(SyncResultNode):
    """Summary of the validation results."""

    mean: float
    """Mean of the simulated values."""
    reference_mean: float
    """Mean of the input values."""

class SyncGeostatisticsConsimResult(SyncTaskResult):
    """Results of the conditional simulation compute task."""

    links: SyncGeostatisticsConsimResultLinks
    """Links related to the simulation task."""
    target: SyncGeostatisticsConsimResultResultTarget
    """The target grid with simulation results."""
    validation_report: SyncGeostatisticsConsimResultResultValidationReport | None
    """Link to the validation report file."""
    validation_summary: SyncGeostatisticsConsimResultValidationSummary | None
    """Summary of the validation results."""

class GeostatisticsContinuousDistributionSource(TypedDict):
    """Source data to create the distribution from."""

    attribute: AttributeInput
    """The attribute containing the values to create a distribution from."""
    object: ObjectInput
    """Object containing the data to create a distribution from."""

class GeostatisticsContinuousDistributionTarget(TypedDict):
    """Target specifying where to create the continuous distribution object."""

    description: NotRequired[str | None]
    """Description to put into the target object."""
    overwrite: NotRequired[bool]
    """Whether to overwrite the existing object."""
    reference: ObjectInput
    """Reference to the targeted object."""
    tags: NotRequired[dict[str, str]]
    """Tags to put into the target object."""

class GeostatisticsContinuousDistributionTailExtrapolationLower(TypedDict):
    """Power model for lower tail extrapolation. The min value must be less than the minimum value in the data."""

    min: float
    """Minimum extent of tail, must be less than the minimum value in the data."""
    power: float
    """Denominator of the exponent for the power model (between 0 exclusive and 1 inclusive)."""

class GeostatisticsContinuousDistributionTailExtrapolationUpper(TypedDict):
    """Power model for extrapolating the upper tail of the distribution."""

    max: float
    """Maximum extent of tail, must be greater than the maximum value in the data."""
    power: float
    """Denominator of the exponent for the power model (between 0 exclusive and 1 inclusive)."""

class GeostatisticsContinuousDistributionTailExtrapolation(TypedDict):
    """Optional tail extrapolation parameters for extending the distribution beyond the data range."""

    lower: NotRequired[GeostatisticsContinuousDistributionTailExtrapolationLower | None]
    """Power model for lower tail extrapolation. The min value must be less than the minimum value in the data."""
    upper: GeostatisticsContinuousDistributionTailExtrapolationUpper
    """Power model for extrapolating the upper tail of the distribution."""

class GeostatisticsContinuousDistributionWeights(TypedDict):
    """Optional weights for creating a weighted (declustered) distribution."""

    attribute: AttributeInput
    """The attribute containing the weights values."""
    object: ObjectInput
    """Object containing the weights data."""

class GeostatisticsContinuousDistributionResultDistribution(ResultNode):
    """Reference to the created continuous distribution object."""

    description: str | None
    """The description of the geoscience object."""
    name: str
    """The name of the geoscience object."""
    reference: str
    """Reference to a geoscience object."""
    schema_id: str
    """The ID of the Geoscience Object schema."""

class GeostatisticsContinuousDistributionResult(TaskResult):
    """Result of the continuous distribution task."""

    distribution: GeostatisticsContinuousDistributionResultDistribution
    """Reference to the created continuous distribution object."""
    message: str
    """A message describing the result of the task."""

class SyncGeostatisticsContinuousDistributionResultDistribution(SyncResultNode):
    """Reference to the created continuous distribution object."""

    description: str | None
    """The description of the geoscience object."""
    name: str
    """The name of the geoscience object."""
    reference: str
    """Reference to a geoscience object."""
    schema_id: str
    """The ID of the Geoscience Object schema."""

class SyncGeostatisticsContinuousDistributionResult(SyncTaskResult):
    """Result of the continuous distribution task."""

    distribution: SyncGeostatisticsContinuousDistributionResultDistribution
    """Reference to the created continuous distribution object."""
    message: str
    """A message describing the result of the task."""

class GeostatisticsDeclusteringSource(TypedDict):
    """The source sample locations."""

    object: ObjectInput
    """Spatial locations of samples to compute declustering weights for."""

class GeostatisticsDeclusteringGrid(TypedDict):
    """The evaluation grid for measuring sample influence."""

    object: ObjectInput
    """Regular grid or set of evaluation locations used to measure each sample's spatial influence."""

class GeostatisticsDeclusteringTargetAttributeCreate(TypedDict):
    name: str
    """The name of the attribute to create."""
    operation: Literal["create"]
    """The operation to perform on the attribute."""

class GeostatisticsDeclusteringTargetAttributeUpdate(TypedDict):
    operation: Literal["update"]
    """The operation to perform on the attribute."""
    reference: AttributeInput
    """Reference to an existing attribute to update."""

class GeostatisticsDeclusteringTarget(TypedDict):
    """The target object and attribute to write declustering weights to."""

    attribute: (
        GeostatisticsDeclusteringTargetAttributeCreate | GeostatisticsDeclusteringTargetAttributeUpdate | AttributeInput
    )
    """Name of the attribute to create or update with the computed declustering weights."""
    object: ObjectInput
    """Object to write the declustering weight attribute onto. Typically the same object as the source."""

class GeostatisticsDeclusteringNeighborhoodEllipsoidEllipsoidRanges(TypedDict):
    """The ranges of the ellipsoid"""

    major: float
    """The major axis length of the ellipsoid"""
    minor: float
    """The minor axis length of the ellipsoid"""
    semi_major: float
    """The semi major axis length of the ellipsoid"""

class GeostatisticsDeclusteringRotation(TypedDict):
    """The rotation of the ellipsoid"""

    dip: NotRequired[float]
    """second rotation, about the x-axis, in degrees"""
    dip_azimuth: NotRequired[float]
    """first rotation, about the z-axis, in degrees"""
    pitch: NotRequired[float]
    """third rotation, about the z-axis, in degrees"""

class GeostatisticsDeclusteringEllipsoid(TypedDict):
    """
    The ellipsoid, to search for points within
    Alias: anisotropy
    """

    ellipsoid_ranges: GeostatisticsDeclusteringNeighborhoodEllipsoidEllipsoidRanges
    """The ranges of the ellipsoid"""
    rotation: GeostatisticsDeclusteringRotation
    """The rotation of the ellipsoid"""

class GeostatisticsDeclusteringExtendedNeighborhood(TypedDict):
    """Search neighborhood parameters. The search ellipsoid and sample limits control which samples influence each grid location during the estimation used to compute declustering weights."""

    ellipsoid: GeostatisticsDeclusteringEllipsoid
    """
    The ellipsoid, to search for points within
    Alias: anisotropy
    """
    max_drillholes_per_estimate: NotRequired[int | None]
    """The maximum number of drillholes to be used in each estimate."""
    max_empty_octants: NotRequired[int]
    """The maximum number of empty octants (aka sectors) allowed when searching for neighbors. Omitting this, or a value of 8, disables the octant check."""
    max_empty_quadrants: NotRequired[int | None]
    """The maximum number of empty quadrants (2D sectors, ignoring Z) allowed when searching for neighbors. If not specified, quadrant filtering is disabled."""
    max_samples: int
    """
    The maximum number of samples to use for each evaluation point.
    Alias: max-samples
    """
    max_samples_per_drillhole: NotRequired[int | None]
    """The maximum number of samples to use from each drillhole (defined by drillhole ID)"""
    max_samples_per_octant: NotRequired[int | None]
    """The maximum number of samples to use from each octant."""
    max_samples_per_quadrant: NotRequired[int | None]
    """The maximum number of samples to use from each quadrant (2D sectors, ignoring Z)."""
    min_samples: NotRequired[int]
    """
    The minimum number of samples to use for each evaluation point.
    Alias: min-samples
    """

class GeostatisticsDeclusteringResultAttribute(ResultNode):
    """Attribute containing the declustering weights."""

    name: str
    """The name of the output attribute."""
    reference: str
    """Reference to the attribute in the geoscience object."""

class GeostatisticsDeclusteringResultTarget(ResultNode):
    """The target that was created or updated with declustering weights."""

    attribute: GeostatisticsDeclusteringResultAttribute
    """Attribute containing the declustering weights."""
    description: str | None
    """The description of the geoscience object."""
    name: str
    """The name of the geoscience object."""
    reference: str
    """Reference to a geoscience object."""
    schema_id: str
    """The ID of the Geoscience Object schema."""

class GeostatisticsDeclusteringResult(TaskResult):
    """Result of the declustering task."""

    message: str
    """A message that says what happened in the task."""
    target: GeostatisticsDeclusteringResultTarget
    """The target that was created or updated with declustering weights."""

class SyncGeostatisticsDeclusteringResultAttribute(SyncResultNode):
    """Attribute containing the declustering weights."""

    name: str
    """The name of the output attribute."""
    reference: str
    """Reference to the attribute in the geoscience object."""

class SyncGeostatisticsDeclusteringResultTarget(SyncResultNode):
    """The target that was created or updated with declustering weights."""

    attribute: SyncGeostatisticsDeclusteringResultAttribute
    """Attribute containing the declustering weights."""
    description: str | None
    """The description of the geoscience object."""
    name: str
    """The name of the geoscience object."""
    reference: str
    """Reference to a geoscience object."""
    schema_id: str
    """The ID of the Geoscience Object schema."""

class SyncGeostatisticsDeclusteringResult(SyncTaskResult):
    """Result of the declustering task."""

    message: str
    """A message that says what happened in the task."""
    target: SyncGeostatisticsDeclusteringResultTarget
    """The target that was created or updated with declustering weights."""

class GeostatisticsExperimentalVariogramSourceFilterCondition(TypedDict):
    """
    A leaf filter condition applied to one attribute of the filtered object.

    Exactly one operator field must be provided.

    - ``in``: Membership test. Provide string category names for
        ``CategoryAttribute`` objects or integers for integer-coded indices.
    - ``not_in``: Exclusion test.
    - ``equal``, ``not_equal``: Numeric equality/inequality.
    - ``greater_than``, ``greater_than_or_equal_to``, ``less_than``,
        ``less_than_or_equal_to``: Numeric comparisons on numeric attributes.
    """

    attribute: AttributeInput
    """Attribute used to filter locations."""
    operator: Literal[
        "in",
        "not_in",
        "equal",
        "not_equal",
        "greater_than",
        "greater_than_or_equal_to",
        "less_than",
        "less_than_or_equal_to",
    ]
    """The filter operator. Use 'in' or 'not_in' for category/membership tests (pair with 'values'). Use 'equal', 'not_equal', 'greater_than', 'greater_than_or_equal_to', 'less_than', or 'less_than_or_equal_to' for numeric comparisons (pair with 'threshold')."""
    threshold: NotRequired[float | None]
    """Numeric threshold for comparison. Required when operator is 'equal', 'not_equal', 'greater_than', 'greater_than_or_equal_to', 'less_than', or 'less_than_or_equal_to'."""
    type: NotRequired[Literal["condition"]]
    """Identifies this as a leaf filter condition."""
    values: NotRequired[list[str] | None]
    """Category values to match. Required when operator is 'in' or 'not_in'. Use strings for string/category-name attributes; use integers for integer-coded attributes."""

class GeostatisticsExperimentalVariogramSourceAnyOfFilter(TypedDict):
    """A composite filter that passes locations satisfying ANY child filter (OR logic)."""

    filters: list[
        GeostatisticsExperimentalVariogramSourceFilterCondition
        | GeostatisticsExperimentalVariogramSourceAllOfFilter
        | GeostatisticsExperimentalVariogramSourceAnyOfFilter
    ]
    """At least one of these filters must be satisfied (OR logic)."""
    type: NotRequired[Literal["any_of"]]
    """Identifies this as an OR composite filter."""

class GeostatisticsExperimentalVariogramSourceAllOfFilter(TypedDict):
    """A composite filter that passes locations satisfying ALL child filters (AND logic)."""

    filters: list[
        GeostatisticsExperimentalVariogramSourceFilterCondition
        | GeostatisticsExperimentalVariogramSourceAllOfFilter
        | GeostatisticsExperimentalVariogramSourceAnyOfFilter
    ]
    """All of these filters must be satisfied (AND logic)."""
    type: NotRequired[Literal["all_of"]]
    """Identifies this as an AND composite filter."""

class GeostatisticsExperimentalVariogramSourceFilter(TypedDict):
    """Optional filter to apply to the source object. This restricts the variogram computation to a subset of the samples, defined by filtering an attribute on the source object (for example a domain or region code)."""

    where: (
        GeostatisticsExperimentalVariogramSourceFilterCondition
        | GeostatisticsExperimentalVariogramSourceAllOfFilter
        | GeostatisticsExperimentalVariogramSourceAnyOfFilter
    )
    """A filter expression defining which locations to include. Use a FilterCondition for a single predicate, AllOfFilter for AND logic, AnyOfFilter for OR logic, or nest them to build compound predicates."""

class GeostatisticsExperimentalVariogramSource(TypedDict):
    """The source object and attribute providing the sample locations and values."""

    attribute: AttributeInput
    """The numeric attribute on the source object to compute the variogram on."""
    domain: NotRequired[str | None]
    """Optional label for the domain these samples represent, recorded on the created experimental variogram object. Typically describes the subset selected by 'filter', for example the name of an estimation domain."""
    drillhole_id: NotRequired[AttributeInput | None]
    """Optional category attribute identifying the drillhole each sample belongs to. Required for downhole variograms when the source is a points object. When the source is a downhole intervals object and this is omitted, the object's intrinsic hole identifier is used instead."""
    filter: NotRequired[GeostatisticsExperimentalVariogramSourceFilter | None]
    """Optional filter to apply to the source object. This restricts the variogram computation to a subset of the samples, defined by filtering an attribute on the source object (for example a domain or region code)."""
    object: ObjectInput
    """Geoscience object containing the sample locations and values. Must be a points or downhole intervals object."""

class GeostatisticsExperimentalVariogramLags(TypedDict):
    """Lag bin definition. Omit to derive density-based lags automatically from the samples."""

    ends: NotRequired[list[float] | None]
    """Upper bounds (exclusive) of each lag bin. Provide with 'starts' for the explicit form."""
    lag_distance: NotRequired[float | None]
    """Width of each uniform lag bin. Provide with 'n_lags' for the uniform form."""
    max_distance: NotRequired[float | None]
    """Largest separation distance the automatically derived bins may reach. Only valid for the automatic form; omit it to derive the range from the extent of the samples. A common choice is half the extent of the domain being modelled."""
    n_lags: NotRequired[int | None]
    """Number of uniform lag bins. Provide with 'lag_distance' for the uniform form."""
    starts: NotRequired[list[float] | None]
    """Lower bounds (inclusive) of each lag bin. Provide with 'ends' for the explicit form."""

class GeostatisticsExperimentalVariogramOmnidirectionalVariogram(TypedDict):
    """An omnidirectional variogram — all sample pairs binned by separation distance."""

    lags: NotRequired[GeostatisticsExperimentalVariogramLags | None]
    """Lag bin definition. Omit to derive density-based lags automatically from the samples."""
    type: Literal["omnidirectional"]
    """Variogram type discriminator."""

class GeostatisticsExperimentalVariogramDirectionalVariogram(TypedDict):
    """A directional variogram — pairs filtered by an azimuth/dip tolerance cone."""

    azimuth: float
    """Azimuth of the search direction in degrees, measured clockwise from North. Range [0, 360)."""
    azimuth_tolerance: NotRequired[float | None]
    """Half-width of the azimuth acceptance cone in degrees. Omit to fall back to a 15 degree default (density-based derivation is not yet available for the angular tolerances)."""
    bandheight: NotRequired[float | None]
    """Maximum perpendicular vertical distance from the search axis. Omit to derive it from the sample density."""
    bandwidth: NotRequired[float | None]
    """Maximum perpendicular horizontal distance from the search axis. Omit to derive it from the sample density."""
    dip: float
    """Dip of the search direction in degrees, measured downward from horizontal. Range [-90, 90]."""
    dip_tolerance: NotRequired[float | None]
    """Half-width of the dip acceptance cone in degrees. Omit to fall back to a 15 degree default (density-based derivation is not yet available for the angular tolerances)."""
    lags: NotRequired[GeostatisticsExperimentalVariogramLags | None]
    """Lag bin definition. Omit to derive density-based lags automatically from the samples."""
    rectangular_cone: NotRequired[bool]
    """When True, the acceptance region is a rectangular (trapezoidal) search where the azimuth and dip tolerances are applied independently, matching Leapfrog's default search. When False, an elliptical cone is used."""
    type: Literal["directional"]
    """Variogram type discriminator."""

class GeostatisticsExperimentalVariogramDownholeVariogram(TypedDict):
    """A downhole variogram — pairs within the same drillhole, binned by along-hole distance."""

    lags: NotRequired[GeostatisticsExperimentalVariogramLags | None]
    """Lag bin definition. Omit to derive density-based lags automatically from the samples."""
    type: Literal["downhole"]
    """Variogram type discriminator."""

class GeostatisticsExperimentalVariogramTarget(TypedDict):
    """Target specifying where to create the experimental variogram object."""

    description: NotRequired[str | None]
    """Description to put into the target object."""
    overwrite: NotRequired[bool]
    """Whether to overwrite the existing object."""
    reference: ObjectInput
    """Reference to the targeted object."""
    tags: NotRequired[dict[str, str]]
    """Tags to put into the target object."""

class GeostatisticsExperimentalVariogramResultResultExperimentalVariogram(ResultNode):
    """Reference to the created experimental variogram object."""

    description: str | None
    """The description of the geoscience object."""
    name: str
    """The name of the geoscience object."""
    reference: str
    """Reference to a geoscience object."""
    schema_id: str
    """The ID of the Geoscience Object schema."""

class GeostatisticsExperimentalVariogramResult(TaskResult):
    """Result of the experimental variogram task."""

    experimental_variogram: GeostatisticsExperimentalVariogramResultResultExperimentalVariogram
    """Reference to the created experimental variogram object."""
    message: str
    """A message describing the result of the task."""

class SyncGeostatisticsExperimentalVariogramResultResultExperimentalVariogram(SyncResultNode):
    """Reference to the created experimental variogram object."""

    description: str | None
    """The description of the geoscience object."""
    name: str
    """The name of the geoscience object."""
    reference: str
    """Reference to a geoscience object."""
    schema_id: str
    """The ID of the Geoscience Object schema."""

class SyncGeostatisticsExperimentalVariogramResult(SyncTaskResult):
    """Result of the experimental variogram task."""

    experimental_variogram: SyncGeostatisticsExperimentalVariogramResultResultExperimentalVariogram
    """Reference to the created experimental variogram object."""
    message: str
    """A message describing the result of the task."""

class GeostatisticsIdwSourceFilterCondition(TypedDict):
    """
    A leaf filter condition applied to one attribute of the filtered object.

    Exactly one operator field must be provided.

    - ``in``: Membership test. Provide string category names for
        ``CategoryAttribute`` objects or integers for integer-coded indices.
    - ``not_in``: Exclusion test.
    - ``equal``, ``not_equal``: Numeric equality/inequality.
    - ``greater_than``, ``greater_than_or_equal_to``, ``less_than``,
        ``less_than_or_equal_to``: Numeric comparisons on numeric attributes.
    """

    attribute: AttributeInput
    """Attribute used to filter locations."""
    operator: Literal[
        "in",
        "not_in",
        "equal",
        "not_equal",
        "greater_than",
        "greater_than_or_equal_to",
        "less_than",
        "less_than_or_equal_to",
    ]
    """The filter operator. Use 'in' or 'not_in' for category/membership tests (pair with 'values'). Use 'equal', 'not_equal', 'greater_than', 'greater_than_or_equal_to', 'less_than', or 'less_than_or_equal_to' for numeric comparisons (pair with 'threshold')."""
    threshold: NotRequired[float | None]
    """Numeric threshold for comparison. Required when operator is 'equal', 'not_equal', 'greater_than', 'greater_than_or_equal_to', 'less_than', or 'less_than_or_equal_to'."""
    type: NotRequired[Literal["condition"]]
    """Identifies this as a leaf filter condition."""
    values: NotRequired[list[str] | None]
    """Category values to match. Required when operator is 'in' or 'not_in'. Use strings for string/category-name attributes; use integers for integer-coded attributes."""

class GeostatisticsIdwSourceAnyOfFilter(TypedDict):
    """A composite filter that passes locations satisfying ANY child filter (OR logic)."""

    filters: list[
        GeostatisticsIdwSourceFilterCondition | GeostatisticsIdwSourceAllOfFilter | GeostatisticsIdwSourceAnyOfFilter
    ]
    """At least one of these filters must be satisfied (OR logic)."""
    type: NotRequired[Literal["any_of"]]
    """Identifies this as an OR composite filter."""

class GeostatisticsIdwSourceAllOfFilter(TypedDict):
    """A composite filter that passes locations satisfying ALL child filters (AND logic)."""

    filters: list[
        GeostatisticsIdwSourceFilterCondition | GeostatisticsIdwSourceAllOfFilter | GeostatisticsIdwSourceAnyOfFilter
    ]
    """All of these filters must be satisfied (AND logic)."""
    type: NotRequired[Literal["all_of"]]
    """Identifies this as an AND composite filter."""

class GeostatisticsIdwSourceFilter(TypedDict):
    """Optional filter to apply to the source object. This allows IDW to use only a subset of the known data, defined by filtering an attribute on the source object."""

    where: GeostatisticsIdwSourceFilterCondition | GeostatisticsIdwSourceAllOfFilter | GeostatisticsIdwSourceAnyOfFilter
    """A filter expression defining which locations to include. Use a FilterCondition for a single predicate, AllOfFilter for AND logic, AnyOfFilter for OR logic, or nest them to build compound predicates."""

class GeostatisticsIdwSource(TypedDict):
    """The source object and attribute containing known values."""

    attribute: AttributeInput
    """The known values, used to estimate values at unknown locations."""
    filter: NotRequired[GeostatisticsIdwSourceFilter | None]
    """Optional filter to apply to the source object. This allows IDW to use only a subset of the known data, defined by filtering an attribute on the source object."""
    object: ObjectInput
    """Spatial locations of known values."""

class GeostatisticsIdwTargetAttributeCreate(TypedDict):
    name: str
    """The name of the attribute to create."""
    operation: Literal["create"]
    """The operation to perform on the attribute."""

class GeostatisticsIdwTargetAttributeUpdate(TypedDict):
    operation: Literal["update"]
    """The operation to perform on the attribute."""
    reference: AttributeInput
    """Reference to an existing attribute to update."""

class GeostatisticsIdwAnyOfFilter(TypedDict):
    """A composite filter that passes locations satisfying ANY child filter (OR logic)."""

    filters: list[GeostatisticsIdwSourceFilterCondition | GeostatisticsIdwAllOfFilter | GeostatisticsIdwAnyOfFilter]
    """At least one of these filters must be satisfied (OR logic)."""
    type: NotRequired[Literal["any_of"]]
    """Identifies this as an OR composite filter."""

class GeostatisticsIdwAllOfFilter(TypedDict):
    """A composite filter that passes locations satisfying ALL child filters (AND logic)."""

    filters: list[GeostatisticsIdwSourceFilterCondition | GeostatisticsIdwAllOfFilter | GeostatisticsIdwAnyOfFilter]
    """All of these filters must be satisfied (AND logic)."""
    type: NotRequired[Literal["all_of"]]
    """Identifies this as an AND composite filter."""

class GeostatisticsIdwFilter(TypedDict):
    """Optional filter to apply to the target object. This allows the estimation to only be applied to a subset of the target object, defined by filtering an attribute on the target object."""

    where: GeostatisticsIdwSourceFilterCondition | GeostatisticsIdwAllOfFilter | GeostatisticsIdwAnyOfFilter
    """A filter expression defining which locations to include. Use a FilterCondition for a single predicate, AllOfFilter for AND logic, AnyOfFilter for OR logic, or nest them to build compound predicates."""

class GeostatisticsIdwTarget(TypedDict):
    """The target object and attribute to create or update with IDW results."""

    attribute: GeostatisticsIdwTargetAttributeCreate | GeostatisticsIdwTargetAttributeUpdate | AttributeInput
    """Name of the attribute to create or update on the target object"""
    filter: NotRequired[GeostatisticsIdwFilter | None]
    """Optional filter to apply to the target object. This allows the estimation to only be applied to a subset of the target object, defined by filtering an attribute on the target object."""
    object: ObjectInput
    """Object to evaluate onto."""

class GeostatisticsIdwNeighborhoodEllipsoidEllipsoidRanges(TypedDict):
    """The ranges of the ellipsoid"""

    major: float
    """The major axis length of the ellipsoid"""
    minor: float
    """The minor axis length of the ellipsoid"""
    semi_major: float
    """The semi major axis length of the ellipsoid"""

class GeostatisticsIdwRotation(TypedDict):
    """The rotation of the ellipsoid"""

    dip: NotRequired[float]
    """second rotation, about the x-axis, in degrees"""
    dip_azimuth: NotRequired[float]
    """first rotation, about the z-axis, in degrees"""
    pitch: NotRequired[float]
    """third rotation, about the z-axis, in degrees"""

class GeostatisticsIdwEllipsoid(TypedDict):
    """
    The ellipsoid, to search for points within
    Alias: anisotropy
    """

    ellipsoid_ranges: GeostatisticsIdwNeighborhoodEllipsoidEllipsoidRanges
    """The ranges of the ellipsoid"""
    rotation: GeostatisticsIdwRotation
    """The rotation of the ellipsoid"""

class GeostatisticsIdwExtendedNeighborhood(TypedDict):
    """In IDW estimation, the value of each evaluation point is determined by a weighted average of nearby points with known values, where weights are inversely proportional to distance raised to the specified power. The search parameters determine which nearby points to use."""

    ellipsoid: GeostatisticsIdwEllipsoid
    """
    The ellipsoid, to search for points within
    Alias: anisotropy
    """
    max_drillholes_per_estimate: NotRequired[int | None]
    """The maximum number of drillholes to be used in each estimate."""
    max_empty_octants: NotRequired[int]
    """The maximum number of empty octants (aka sectors) allowed when searching for neighbors. Omitting this, or a value of 8, disables the octant check."""
    max_empty_quadrants: NotRequired[int | None]
    """The maximum number of empty quadrants (2D sectors, ignoring Z) allowed when searching for neighbors. If not specified, quadrant filtering is disabled."""
    max_samples: int
    """
    The maximum number of samples to use for each evaluation point.
    Alias: max-samples
    """
    max_samples_per_drillhole: NotRequired[int | None]
    """The maximum number of samples to use from each drillhole (defined by drillhole ID)"""
    max_samples_per_octant: NotRequired[int | None]
    """The maximum number of samples to use from each octant."""
    max_samples_per_quadrant: NotRequired[int | None]
    """The maximum number of samples to use from each quadrant (2D sectors, ignoring Z)."""
    min_samples: NotRequired[int]
    """
    The minimum number of samples to use for each evaluation point.
    Alias: min-samples
    """

class GeostatisticsIdwResultAttribute(ResultNode):
    """Attribute containing the IDW result."""

    name: str
    """The name of the output attribute."""
    reference: str
    """Reference to the attribute in the geoscience object."""

class GeostatisticsIdwResultTarget(ResultNode):
    """The target that was created or updated."""

    attribute: GeostatisticsIdwResultAttribute
    """Attribute containing the IDW result."""
    description: str | None
    """The description of the geoscience object."""
    name: str
    """The name of the geoscience object."""
    reference: str
    """Reference to a geoscience object."""
    schema_id: str
    """The ID of the Geoscience Object schema."""

class GeostatisticsIdwResult(TaskResult):
    """Result of the IDW task."""

    message: str
    """A message that says what happened in the task."""
    target: GeostatisticsIdwResultTarget
    """The target that was created or updated."""

class SyncGeostatisticsIdwResultAttribute(SyncResultNode):
    """Attribute containing the IDW result."""

    name: str
    """The name of the output attribute."""
    reference: str
    """Reference to the attribute in the geoscience object."""

class SyncGeostatisticsIdwResultTarget(SyncResultNode):
    """The target that was created or updated."""

    attribute: SyncGeostatisticsIdwResultAttribute
    """Attribute containing the IDW result."""
    description: str | None
    """The description of the geoscience object."""
    name: str
    """The name of the geoscience object."""
    reference: str
    """Reference to a geoscience object."""
    schema_id: str
    """The ID of the Geoscience Object schema."""

class SyncGeostatisticsIdwResult(SyncTaskResult):
    """Result of the IDW task."""

    message: str
    """A message that says what happened in the task."""
    target: SyncGeostatisticsIdwResultTarget
    """The target that was created or updated."""

class GeostatisticsKnnSourceFilterCondition(TypedDict):
    """
    A leaf filter condition applied to one attribute of the filtered object.

    Exactly one operator field must be provided.

    - ``in``: Membership test. Provide string category names for
        ``CategoryAttribute`` objects or integers for integer-coded indices.
    - ``not_in``: Exclusion test.
    - ``equal``, ``not_equal``: Numeric equality/inequality.
    - ``greater_than``, ``greater_than_or_equal_to``, ``less_than``,
        ``less_than_or_equal_to``: Numeric comparisons on numeric attributes.
    """

    attribute: AttributeInput
    """Attribute used to filter locations."""
    operator: Literal[
        "in",
        "not_in",
        "equal",
        "not_equal",
        "greater_than",
        "greater_than_or_equal_to",
        "less_than",
        "less_than_or_equal_to",
    ]
    """The filter operator. Use 'in' or 'not_in' for category/membership tests (pair with 'values'). Use 'equal', 'not_equal', 'greater_than', 'greater_than_or_equal_to', 'less_than', or 'less_than_or_equal_to' for numeric comparisons (pair with 'threshold')."""
    threshold: NotRequired[float | None]
    """Numeric threshold for comparison. Required when operator is 'equal', 'not_equal', 'greater_than', 'greater_than_or_equal_to', 'less_than', or 'less_than_or_equal_to'."""
    type: NotRequired[Literal["condition"]]
    """Identifies this as a leaf filter condition."""
    values: NotRequired[list[str] | None]
    """Category values to match. Required when operator is 'in' or 'not_in'. Use strings for string/category-name attributes; use integers for integer-coded attributes."""

class GeostatisticsKnnSourceAnyOfFilter(TypedDict):
    """A composite filter that passes locations satisfying ANY child filter (OR logic)."""

    filters: list[
        GeostatisticsKnnSourceFilterCondition | GeostatisticsKnnSourceAllOfFilter | GeostatisticsKnnSourceAnyOfFilter
    ]
    """At least one of these filters must be satisfied (OR logic)."""
    type: NotRequired[Literal["any_of"]]
    """Identifies this as an OR composite filter."""

class GeostatisticsKnnSourceAllOfFilter(TypedDict):
    """A composite filter that passes locations satisfying ALL child filters (AND logic)."""

    filters: list[
        GeostatisticsKnnSourceFilterCondition | GeostatisticsKnnSourceAllOfFilter | GeostatisticsKnnSourceAnyOfFilter
    ]
    """All of these filters must be satisfied (AND logic)."""
    type: NotRequired[Literal["all_of"]]
    """Identifies this as an AND composite filter."""

class GeostatisticsKnnSourceFilter(TypedDict):
    """Optional filter to apply to the source object. This allows KNN to use only a subset of the known data, defined by filtering an attribute on the source object."""

    where: GeostatisticsKnnSourceFilterCondition | GeostatisticsKnnSourceAllOfFilter | GeostatisticsKnnSourceAnyOfFilter
    """A filter expression defining which locations to include. Use a FilterCondition for a single predicate, AllOfFilter for AND logic, AnyOfFilter for OR logic, or nest them to build compound predicates."""

class GeostatisticsKnnSource(TypedDict):
    """The source object and attribute containing known values."""

    attribute: AttributeInput
    """The known values, used to estimate values at unknown locations."""
    filter: NotRequired[GeostatisticsKnnSourceFilter | None]
    """Optional filter to apply to the source object. This allows KNN to use only a subset of the known data, defined by filtering an attribute on the source object."""
    object: ObjectInput
    """Spatial locations of known values."""

class GeostatisticsKnnTargetAttributeCreate(TypedDict):
    name: str
    """The name of the attribute to create."""
    operation: Literal["create"]
    """The operation to perform on the attribute."""

class GeostatisticsKnnTargetAttributeUpdate(TypedDict):
    operation: Literal["update"]
    """The operation to perform on the attribute."""
    reference: AttributeInput
    """Reference to an existing attribute to update."""

class GeostatisticsKnnAnyOfFilter(TypedDict):
    """A composite filter that passes locations satisfying ANY child filter (OR logic)."""

    filters: list[GeostatisticsKnnSourceFilterCondition | GeostatisticsKnnAllOfFilter | GeostatisticsKnnAnyOfFilter]
    """At least one of these filters must be satisfied (OR logic)."""
    type: NotRequired[Literal["any_of"]]
    """Identifies this as an OR composite filter."""

class GeostatisticsKnnAllOfFilter(TypedDict):
    """A composite filter that passes locations satisfying ALL child filters (AND logic)."""

    filters: list[GeostatisticsKnnSourceFilterCondition | GeostatisticsKnnAllOfFilter | GeostatisticsKnnAnyOfFilter]
    """All of these filters must be satisfied (AND logic)."""
    type: NotRequired[Literal["all_of"]]
    """Identifies this as an AND composite filter."""

class GeostatisticsKnnFilter(TypedDict):
    """Optional filter to apply to the target object. This allows the estimation to only be applied to a subset of the target object, defined by filtering an attribute on the target object."""

    where: GeostatisticsKnnSourceFilterCondition | GeostatisticsKnnAllOfFilter | GeostatisticsKnnAnyOfFilter
    """A filter expression defining which locations to include. Use a FilterCondition for a single predicate, AllOfFilter for AND logic, AnyOfFilter for OR logic, or nest them to build compound predicates."""

class GeostatisticsKnnTarget(TypedDict):
    """The target object and attribute to create or update with KNN results."""

    attribute: GeostatisticsKnnTargetAttributeCreate | GeostatisticsKnnTargetAttributeUpdate | AttributeInput
    """Name of the attribute to create or update on the target object"""
    filter: NotRequired[GeostatisticsKnnFilter | None]
    """Optional filter to apply to the target object. This allows the estimation to only be applied to a subset of the target object, defined by filtering an attribute on the target object."""
    object: ObjectInput
    """Object to evaluate onto."""

class GeostatisticsKnnNeighborhoodEllipsoidEllipsoidRanges(TypedDict):
    """The ranges of the ellipsoid"""

    major: float
    """The major axis length of the ellipsoid"""
    minor: float
    """The minor axis length of the ellipsoid"""
    semi_major: float
    """The semi major axis length of the ellipsoid"""

class GeostatisticsKnnRotation(TypedDict):
    """The rotation of the ellipsoid"""

    dip: NotRequired[float]
    """second rotation, about the x-axis, in degrees"""
    dip_azimuth: NotRequired[float]
    """first rotation, about the z-axis, in degrees"""
    pitch: NotRequired[float]
    """third rotation, about the z-axis, in degrees"""

class GeostatisticsKnnEllipsoid(TypedDict):
    """
    The ellipsoid, to search for points within
    Alias: anisotropy
    """

    ellipsoid_ranges: GeostatisticsKnnNeighborhoodEllipsoidEllipsoidRanges
    """The ranges of the ellipsoid"""
    rotation: GeostatisticsKnnRotation
    """The rotation of the ellipsoid"""

class GeostatisticsKnnExtendedNeighborhood(TypedDict):
    """In KNN estimation, the value of each evaluation point is determined by the arithmetic mean of a set of nearby points with known values. The search parameters determines which nearby points to use."""

    ellipsoid: GeostatisticsKnnEllipsoid
    """
    The ellipsoid, to search for points within
    Alias: anisotropy
    """
    max_drillholes_per_estimate: NotRequired[int | None]
    """The maximum number of drillholes to be used in each estimate."""
    max_empty_octants: NotRequired[int]
    """The maximum number of empty octants (aka sectors) allowed when searching for neighbors. Omitting this, or a value of 8, disables the octant check."""
    max_empty_quadrants: NotRequired[int | None]
    """The maximum number of empty quadrants (2D sectors, ignoring Z) allowed when searching for neighbors. If not specified, quadrant filtering is disabled."""
    max_samples: int
    """
    The maximum number of samples to use for each evaluation point.
    Alias: max-samples
    """
    max_samples_per_drillhole: NotRequired[int | None]
    """The maximum number of samples to use from each drillhole (defined by drillhole ID)"""
    max_samples_per_octant: NotRequired[int | None]
    """The maximum number of samples to use from each octant."""
    max_samples_per_quadrant: NotRequired[int | None]
    """The maximum number of samples to use from each quadrant (2D sectors, ignoring Z)."""
    min_samples: NotRequired[int]
    """
    The minimum number of samples to use for each evaluation point.
    Alias: min-samples
    """

class GeostatisticsKnnResultAttribute(ResultNode):
    """Attribute containing the KNN result."""

    name: str
    """The name of the output attribute."""
    reference: str
    """Reference to the attribute in the geoscience object."""

class GeostatisticsKnnResultTarget(ResultNode):
    """The target that was created or updated."""

    attribute: GeostatisticsKnnResultAttribute
    """Attribute containing the KNN result."""
    description: str | None
    """The description of the geoscience object."""
    name: str
    """The name of the geoscience object."""
    reference: str
    """Reference to a geoscience object."""
    schema_id: str
    """The ID of the Geoscience Object schema."""

class GeostatisticsKnnResult(TaskResult):
    """Result of the KNN task."""

    message: str
    """A message that says what happened in the task."""
    target: GeostatisticsKnnResultTarget
    """The target that was created or updated."""

class SyncGeostatisticsKnnResultAttribute(SyncResultNode):
    """Attribute containing the KNN result."""

    name: str
    """The name of the output attribute."""
    reference: str
    """Reference to the attribute in the geoscience object."""

class SyncGeostatisticsKnnResultTarget(SyncResultNode):
    """The target that was created or updated."""

    attribute: SyncGeostatisticsKnnResultAttribute
    """Attribute containing the KNN result."""
    description: str | None
    """The description of the geoscience object."""
    name: str
    """The name of the geoscience object."""
    reference: str
    """Reference to a geoscience object."""
    schema_id: str
    """The ID of the Geoscience Object schema."""

class SyncGeostatisticsKnnResult(SyncTaskResult):
    """Result of the KNN task."""

    message: str
    """A message that says what happened in the task."""
    target: SyncGeostatisticsKnnResultTarget
    """The target that was created or updated."""

class GeostatisticsLocationWiseSource(TypedDict):
    """Source object and attribute containing ensemble values."""

    attribute: AttributeInput
    """Reference to the ensemble to be processed."""
    object: ObjectInput
    """Spatial locations of known values."""

class GeostatisticsLocationWiseTarget(TypedDict):
    """Target object to save summary statistics."""

    object: ObjectInput
    """Object to evaluate onto."""

class GeostatisticsLocationWiseMeanAboveCutoff(TypedDict):
    """Compute E[value | value > cutoff] per location for each cutoff."""

    cutoffs: list[float]
    """List of cutoff values to compute E[value | value > cutoff] per location."""

class GeostatisticsLocationWiseProbabilityAboveCutoff(TypedDict):
    """Compute P(value > cutoff) per location for each cutoff."""

    cutoffs: list[float]
    """List of cutoff values to compute P(value > cutoff) per location."""

class GeostatisticsLocationWiseResultResultTargetAttributesItem(ResultNode):
    """Location-wise computed values."""

    name: str
    """The name of the output attribute."""
    reference: str
    """Reference to the attribute in the geoscience object."""

class GeostatisticsLocationWiseResultTarget(ResultNode):
    """The target that was created or updated."""

    attributes: list[GeostatisticsLocationWiseResultResultTargetAttributesItem]
    """Computed location-wise values."""
    description: str | None
    """The description of the geoscience object."""
    name: str
    """The name of the geoscience object."""
    reference: str
    """Reference to a geoscience object."""
    schema_id: str
    """The ID of the Geoscience Object schema."""

class GeostatisticsLocationWiseResult(TaskResult):
    """Result of a location-wise task."""

    message: str
    """A message that says what happened in the task."""
    target: GeostatisticsLocationWiseResultTarget
    """The target that was created or updated."""

class SyncGeostatisticsLocationWiseResultResultTargetAttributesItem(SyncResultNode):
    """Location-wise computed values."""

    name: str
    """The name of the output attribute."""
    reference: str
    """Reference to the attribute in the geoscience object."""

class SyncGeostatisticsLocationWiseResultTarget(SyncResultNode):
    """The target that was created or updated."""

    attributes: list[SyncGeostatisticsLocationWiseResultResultTargetAttributesItem]
    """Computed location-wise values."""
    description: str | None
    """The description of the geoscience object."""
    name: str
    """The name of the geoscience object."""
    reference: str
    """Reference to a geoscience object."""
    schema_id: str
    """The ID of the Geoscience Object schema."""

class SyncGeostatisticsLocationWiseResult(SyncTaskResult):
    """Result of a location-wise task."""

    message: str
    """A message that says what happened in the task."""
    target: SyncGeostatisticsLocationWiseResultTarget
    """The target that was created or updated."""

class GeostatisticsLossCalculationSource(TypedDict):
    """The source object and attribute containing known values."""

    attribute: AttributeInput
    """The original values, used to calculate grade categories."""
    object: ObjectInput
    """The location of a regular or masked 3D grid with known values. Should be one of Regular3DGrid_V1_2_0, Regular3DGrid_V1_3_0, RegularMasked3DGrid_V1_2_0, or RegularMasked3DGrid_V1_3_0."""

class GeostatisticsLossCalculationTargetAttributeCreate(TypedDict):
    name: str
    """The name of the attribute to create."""
    operation: Literal["create"]
    """The operation to perform on the attribute."""

class GeostatisticsLossCalculationTargetAttributeUpdate(TypedDict):
    operation: Literal["update"]
    """The operation to perform on the attribute."""
    reference: AttributeInput
    """Reference to an existing attribute to update."""

class GeostatisticsLossCalculationTarget(TypedDict):
    """The target object and attribute to create or update with grade calculation results."""

    attribute: (
        GeostatisticsLossCalculationTargetAttributeCreate
        | GeostatisticsLossCalculationTargetAttributeUpdate
        | AttributeInput
    )
    """Name of the attribute to create or update on the target object."""
    object: ObjectInput
    """Object to evaluate onto. Can be the same as the source object. Should be one of Regular3DGrid_V1_2_0, Regular3DGrid_V1_3_0, RegularMasked3DGrid_V1_2_0, or RegularMasked3DGrid_V1_3_0."""

class GeostatisticsLossCalculationMaterialCategories(TypedDict):
    """
    Definition of a material category used in grade calculations.
    Each category is defined by a cutoff grade, metal price, and label.
    """

    cutoff_grade: NotRequired[float]
    """The cutoff grade"""
    label: NotRequired[str]
    """The category label"""
    metal_price: NotRequired[float]
    """The metal price"""

class GeostatisticsLossCalculationResultAttribute(ResultNode):
    """Attribute containing the material category results from the grade calculation."""

    name: str
    """The name of the output attribute."""
    reference: str
    """Reference to the attribute in the geoscience object."""

class GeostatisticsLossCalculationResultTarget(ResultNode):
    """The target that was created or updated."""

    attribute: GeostatisticsLossCalculationResultAttribute
    """Attribute containing the material category results from the grade calculation."""
    description: str | None
    """The description of the geoscience object."""
    name: str
    """The name of the geoscience object."""
    reference: str
    """Reference to a geoscience object."""
    schema_id: str
    """The ID of the Geoscience Object schema."""

class GeostatisticsLossCalculationResult(TaskResult):
    """Result of the grade calculation task."""

    message: str
    """Description of the result"""
    target: GeostatisticsLossCalculationResultTarget | None
    """The target that was created or updated."""

class SyncGeostatisticsLossCalculationResultAttribute(SyncResultNode):
    """Attribute containing the material category results from the grade calculation."""

    name: str
    """The name of the output attribute."""
    reference: str
    """Reference to the attribute in the geoscience object."""

class SyncGeostatisticsLossCalculationResultTarget(SyncResultNode):
    """The target that was created or updated."""

    attribute: SyncGeostatisticsLossCalculationResultAttribute
    """Attribute containing the material category results from the grade calculation."""
    description: str | None
    """The description of the geoscience object."""
    name: str
    """The name of the geoscience object."""
    reference: str
    """Reference to a geoscience object."""
    schema_id: str
    """The ID of the Geoscience Object schema."""

class SyncGeostatisticsLossCalculationResult(SyncTaskResult):
    """Result of the grade calculation task."""

    message: str
    """Description of the result"""
    target: SyncGeostatisticsLossCalculationResultTarget | None
    """The target that was created or updated."""

class GeostatisticsNormalScoreSource(TypedDict):
    """The source object and attribute containing the values to transform."""

    attribute: AttributeInput
    """The numeric attribute to apply the normal-score transformation to."""
    object: ObjectInput
    """Object containing the values to transform."""

class GeostatisticsNormalScoreTargetAttributeCreate(TypedDict):
    name: str
    """The name of the attribute to create."""
    operation: Literal["create"]
    """The operation to perform on the attribute."""

class GeostatisticsNormalScoreTargetAttributeUpdate(TypedDict):
    operation: Literal["update"]
    """The operation to perform on the attribute."""
    reference: AttributeInput
    """Reference to an existing attribute to update."""

class GeostatisticsNormalScoreTarget(TypedDict):
    """The target object and attribute to write the transformed values to."""

    attribute: (
        GeostatisticsNormalScoreTargetAttributeCreate | GeostatisticsNormalScoreTargetAttributeUpdate | AttributeInput
    )
    """Name of the attribute to create or update on the target object."""
    object: ObjectInput
    """Object to write the transformed values to."""

class GeostatisticsNormalScoreResultAttribute(ResultNode):
    """Attribute containing the transformed values."""

    name: str
    """The name of the output attribute."""
    reference: str
    """Reference to the attribute in the geoscience object."""

class GeostatisticsNormalScoreResultTarget(ResultNode):
    """The target object that was created or updated."""

    attribute: GeostatisticsNormalScoreResultAttribute
    """Attribute containing the transformed values."""
    description: str | None
    """The description of the geoscience object."""
    name: str
    """The name of the geoscience object."""
    reference: str
    """Reference to a geoscience object."""
    schema_id: str
    """The ID of the Geoscience Object schema."""

class GeostatisticsNormalScoreResult(TaskResult):
    """Result of the normal-score transformation task."""

    message: str
    """A message describing what happened in the task."""
    target: GeostatisticsNormalScoreResultTarget
    """The target object that was created or updated."""

class SyncGeostatisticsNormalScoreResultAttribute(SyncResultNode):
    """Attribute containing the transformed values."""

    name: str
    """The name of the output attribute."""
    reference: str
    """Reference to the attribute in the geoscience object."""

class SyncGeostatisticsNormalScoreResultTarget(SyncResultNode):
    """The target object that was created or updated."""

    attribute: SyncGeostatisticsNormalScoreResultAttribute
    """Attribute containing the transformed values."""
    description: str | None
    """The description of the geoscience object."""
    name: str
    """The name of the geoscience object."""
    reference: str
    """Reference to a geoscience object."""
    schema_id: str
    """The ID of the Geoscience Object schema."""

class SyncGeostatisticsNormalScoreResult(SyncTaskResult):
    """Result of the normal-score transformation task."""

    message: str
    """A message describing what happened in the task."""
    target: SyncGeostatisticsNormalScoreResultTarget
    """The target object that was created or updated."""

class GeostatisticsProfitCalculationSource(TypedDict):
    """The source object and attribute containing known values."""

    attribute: AttributeInput
    """The original values, used to calculate grade categories."""
    object: ObjectInput
    """The location of a regular or masked 3D grid with known values. Should be one of Regular3DGrid_V1_2_0, Regular3DGrid_V1_3_0, RegularMasked3DGrid_V1_2_0, or RegularMasked3DGrid_V1_3_0."""

class GeostatisticsProfitCalculationTargetAttributeCreate(TypedDict):
    name: str
    """The name of the attribute to create."""
    operation: Literal["create"]
    """The operation to perform on the attribute."""

class GeostatisticsProfitCalculationTargetAttributeUpdate(TypedDict):
    operation: Literal["update"]
    """The operation to perform on the attribute."""
    reference: AttributeInput
    """Reference to an existing attribute to update."""

class GeostatisticsProfitCalculationTarget(TypedDict):
    """The target object and attribute to create or update with grade calculation results."""

    attribute: (
        GeostatisticsProfitCalculationTargetAttributeCreate
        | GeostatisticsProfitCalculationTargetAttributeUpdate
        | AttributeInput
    )
    """Name of the attribute to create or update on the target object."""
    object: ObjectInput
    """Object to evaluate onto. Can be the same as the source object. Should be one of Regular3DGrid_V1_2_0, Regular3DGrid_V1_3_0, RegularMasked3DGrid_V1_2_0, or RegularMasked3DGrid_V1_3_0."""

class GeostatisticsProfitCalculationMaterialCategories(TypedDict):
    """
    Definition of a material category used in grade calculations.
    Each category is defined by a cutoff grade, metal price, and label.
    """

    cutoff_grade: NotRequired[float]
    """The cutoff grade"""
    label: NotRequired[str]
    """The category label"""
    metal_price: NotRequired[float]
    """The metal price"""

class GeostatisticsProfitCalculationResultAttribute(ResultNode):
    """Attribute containing the material category results from the grade calculation."""

    name: str
    """The name of the output attribute."""
    reference: str
    """Reference to the attribute in the geoscience object."""

class GeostatisticsProfitCalculationResultTarget(ResultNode):
    """The target that was created or updated."""

    attribute: GeostatisticsProfitCalculationResultAttribute
    """Attribute containing the material category results from the grade calculation."""
    description: str | None
    """The description of the geoscience object."""
    name: str
    """The name of the geoscience object."""
    reference: str
    """Reference to a geoscience object."""
    schema_id: str
    """The ID of the Geoscience Object schema."""

class GeostatisticsProfitCalculationResult(TaskResult):
    """Result of the grade calculation task."""

    message: str
    """Description of the result"""
    target: GeostatisticsProfitCalculationResultTarget | None
    """The target that was created or updated."""

class SyncGeostatisticsProfitCalculationResultAttribute(SyncResultNode):
    """Attribute containing the material category results from the grade calculation."""

    name: str
    """The name of the output attribute."""
    reference: str
    """Reference to the attribute in the geoscience object."""

class SyncGeostatisticsProfitCalculationResultTarget(SyncResultNode):
    """The target that was created or updated."""

    attribute: SyncGeostatisticsProfitCalculationResultAttribute
    """Attribute containing the material category results from the grade calculation."""
    description: str | None
    """The description of the geoscience object."""
    name: str
    """The name of the geoscience object."""
    reference: str
    """Reference to a geoscience object."""
    schema_id: str
    """The ID of the Geoscience Object schema."""

class SyncGeostatisticsProfitCalculationResult(SyncTaskResult):
    """Result of the grade calculation task."""

    message: str
    """Description of the result"""
    target: SyncGeostatisticsProfitCalculationResultTarget | None
    """The target that was created or updated."""

class GeostatisticsSimulationReportNeighborhoodEllipsoidEllipsoidRanges(TypedDict):
    """The ranges of the ellipsoid"""

    major: float
    """The major axis length of the ellipsoid"""
    minor: float
    """The minor axis length of the ellipsoid"""
    semi_major: float
    """The semi major axis length of the ellipsoid"""

class GeostatisticsSimulationReportRotation(TypedDict):
    """The rotation of the ellipsoid"""

    dip: NotRequired[float]
    """second rotation, about the x-axis, in degrees"""
    dip_azimuth: NotRequired[float]
    """first rotation, about the z-axis, in degrees"""
    pitch: NotRequired[float]
    """third rotation, about the z-axis, in degrees"""

class GeostatisticsSimulationReportEllipsoid(TypedDict):
    """
    The ellipsoid, to search for points within
    Alias: anisotropy
    """

    ellipsoid_ranges: GeostatisticsSimulationReportNeighborhoodEllipsoidEllipsoidRanges
    """The ranges of the ellipsoid"""
    rotation: GeostatisticsSimulationReportRotation
    """The rotation of the ellipsoid"""

class GeostatisticsSimulationReportNeighborhood(TypedDict):
    """Search neighborhood for conditioning."""

    ellipsoid: GeostatisticsSimulationReportEllipsoid
    """
    The ellipsoid, to search for points within
    Alias: anisotropy
    """
    max_samples: int
    """
    The maximum number of samples to use for each evaluation point.
    Alias: max-samples
    """
    min_samples: NotRequired[int]
    """
    The minimum number of samples to use for each evaluation point.
    Alias: min-samples
    """

class GeostatisticsSimulationReportBlockDiscretization(TypedDict):
    """Sub-block discretization used for support correction within each grid block."""

    nx: NotRequired[int]
    """Number of subdivisions in the x direction"""
    ny: NotRequired[int]
    """Number of subdivisions in the y direction"""
    nz: NotRequired[int]
    """Number of subdivisions in the z direction"""

class GeostatisticsSimulationReportDistributionTailExtrapolationLower(TypedDict):
    """Power model for extrapolating the lower tail of the distribution. Optional."""

    min: float
    """Minimum extent of tail, must be less than the minimum value in the data."""
    power: float
    """Denominator of the exponent for the power model (between 0 exclusive and 1 inclusive)."""

class GeostatisticsSimulationReportDistributionTailExtrapolationUpper(TypedDict):
    """Power model for extrapolating the upper tail of the distribution."""

    max: float
    """Maximum extent of tail, must be greater than the maximum value in the data."""
    power: float
    """Denominator of the exponent for the power model (between 0 exclusive and 1 inclusive)."""

class GeostatisticsSimulationReportTailExtrapolation(TypedDict):
    """Tail extrapolation"""

    lower: GeostatisticsSimulationReportDistributionTailExtrapolationLower | None
    """Power model for extrapolating the lower tail of the distribution. Optional."""
    upper: GeostatisticsSimulationReportDistributionTailExtrapolationUpper
    """Power model for extrapolating the upper tail of the distribution."""

class GeostatisticsSimulationReportContinuousDistributionParameters(TypedDict):
    """Parameters for a continuous distribution used for normal-score transformation."""

    tail_extrapolation: GeostatisticsSimulationReportTailExtrapolation | None
    """Tail extrapolation"""
    weights: NotRequired[AttributeInput | None]
    """Optional weights attribute for weighted distribution"""

class GeostatisticsSimulationReportValidationReportMaterialCategory(TypedDict):
    """A material category with cutoff grade and price information."""

    cutoff: float
    """Cutoff grade for the material category."""
    min_price: float
    """Minimum metal price for the material category."""
    name: str
    """Name of the material category."""

class GeostatisticsSimulationReportCost(TypedDict):
    """Material cost parameters."""

    mining_ore: float
    """Mining cost per unit of ore material."""
    mining_waste: float
    """Mining cost per unit of waste material."""
    processing: float
    """Processing cost per unit of material."""
    recovery_percentage: float
    """Fraction of metal expected to be recovered during processing."""

class GeostatisticsSimulationReportMaterial(TypedDict):
    """Material cost and category information for loss calculations."""

    categories: list[GeostatisticsSimulationReportValidationReportMaterialCategory]
    """List of material categories."""
    cost: GeostatisticsSimulationReportCost
    """Material cost parameters."""

class GeostatisticsSimulationReportValidationReportContextItem(TypedDict):
    """A single item of contextual information to be included in the validation report."""

    label: str
    """Label of the context item, which will appear on the report."""
    value: str
    """Value of the context item."""

class GeostatisticsSimulationReportReportContext(TypedDict):
    """Context for the validation report."""

    details: list[GeostatisticsSimulationReportValidationReportContextItem]
    """Key-value pairs of details to appear on the report."""
    title: str
    """Title that will appear on the report."""

class GeostatisticsSimulationReportReportMeanThresholds(TypedDict):
    """Thresholds for the mean comparison in the validation report."""

    acceptable: float
    """Threshold for the percentage difference between the reference and simulated means that is considered acceptable."""
    marginal: float
    """Threshold for the percentage difference between the reference and simulated means that is considered marginal."""

class GeostatisticsSimulationReportResultLinks(ResultNode):
    """Links related to the simulation report task."""

    dashboard: str | None
    """Link to the validation dashboard."""

class GeostatisticsSimulationReportResultResultValidationReport(ResultNode):
    """Link to the validation report file."""

    name: str
    """The name of the file, typically the terminal segment of the path."""
    reference: str
    """Reference to a file."""

class GeostatisticsSimulationReportResultValidationSummary(ResultNode):
    """Summary of the validation results."""

    mean: float
    """Mean of the simulated values."""
    reference_mean: float
    """Mean of the input values."""

class GeostatisticsSimulationReportResult(TaskResult):
    """Results of the simulation report compute task."""

    links: GeostatisticsSimulationReportResultLinks
    """Links related to the simulation report task."""
    validation_report: GeostatisticsSimulationReportResultResultValidationReport
    """Link to the validation report file."""
    validation_summary: GeostatisticsSimulationReportResultValidationSummary
    """Summary of the validation results."""

class SyncGeostatisticsSimulationReportResultLinks(SyncResultNode):
    """Links related to the simulation report task."""

    dashboard: str | None
    """Link to the validation dashboard."""

class SyncGeostatisticsSimulationReportResultResultValidationReport(SyncResultNode):
    """Link to the validation report file."""

    name: str
    """The name of the file, typically the terminal segment of the path."""
    reference: str
    """Reference to a file."""

class SyncGeostatisticsSimulationReportResultValidationSummary(SyncResultNode):
    """Summary of the validation results."""

    mean: float
    """Mean of the simulated values."""
    reference_mean: float
    """Mean of the input values."""

class SyncGeostatisticsSimulationReportResult(SyncTaskResult):
    """Results of the simulation report compute task."""

    links: SyncGeostatisticsSimulationReportResultLinks
    """Links related to the simulation report task."""
    validation_report: SyncGeostatisticsSimulationReportResultResultValidationReport
    """Link to the validation report file."""
    validation_summary: SyncGeostatisticsSimulationReportResultValidationSummary
    """Summary of the validation results."""

class GeotechAddToIntersectedGeometryTarget(TypedDict):
    """Target specifying where to create the new Geological Model Meshes object. Provide an object with a reference field containing a path-based geoscience object URL and an overwrite field containing a boolean value. Only path-based object references can be used for creating a new object."""

    description: NotRequired[str | None]
    """Description to put into the target object."""
    overwrite: NotRequired[bool]
    """Whether to overwrite the existing object."""
    reference: ObjectInput
    """Reference to the targeted object."""
    tags: NotRequired[dict[str, str]]
    """Tags to put into the target object."""

class GeotechAddToIntersectedGeometryPoint3D(TypedDict):
    """Maximum bounding point to clip to."""

    x: float
    """The x coordinate."""
    y: float
    """The y coordinate."""
    z: float
    """The z coordinate."""

class GeotechAddToIntersectedGeometryPoint3D2(TypedDict):
    """Minimum bounding point to clip to."""

    x: float
    """The x coordinate."""
    y: float
    """The y coordinate."""
    z: float
    """The z coordinate."""

class GeotechAddToIntersectedGeometryResultResultObject(ResultNode):
    """The new Geological Model Meshes object with added meshes."""

    description: str | None
    """The description of the geoscience object."""
    name: str
    """The name of the geoscience object."""
    reference: str
    """Reference to a geoscience object."""
    schema_id: str
    """The ID of the Geoscience Object schema."""

class GeotechAddToIntersectedGeometryResult(TaskResult):
    """Results of add-to-intersected-geometry."""

    message: str
    """Description of the result of the operation."""
    object: GeotechAddToIntersectedGeometryResultResultObject
    """The new Geological Model Meshes object with added meshes."""

class SyncGeotechAddToIntersectedGeometryResultResultObject(SyncResultNode):
    """The new Geological Model Meshes object with added meshes."""

    description: str | None
    """The description of the geoscience object."""
    name: str
    """The name of the geoscience object."""
    reference: str
    """Reference to a geoscience object."""
    schema_id: str
    """The ID of the Geoscience Object schema."""

class SyncGeotechAddToIntersectedGeometryResult(SyncTaskResult):
    """Results of add-to-intersected-geometry."""

    message: str
    """Description of the result of the operation."""
    object: SyncGeotechAddToIntersectedGeometryResultResultObject
    """The new Geological Model Meshes object with added meshes."""

class GeotechCalculateMeshQualityHistogramResultResultBinsItem(ResultNode):
    """A mesh quality range."""

    lower_bound: float
    """Inclusive lower bound of the quality range."""
    triangle_count: int
    """Number of triangles in this quality range."""
    upper_bound: float
    """Upper bound of the quality range."""

class GeotechCalculateMeshQualityHistogramResult(TaskResult):
    """Results of calculate-mesh-quality-histogram."""

    bins: list[GeotechCalculateMeshQualityHistogramResultResultBinsItem]
    """Ordered quality bins spanning the range from 0.0 to 1.0."""
    message: str
    """Description of the result of the operation."""

class SyncGeotechCalculateMeshQualityHistogramResultResultBinsItem(SyncResultNode):
    """A mesh quality range."""

    lower_bound: float
    """Inclusive lower bound of the quality range."""
    triangle_count: int
    """Number of triangles in this quality range."""
    upper_bound: float
    """Upper bound of the quality range."""

class SyncGeotechCalculateMeshQualityHistogramResult(SyncTaskResult):
    """Results of calculate-mesh-quality-histogram."""

    bins: list[SyncGeotechCalculateMeshQualityHistogramResultResultBinsItem]
    """Ordered quality bins spanning the range from 0.0 to 1.0."""
    message: str
    """Description of the result of the operation."""

class GeotechCheckTriangleOrientationResult(TaskResult):
    """Results of check-triangle-orientation."""

    message: str
    """Description of the result of the operation."""
    triangles: list[int]
    """Indices of triangles with inconsistent orientation relative to their neighbors."""

class SyncGeotechCheckTriangleOrientationResult(SyncTaskResult):
    """Results of check-triangle-orientation."""

    message: str
    """Description of the result of the operation."""
    triangles: list[int]
    """Indices of triangles with inconsistent orientation relative to their neighbors."""

class GeotechCopyGeologicalModelMeshesTarget(TypedDict):
    """Target specifying where to create the new Geological Model Meshes object. Provide an object with a reference field containing a path-based geoscience object URL and an overwrite field containing a boolean value. Only path-based object references can be used for creating a new object."""

    description: NotRequired[str | None]
    """Description to put into the target object."""
    overwrite: NotRequired[bool]
    """Whether to overwrite the existing object."""
    reference: ObjectInput
    """Reference to the targeted object."""
    tags: NotRequired[dict[str, str]]
    """Tags to put into the target object."""

class GeotechCopyGeologicalModelMeshesResultResultObject(ResultNode):
    """The new Geological Model Meshes object."""

    description: str | None
    """The description of the geoscience object."""
    name: str
    """The name of the geoscience object."""
    reference: str
    """Reference to a geoscience object."""
    schema_id: str
    """The ID of the Geoscience Object schema."""

class GeotechCopyGeologicalModelMeshesResult(TaskResult):
    """Results of copy-geological-model-meshes."""

    message: str
    """Description of the result of the operation."""
    object: GeotechCopyGeologicalModelMeshesResultResultObject
    """The new Geological Model Meshes object."""

class SyncGeotechCopyGeologicalModelMeshesResultResultObject(SyncResultNode):
    """The new Geological Model Meshes object."""

    description: str | None
    """The description of the geoscience object."""
    name: str
    """The name of the geoscience object."""
    reference: str
    """Reference to a geoscience object."""
    schema_id: str
    """The ID of the Geoscience Object schema."""

class SyncGeotechCopyGeologicalModelMeshesResult(SyncTaskResult):
    """Results of copy-geological-model-meshes."""

    message: str
    """Description of the result of the operation."""
    object: SyncGeotechCopyGeologicalModelMeshesResultResultObject
    """The new Geological Model Meshes object."""

class GeotechCreateCrossSectionTarget(TypedDict):
    """Target specifying where to create the 2D cross-section Design Geometry result. Provide an object with a reference field containing a path-based geoscience object URL and an overwrite field containing a boolean value."""

    description: NotRequired[str | None]
    """Description to put into the target object."""
    overwrite: NotRequired[bool]
    """Whether to overwrite the existing object."""
    reference: ObjectInput
    """Reference to the targeted object."""
    tags: NotRequired[dict[str, str]]
    """Tags to put into the target object."""

class GeotechCreateCrossSectionPoint3D(TypedDict):
    """Origin point of the cross-section plane."""

    x: float
    """The x coordinate."""
    y: float
    """The y coordinate."""
    z: float
    """The z coordinate."""

class GeotechCreateCrossSectionResultResultObject(ResultNode):
    """The 2D cross-section design geometry output."""

    description: str | None
    """The description of the geoscience object."""
    name: str
    """The name of the geoscience object."""
    reference: str
    """Reference to a geoscience object."""
    schema_id: str
    """The ID of the Geoscience Object schema."""

class GeotechCreateCrossSectionResult(TaskResult):
    """Results for create-cross-section."""

    message: str
    """Summary of the task outcome."""
    object: GeotechCreateCrossSectionResultResultObject
    """The 2D cross-section design geometry output."""

class SyncGeotechCreateCrossSectionResultResultObject(SyncResultNode):
    """The 2D cross-section design geometry output."""

    description: str | None
    """The description of the geoscience object."""
    name: str
    """The name of the geoscience object."""
    reference: str
    """Reference to a geoscience object."""
    schema_id: str
    """The ID of the Geoscience Object schema."""

class SyncGeotechCreateCrossSectionResult(SyncTaskResult):
    """Results for create-cross-section."""

    message: str
    """Summary of the task outcome."""
    object: SyncGeotechCreateCrossSectionResultResultObject
    """The 2D cross-section design geometry output."""

class GeotechCreateGeologicalModelMeshesTarget(TypedDict):
    """Target specifying where to create the new Geological Model Meshes object. Provide an object with a reference field containing a path-based geoscience object URL and an overwrite field containing a boolean value. Only path-based object references can be used for creating a new object."""

    description: NotRequired[str | None]
    """Description to put into the target object."""
    overwrite: NotRequired[bool]
    """Whether to overwrite the existing object."""
    reference: ObjectInput
    """Reference to the targeted object."""
    tags: NotRequired[dict[str, str]]
    """Tags to put into the target object."""

class GeotechCreateGeologicalModelMeshesResultResultObject(ResultNode):
    """The new Geological Model Meshes object."""

    description: str | None
    """The description of the geoscience object."""
    name: str
    """The name of the geoscience object."""
    reference: str
    """Reference to a geoscience object."""
    schema_id: str
    """The ID of the Geoscience Object schema."""

class GeotechCreateGeologicalModelMeshesResult(TaskResult):
    """Results of create-geological-model-meshes."""

    message: str
    """Description of the result of the operation."""
    object: GeotechCreateGeologicalModelMeshesResultResultObject
    """The new Geological Model Meshes object."""

class SyncGeotechCreateGeologicalModelMeshesResultResultObject(SyncResultNode):
    """The new Geological Model Meshes object."""

    description: str | None
    """The description of the geoscience object."""
    name: str
    """The name of the geoscience object."""
    reference: str
    """Reference to a geoscience object."""
    schema_id: str
    """The ID of the Geoscience Object schema."""

class SyncGeotechCreateGeologicalModelMeshesResult(SyncTaskResult):
    """Results of create-geological-model-meshes."""

    message: str
    """Description of the result of the operation."""
    object: SyncGeotechCreateGeologicalModelMeshesResultResultObject
    """The new Geological Model Meshes object."""

class GeotechDetectDegenerateTrianglesResult(TaskResult):
    """Results of detect-degenerate-triangles."""

    message: str
    """Description of the result of the operation."""
    triangles: list[int]
    """Indices of detected degenerate triangles."""

class SyncGeotechDetectDegenerateTrianglesResult(SyncTaskResult):
    """Results of detect-degenerate-triangles."""

    message: str
    """Description of the result of the operation."""
    triangles: list[int]
    """Indices of detected degenerate triangles."""

class GeotechDetectDuplicatePointsResult(TaskResult):
    """Results of detect-duplicate-points."""

    message: str
    """Description of the result of the operation."""
    points: list[int]
    """Indices of duplicate vertices (excluding first occurrence)."""

class SyncGeotechDetectDuplicatePointsResult(SyncTaskResult):
    """Results of detect-duplicate-points."""

    message: str
    """Description of the result of the operation."""
    points: list[int]
    """Indices of duplicate vertices (excluding first occurrence)."""

class GeotechDetectDuplicateTrianglesResult(TaskResult):
    """Results of detect-duplicate-triangles."""

    message: str
    """Description of the result of the operation."""
    triangles: list[int]
    """Indices of detected duplicate triangles (excluding first occurrence)."""

class SyncGeotechDetectDuplicateTrianglesResult(SyncTaskResult):
    """Results of detect-duplicate-triangles."""

    message: str
    """Description of the result of the operation."""
    triangles: list[int]
    """Indices of detected duplicate triangles (excluding first occurrence)."""

class GeotechDetectFinsResult(TaskResult):
    """Results of detect-fins."""

    fins: list[list[int]]
    """Each fin is a list of triangle indices forming a connected component without a closed boundary."""
    message: str
    """Description of the result of the operation."""

class SyncGeotechDetectFinsResult(SyncTaskResult):
    """Results of detect-fins."""

    fins: list[list[int]]
    """Each fin is a list of triangle indices forming a connected component without a closed boundary."""
    message: str
    """Description of the result of the operation."""

class GeotechDetectHolesResult(TaskResult):
    """Results of detect-holes."""

    holes: list[list[list[int]]]
    """Each hole is a list of boundary edges, where each edge is a pair of vertex indices."""
    message: str
    """Description of the result of the operation."""

class SyncGeotechDetectHolesResult(SyncTaskResult):
    """Results of detect-holes."""

    holes: list[list[list[int]]]
    """Each hole is a list of boundary edges, where each edge is a pair of vertex indices."""
    message: str
    """Description of the result of the operation."""

class GeotechDetectNonManifoldEdgesResult(TaskResult):
    """Results of detect-non-manifold-edges."""

    edges: list[list[int]]
    """Non-manifold edges, each as a pair of vertex indices."""
    message: str
    """Description of the result of the operation."""

class SyncGeotechDetectNonManifoldEdgesResult(SyncTaskResult):
    """Results of detect-non-manifold-edges."""

    edges: list[list[int]]
    """Non-manifold edges, each as a pair of vertex indices."""
    message: str
    """Description of the result of the operation."""

class GeotechDetectNonManifoldVerticesResult(TaskResult):
    """Results of detect-non-manifold-vertices."""

    message: str
    """Description of the result of the operation."""
    points: list[int]
    """Indices of vertices with non-manifold topology."""

class SyncGeotechDetectNonManifoldVerticesResult(SyncTaskResult):
    """Results of detect-non-manifold-vertices."""

    message: str
    """Description of the result of the operation."""
    points: list[int]
    """Indices of vertices with non-manifold topology."""

class GeotechDetectNonPartitioningSurfacesResultNonPartitioningSurfaceResult(ResultNode):
    """A single non-partitioning surface."""

    edges: list[list[int]]
    """Boundary edges of this surface, each as a pair of vertex indices."""
    triangles: list[int]
    """Indices of triangles forming this surface."""

class GeotechDetectNonPartitioningSurfacesResult(TaskResult):
    """Results of detect-non-partitioning-surfaces."""

    message: str
    """Description of the result of the operation."""
    surfaces: list[GeotechDetectNonPartitioningSurfacesResultNonPartitioningSurfaceResult]
    """Detected non-partitioning surfaces with their triangles and edges."""

class SyncGeotechDetectNonPartitioningSurfacesResultNonPartitioningSurfaceResult(SyncResultNode):
    """A single non-partitioning surface."""

    edges: list[list[int]]
    """Boundary edges of this surface, each as a pair of vertex indices."""
    triangles: list[int]
    """Indices of triangles forming this surface."""

class SyncGeotechDetectNonPartitioningSurfacesResult(SyncTaskResult):
    """Results of detect-non-partitioning-surfaces."""

    message: str
    """Description of the result of the operation."""
    surfaces: list[SyncGeotechDetectNonPartitioningSurfacesResultNonPartitioningSurfaceResult]
    """Detected non-partitioning surfaces with their triangles and edges."""

class GeotechDetectSelfIntersectionsResult(TaskResult):
    """Results of detect-self-intersections."""

    message: str
    """Description of the result of the operation."""
    triangles: list[int]
    """Indices of triangles involved in self-intersections."""

class SyncGeotechDetectSelfIntersectionsResult(SyncTaskResult):
    """Results of detect-self-intersections."""

    message: str
    """Description of the result of the operation."""
    triangles: list[int]
    """Indices of triangles involved in self-intersections."""

class GeotechFillHolesTarget(TypedDict):
    """Target specifying where to create the output mesh object. Provide an object with a reference field containing a path-based geoscience object URL and an overwrite field containing a boolean value."""

    description: NotRequired[str | None]
    """Description to put into the target object."""
    overwrite: NotRequired[bool]
    """Whether to overwrite the existing object."""
    reference: ObjectInput
    """Reference to the targeted object."""
    tags: NotRequired[dict[str, str]]
    """Tags to put into the target object."""

class GeotechFillHolesResultResultObject(ResultNode):
    """The new mesh object with holes filled."""

    description: str | None
    """The description of the geoscience object."""
    name: str
    """The name of the geoscience object."""
    reference: str
    """Reference to a geoscience object."""
    schema_id: str
    """The ID of the Geoscience Object schema."""

class GeotechFillHolesResult(TaskResult):
    """Results of fill-holes."""

    message: str
    """Description of the result of the operation."""
    object: GeotechFillHolesResultResultObject
    """The new mesh object with holes filled."""

class SyncGeotechFillHolesResultResultObject(SyncResultNode):
    """The new mesh object with holes filled."""

    description: str | None
    """The description of the geoscience object."""
    name: str
    """The name of the geoscience object."""
    reference: str
    """Reference to a geoscience object."""
    schema_id: str
    """The ID of the Geoscience Object schema."""

class SyncGeotechFillHolesResult(SyncTaskResult):
    """Results of fill-holes."""

    message: str
    """Description of the result of the operation."""
    object: SyncGeotechFillHolesResultResultObject
    """The new mesh object with holes filled."""

class GeotechFixSelfIntersectionsTarget(TypedDict):
    """Target specifying where to create the output mesh object. Provide an object with a reference field containing a path-based geoscience object URL and an overwrite field containing a boolean value."""

    description: NotRequired[str | None]
    """Description to put into the target object."""
    overwrite: NotRequired[bool]
    """Whether to overwrite the existing object."""
    reference: ObjectInput
    """Reference to the targeted object."""
    tags: NotRequired[dict[str, str]]
    """Tags to put into the target object."""

class GeotechFixSelfIntersectionsIntersectSettings(TypedDict):
    """Settings that control the intersection detection behaviour."""

    assume_no_self_intersections_in_parts: NotRequired[bool]
    """When true, skip checking each part for self-intersections and only check between parts."""
    tolerance: NotRequired[float]
    """Numerical tolerance used when detecting intersections."""

class GeotechFixSelfIntersectionsResultResultObject(ResultNode):
    """The new mesh object with self-intersections fixed."""

    description: str | None
    """The description of the geoscience object."""
    name: str
    """The name of the geoscience object."""
    reference: str
    """Reference to a geoscience object."""
    schema_id: str
    """The ID of the Geoscience Object schema."""

class GeotechFixSelfIntersectionsResult(TaskResult):
    """Results of fix-self-intersections."""

    message: str
    """Description of the result of the operation."""
    object: GeotechFixSelfIntersectionsResultResultObject
    """The new mesh object with self-intersections fixed."""

class SyncGeotechFixSelfIntersectionsResultResultObject(SyncResultNode):
    """The new mesh object with self-intersections fixed."""

    description: str | None
    """The description of the geoscience object."""
    name: str
    """The name of the geoscience object."""
    reference: str
    """Reference to a geoscience object."""
    schema_id: str
    """The ID of the Geoscience Object schema."""

class SyncGeotechFixSelfIntersectionsResult(SyncTaskResult):
    """Results of fix-self-intersections."""

    message: str
    """Description of the result of the operation."""
    object: SyncGeotechFixSelfIntersectionsResultResultObject
    """The new mesh object with self-intersections fixed."""

class GeotechInitAggregateGeomGmmTarget(TypedDict):
    """Target specifying where to create the new Geological Model Meshes object. Provide an object with a reference field containing a path-based geoscience object URL and an overwrite field containing a boolean value. Only path-based object references can be used for creating a new object."""

    description: NotRequired[str | None]
    """Description to put into the target object."""
    overwrite: NotRequired[bool]
    """Whether to overwrite the existing object."""
    reference: ObjectInput
    """Reference to the targeted object."""
    tags: NotRequired[dict[str, str]]
    """Tags to put into the target object."""

class GeotechInitAggregateGeomGmmPoint3D(TypedDict):
    """The minimum corner of the bounding box."""

    x: float
    """The x coordinate."""
    y: float
    """The y coordinate."""
    z: float
    """The z coordinate."""

class GeotechInitAggregateGeomGmmPoint3D2(TypedDict):
    """The maximum corner of the bounding box."""

    x: float
    """The x coordinate."""
    y: float
    """The y coordinate."""
    z: float
    """The z coordinate."""

class GeotechInitAggregateGeomGmmResultResultObject(ResultNode):
    """The new Geological Model Meshes object."""

    description: str | None
    """The description of the geoscience object."""
    name: str
    """The name of the geoscience object."""
    reference: str
    """Reference to a geoscience object."""
    schema_id: str
    """The ID of the Geoscience Object schema."""

class GeotechInitAggregateGeomGmmResult(TaskResult):
    """Results of init-aggregate-geom-gmm."""

    message: str
    """Description of the result of the operation."""
    object: GeotechInitAggregateGeomGmmResultResultObject
    """The new Geological Model Meshes object."""

class SyncGeotechInitAggregateGeomGmmResultResultObject(SyncResultNode):
    """The new Geological Model Meshes object."""

    description: str | None
    """The description of the geoscience object."""
    name: str
    """The name of the geoscience object."""
    reference: str
    """Reference to a geoscience object."""
    schema_id: str
    """The ID of the Geoscience Object schema."""

class SyncGeotechInitAggregateGeomGmmResult(SyncTaskResult):
    """Results of init-aggregate-geom-gmm."""

    message: str
    """Description of the result of the operation."""
    object: SyncGeotechInitAggregateGeomGmmResultResultObject
    """The new Geological Model Meshes object."""

class GeotechLocalRemeshTarget(TypedDict):
    """Target specifying where to create the output mesh object. Provide an object with a reference field containing a path-based geoscience object URL and an overwrite field containing a boolean value."""

    description: NotRequired[str | None]
    """Description to put into the target object."""
    overwrite: NotRequired[bool]
    """Whether to overwrite the existing object."""
    reference: ObjectInput
    """Reference to the targeted object."""
    tags: NotRequired[dict[str, str]]
    """Tags to put into the target object."""

class GeotechLocalRemeshRemeshSettings(TypedDict):
    """Settings that control the underlying remeshing algorithm."""

    initial_cleanup: NotRequired[bool]
    """Whether to run initial cleanup such as node merging, gap filling, and topological fixing."""
    is_closed: NotRequired[bool]
    """Whether the mesh is closed; should be true for manifold GMVs."""
    line_proximity_detection: NotRequired[bool]
    """Whether to consider distance between skeleton lines when sizing elements."""
    max_chordal_error: NotRequired[float]
    """Maximum relative local error. -0.005 corresponds to 0.5%."""
    optimize: NotRequired[bool]
    """Whether to perform final optimization."""
    patch_angle_tolerance: NotRequired[float]
    """Angle change in degrees used to define a patch skeleton line."""
    regularize_mode: NotRequired[bool]
    """If true, do not remesh; only regularize the mesh."""
    shape_quality_weight: NotRequired[float]
    """Size-versus-shape weight from 0 to 1."""
    strain_tolerance: NotRequired[float]
    """Relative quality difference before and after unfolding to a plane."""
    target_h: NotRequired[float]
    """Target element size. Zero bases the size on skeleton lines."""

class GeotechLocalRemeshResultResultObject(ResultNode):
    """The new mesh object with the local remeshing applied."""

    description: str | None
    """The description of the geoscience object."""
    name: str
    """The name of the geoscience object."""
    reference: str
    """Reference to a geoscience object."""
    schema_id: str
    """The ID of the Geoscience Object schema."""

class GeotechLocalRemeshResult(TaskResult):
    """Results of local-remesh."""

    message: str
    """Description of the result of the operation."""
    object: GeotechLocalRemeshResultResultObject
    """The new mesh object with the local remeshing applied."""

class SyncGeotechLocalRemeshResultResultObject(SyncResultNode):
    """The new mesh object with the local remeshing applied."""

    description: str | None
    """The description of the geoscience object."""
    name: str
    """The name of the geoscience object."""
    reference: str
    """Reference to a geoscience object."""
    schema_id: str
    """The ID of the Geoscience Object schema."""

class SyncGeotechLocalRemeshResult(SyncTaskResult):
    """Results of local-remesh."""

    message: str
    """Description of the result of the operation."""
    object: SyncGeotechLocalRemeshResultResultObject
    """The new mesh object with the local remeshing applied."""

class GeotechMakeGmmIntoIntersectedGeometryTarget(TypedDict):
    """Target specifying where to create the new Geological Model Meshes object. Provide an object with a reference field containing a path-based geoscience object URL and an overwrite field containing a boolean value. Only path-based object references can be used for creating a new object."""

    description: NotRequired[str | None]
    """Description to put into the target object."""
    overwrite: NotRequired[bool]
    """Whether to overwrite the existing object."""
    reference: ObjectInput
    """Reference to the targeted object."""
    tags: NotRequired[dict[str, str]]
    """Tags to put into the target object."""

class GeotechMakeGmmIntoIntersectedGeometryCollapseOverlapsSettings(TypedDict):
    """Settings for collapsing overlapping mesh regions."""

    distance_tolerance: NotRequired[float]
    """Distance tolerance for collapse, relative to the plane of one of the triangles."""
    overlap_threshold: NotRequired[float]
    """Overlap threshold fraction."""

class GeotechMakeGmmIntoIntersectedGeometryPoint3D(TypedDict):
    """Maximum bounding point to clip to."""

    x: float
    """The x coordinate."""
    y: float
    """The y coordinate."""
    z: float
    """The z coordinate."""

class GeotechMakeGmmIntoIntersectedGeometryPoint3D2(TypedDict):
    """Minimum bounding point to clip to."""

    x: float
    """The x coordinate."""
    y: float
    """The y coordinate."""
    z: float
    """The z coordinate."""

class GeotechMakeGmmIntoIntersectedGeometryRemeshSettings(TypedDict):
    """Settings for remeshing during automatic fix."""

    initial_cleanup: NotRequired[bool]
    """Whether to run initial cleanup such as node merging, gap filling, and topological fixing."""
    is_closed: NotRequired[bool]
    """Whether the mesh is closed; should be true for manifold GMVs."""
    line_proximity_detection: NotRequired[bool]
    """Whether to consider distance between skeleton lines when sizing elements."""
    max_chordal_error: NotRequired[float]
    """Maximum relative local error. -0.005 corresponds to 0.5%."""
    optimize: NotRequired[bool]
    """Whether to perform final optimization."""
    patch_angle_tolerance: NotRequired[float]
    """Angle change in degrees used to define a patch skeleton line."""
    regularize_mode: NotRequired[bool]
    """If true, do not remesh; only regularize the mesh."""
    shape_quality_weight: NotRequired[float]
    """Size-versus-shape weight from 0 to 1."""
    strain_tolerance: NotRequired[float]
    """Relative quality difference before and after unfolding to a plane."""
    target_h: NotRequired[float]
    """Target element size. Zero bases the size on skeleton lines."""

class GeotechMakeGmmIntoIntersectedGeometryResultResultObject(ResultNode):
    """The new Geological Model Meshes object with intersected geometry."""

    description: str | None
    """The description of the geoscience object."""
    name: str
    """The name of the geoscience object."""
    reference: str
    """Reference to a geoscience object."""
    schema_id: str
    """The ID of the Geoscience Object schema."""

class GeotechMakeGmmIntoIntersectedGeometryResult(TaskResult):
    """Results of make-gmm-into-intersected-geometry."""

    message: str
    """Description of the result of the operation."""
    object: GeotechMakeGmmIntoIntersectedGeometryResultResultObject
    """The new Geological Model Meshes object with intersected geometry."""

class SyncGeotechMakeGmmIntoIntersectedGeometryResultResultObject(SyncResultNode):
    """The new Geological Model Meshes object with intersected geometry."""

    description: str | None
    """The description of the geoscience object."""
    name: str
    """The name of the geoscience object."""
    reference: str
    """Reference to a geoscience object."""
    schema_id: str
    """The ID of the Geoscience Object schema."""

class SyncGeotechMakeGmmIntoIntersectedGeometryResult(SyncTaskResult):
    """Results of make-gmm-into-intersected-geometry."""

    message: str
    """Description of the result of the operation."""
    object: SyncGeotechMakeGmmIntoIntersectedGeometryResultResultObject
    """The new Geological Model Meshes object with intersected geometry."""

class GeotechMakeIntoModelInputTarget(TypedDict):
    """Target specifying where to create the new Triangle Mesh object. Provide an object with a reference field containing a path-based geoscience object URL and an overwrite field containing a boolean value. Only path-based object references can be used for creating a new object."""

    description: NotRequired[str | None]
    """Description to put into the target object."""
    overwrite: NotRequired[bool]
    """Whether to overwrite the existing object."""
    reference: ObjectInput
    """Reference to the targeted object."""
    tags: NotRequired[dict[str, str]]
    """Tags to put into the target object."""

class GeotechMakeIntoModelInputMaxPoint(TypedDict):
    """Maximum bounding point to clip the triangle mesh to."""

    x: float
    """The x coordinate."""
    y: float
    """The y coordinate."""
    z: float
    """The z coordinate."""

class GeotechMakeIntoModelInputMinPoint(TypedDict):
    """Minimum bounding point to clip the triangle mesh to."""

    x: float
    """The x coordinate."""
    y: float
    """The y coordinate."""
    z: float
    """The z coordinate."""

class GeotechMakeIntoModelInputResultResultObject(ResultNode):
    """The prepared Triangle Mesh object."""

    description: str | None
    """The description of the geoscience object."""
    name: str
    """The name of the geoscience object."""
    reference: str
    """Reference to a geoscience object."""
    schema_id: str
    """The ID of the Geoscience Object schema."""

class GeotechMakeIntoModelInputResult(TaskResult):
    """Results for make-into-model-input."""

    message: str
    """Description of the result of the operation."""
    object: GeotechMakeIntoModelInputResultResultObject
    """The prepared Triangle Mesh object."""

class SyncGeotechMakeIntoModelInputResultResultObject(SyncResultNode):
    """The prepared Triangle Mesh object."""

    description: str | None
    """The description of the geoscience object."""
    name: str
    """The name of the geoscience object."""
    reference: str
    """Reference to a geoscience object."""
    schema_id: str
    """The ID of the Geoscience Object schema."""

class SyncGeotechMakeIntoModelInputResult(SyncTaskResult):
    """Results for make-into-model-input."""

    message: str
    """Description of the result of the operation."""
    object: SyncGeotechMakeIntoModelInputResultResultObject
    """The prepared Triangle Mesh object."""

class GeotechMarkVolumesForRemovalTarget(TypedDict):
    """Target specifying where to create the new Geological Model Meshes object. Provide an object with a reference field containing a path-based geoscience object URL and an overwrite field containing a boolean value. Only path-based object references can be used for creating a new object."""

    description: NotRequired[str | None]
    """Description to put into the target object."""
    overwrite: NotRequired[bool]
    """Whether to overwrite the existing object."""
    reference: ObjectInput
    """Reference to the targeted object."""
    tags: NotRequired[dict[str, str]]
    """Tags to put into the target object."""

class GeotechMarkVolumesForRemovalSettings(TypedDict):
    """Settings for marking volumes for removal."""

    should_remove: NotRequired[bool]
    """When true, mark the specified volumes for removal. When false, clear the removal marking."""
    volume_indices: list[int]
    """Indices of volumes to mark or unmark for removal."""

class GeotechMarkVolumesForRemovalResultResultObject(ResultNode):
    """The new Geological Model Meshes object with volumes marked for removal."""

    description: str | None
    """The description of the geoscience object."""
    name: str
    """The name of the geoscience object."""
    reference: str
    """Reference to a geoscience object."""
    schema_id: str
    """The ID of the Geoscience Object schema."""

class GeotechMarkVolumesForRemovalResult(TaskResult):
    """Results of mark-volumes-for-removal."""

    message: str
    """Description of the result of the operation."""
    object: GeotechMarkVolumesForRemovalResultResultObject
    """The new Geological Model Meshes object with volumes marked for removal."""

class SyncGeotechMarkVolumesForRemovalResultResultObject(SyncResultNode):
    """The new Geological Model Meshes object with volumes marked for removal."""

    description: str | None
    """The description of the geoscience object."""
    name: str
    """The name of the geoscience object."""
    reference: str
    """Reference to a geoscience object."""
    schema_id: str
    """The ID of the Geoscience Object schema."""

class SyncGeotechMarkVolumesForRemovalResult(SyncTaskResult):
    """Results of mark-volumes-for-removal."""

    message: str
    """Description of the result of the operation."""
    object: SyncGeotechMarkVolumesForRemovalResultResultObject
    """The new Geological Model Meshes object with volumes marked for removal."""

class GeotechParameterizeVolumesTarget(TypedDict):
    """Target specifying where to create the Design Geometry result. Provide an object with a reference field containing a path-based geoscience object URL and an overwrite field containing a boolean value."""

    description: NotRequired[str | None]
    """Description to put into the target object."""
    overwrite: NotRequired[bool]
    """Whether to overwrite the existing object."""
    reference: ObjectInput
    """Reference to the targeted object."""
    tags: NotRequired[dict[str, str]]
    """Tags to put into the target object."""

class GeotechParameterizeVolumesResultResultObject(ResultNode):
    """The parameterized design geometry output."""

    description: str | None
    """The description of the geoscience object."""
    name: str
    """The name of the geoscience object."""
    reference: str
    """Reference to a geoscience object."""
    schema_id: str
    """The ID of the Geoscience Object schema."""

class GeotechParameterizeVolumesResult(TaskResult):
    """Results for parameterize-volumes."""

    message: str
    """Summary of the task outcome."""
    object: GeotechParameterizeVolumesResultResultObject
    """The parameterized design geometry output."""

class SyncGeotechParameterizeVolumesResultResultObject(SyncResultNode):
    """The parameterized design geometry output."""

    description: str | None
    """The description of the geoscience object."""
    name: str
    """The name of the geoscience object."""
    reference: str
    """Reference to a geoscience object."""
    schema_id: str
    """The ID of the Geoscience Object schema."""

class SyncGeotechParameterizeVolumesResult(SyncTaskResult):
    """Results for parameterize-volumes."""

    message: str
    """Summary of the task outcome."""
    object: SyncGeotechParameterizeVolumesResultResultObject
    """The parameterized design geometry output."""

class GeotechRemeshTarget(TypedDict):
    """Target specifying where to create the output mesh object. Provide an object with a reference field containing a path-based geoscience object URL and an overwrite field containing a boolean value."""

    description: NotRequired[str | None]
    """Description to put into the target object."""
    overwrite: NotRequired[bool]
    """Whether to overwrite the existing object."""
    reference: ObjectInput
    """Reference to the targeted object."""
    tags: NotRequired[dict[str, str]]
    """Tags to put into the target object."""

class GeotechRemeshResultResultObject(ResultNode):
    """The new mesh object with the remeshing applied."""

    description: str | None
    """The description of the geoscience object."""
    name: str
    """The name of the geoscience object."""
    reference: str
    """Reference to a geoscience object."""
    schema_id: str
    """The ID of the Geoscience Object schema."""

class GeotechRemeshResult(TaskResult):
    """Results of remesh."""

    message: str
    """Description of the result of the operation."""
    object: GeotechRemeshResultResultObject
    """The new mesh object with the remeshing applied."""

class SyncGeotechRemeshResultResultObject(SyncResultNode):
    """The new mesh object with the remeshing applied."""

    description: str | None
    """The description of the geoscience object."""
    name: str
    """The name of the geoscience object."""
    reference: str
    """Reference to a geoscience object."""
    schema_id: str
    """The ID of the Geoscience Object schema."""

class SyncGeotechRemeshResult(SyncTaskResult):
    """Results of remesh."""

    message: str
    """Description of the result of the operation."""
    object: SyncGeotechRemeshResultResultObject
    """The new mesh object with the remeshing applied."""

class GeotechRemoveTrianglesTarget(TypedDict):
    """Target specifying where to create the output mesh object. Provide an object with a reference field containing a path-based geoscience object URL and an overwrite field containing a boolean value."""

    description: NotRequired[str | None]
    """Description to put into the target object."""
    overwrite: NotRequired[bool]
    """Whether to overwrite the existing object."""
    reference: ObjectInput
    """Reference to the targeted object."""
    tags: NotRequired[dict[str, str]]
    """Tags to put into the target object."""

class GeotechRemoveTrianglesResultResultObject(ResultNode):
    """The new mesh object with the selected triangles removed."""

    description: str | None
    """The description of the geoscience object."""
    name: str
    """The name of the geoscience object."""
    reference: str
    """Reference to a geoscience object."""
    schema_id: str
    """The ID of the Geoscience Object schema."""

class GeotechRemoveTrianglesResult(TaskResult):
    """Results of remove-triangles."""

    message: str
    """Description of the result of the operation."""
    object: GeotechRemoveTrianglesResultResultObject
    """The new mesh object with the selected triangles removed."""

class SyncGeotechRemoveTrianglesResultResultObject(SyncResultNode):
    """The new mesh object with the selected triangles removed."""

    description: str | None
    """The description of the geoscience object."""
    name: str
    """The name of the geoscience object."""
    reference: str
    """Reference to a geoscience object."""
    schema_id: str
    """The ID of the Geoscience Object schema."""

class SyncGeotechRemoveTrianglesResult(SyncTaskResult):
    """Results of remove-triangles."""

    message: str
    """Description of the result of the operation."""
    object: SyncGeotechRemoveTrianglesResultResultObject
    """The new mesh object with the selected triangles removed."""

class GeotechSubsumeEmbeddedVolumeInGmmTarget(TypedDict):
    """Target specifying where to create the new Geological Model Meshes object. Provide an object with a reference field containing a path-based geoscience object URL and an overwrite field containing a boolean value. Only path-based object references can be used for creating a new object."""

    description: NotRequired[str | None]
    """Description to put into the target object."""
    overwrite: NotRequired[bool]
    """Whether to overwrite the existing object."""
    reference: ObjectInput
    """Reference to the targeted object."""
    tags: NotRequired[dict[str, str]]
    """Tags to put into the target object."""

class GeotechSubsumeEmbeddedVolumeInGmmExplicitTargetSelection(TypedDict):
    """Select the target volume by its explicit index."""

    selection_mode: Literal["Explicit"]
    """Explicit target selection mode."""
    volume_index: int
    """The index of the target volume."""

class GeotechSubsumeEmbeddedVolumeInGmmLargestNeighbouringVolumeTargetSelection(TypedDict):
    """Automatically select the neighbouring volume with the largest volume."""

    selection_mode: Literal["LargestNeighbouringVolume"]
    """Largest neighbouring volume selection mode."""

class GeotechSubsumeEmbeddedVolumeInGmmLargestNeighbouringContactAreaTargetSelection(TypedDict):
    """Automatically select the neighbouring volume with the largest shared contact area."""

    selection_mode: Literal["LargestNeighbouringContactArea"]
    """Largest neighbouring contact area selection mode."""

class GeotechSubsumeEmbeddedVolumeInGmmResultResultObject(ResultNode):
    """The new Geological Model Meshes object with the volume subsumed."""

    description: str | None
    """The description of the geoscience object."""
    name: str
    """The name of the geoscience object."""
    reference: str
    """Reference to a geoscience object."""
    schema_id: str
    """The ID of the Geoscience Object schema."""

class GeotechSubsumeEmbeddedVolumeInGmmResult(TaskResult):
    """Results of subsume-embedded-volume-in-gmm."""

    message: str
    """Description of the result of the operation."""
    object: GeotechSubsumeEmbeddedVolumeInGmmResultResultObject
    """The new Geological Model Meshes object with the volume subsumed."""

class SyncGeotechSubsumeEmbeddedVolumeInGmmResultResultObject(SyncResultNode):
    """The new Geological Model Meshes object with the volume subsumed."""

    description: str | None
    """The description of the geoscience object."""
    name: str
    """The name of the geoscience object."""
    reference: str
    """Reference to a geoscience object."""
    schema_id: str
    """The ID of the Geoscience Object schema."""

class SyncGeotechSubsumeEmbeddedVolumeInGmmResult(SyncTaskResult):
    """Results of subsume-embedded-volume-in-gmm."""

    message: str
    """Description of the result of the operation."""
    object: SyncGeotechSubsumeEmbeddedVolumeInGmmResultResultObject
    """The new Geological Model Meshes object with the volume subsumed."""

class GeotechValidateGmmAsIntersectedGeometryResult(TaskResult):
    """Results of validate-gmm-as-intersected-geometry."""

    issues: list[str]
    """Descriptions of validation issues found. Empty if the geometry is valid."""
    message: str
    """Description of the result of the operation."""

class SyncGeotechValidateGmmAsIntersectedGeometryResult(SyncTaskResult):
    """Results of validate-gmm-as-intersected-geometry."""

    issues: list[str]
    """Descriptions of validation issues found. Empty if the geometry is valid."""
    message: str
    """Description of the result of the operation."""

class GtmAddToIntersectedGeometryGooseContext(TypedDict):
    """Context information for the Goose API"""

    apiToken: str
    """Authentication token for the API"""
    host: str
    """The hostname for the API"""
    hubUrl: NotRequired[str]
    """Hub URL currently being used"""
    workspaceId: str
    """The workspace identifier"""

class GtmAddToIntersectedGeometryMinPoint(TypedDict):
    """[TODO: Add description for minPoint]"""

    x: float
    """X coordinate of the min point"""
    y: float
    """Y coordinate of the min point"""
    z: float
    """Z coordinate of the min point"""

class GtmAddToIntersectedGeometryMaxPoint(TypedDict):
    """[TODO: Add description for maxPoint]"""

    x: float
    """X coordinate of the max point"""
    y: float
    """Y coordinate of the max point"""
    z: float
    """Z coordinate of the max point"""

class GtmAddToIntersectedGeometryResult(TaskResult):
    """Results of the task."""

    created: list[str]
    """Array of created object references"""
    deleted: list[str]
    """Array of deleted object references"""
    modified: list[str]
    """Array of modified object references"""

class SyncGtmAddToIntersectedGeometryResult(SyncTaskResult):
    """Results of the task."""

    created: list[str]
    """Array of created object references"""
    deleted: list[str]
    """Array of deleted object references"""
    modified: list[str]
    """Array of modified object references"""

class GtmClipGooseContext(TypedDict):
    """Context information for the Goose API"""

    apiToken: str
    """Authentication token for the API"""
    host: str
    """The hostname for the API"""
    hubUrl: NotRequired[str]
    """Hub URL currently being used"""
    workspaceId: str
    """The workspace identifier"""

class GtmClipMinPoint(TypedDict):
    """[TODO: Add description for minPoint]"""

    x: float
    """X coordinate of the min point"""
    y: float
    """Y coordinate of the min point"""
    z: float
    """Z coordinate of the min point"""

class GtmClipMaxPoint(TypedDict):
    """[TODO: Add description for maxPoint]"""

    x: float
    """X coordinate of the max point"""
    y: float
    """Y coordinate of the max point"""
    z: float
    """Z coordinate of the max point"""

class GtmClipResult(TaskResult):
    """Results of the task."""

    created: list[str]
    """Array of created object references"""
    deleted: list[str]
    """Array of deleted object references"""
    modified: list[str]
    """Array of modified object references"""

class SyncGtmClipResult(SyncTaskResult):
    """Results of the task."""

    created: list[str]
    """Array of created object references"""
    deleted: list[str]
    """Array of deleted object references"""
    modified: list[str]
    """Array of modified object references"""

class GtmCreateCrossSectionGooseContext(TypedDict):
    """Context information for the Goose API"""

    apiToken: str
    """Authentication token for the API"""
    host: str
    """The hostname for the API"""
    hubUrl: NotRequired[str]
    """Hub URL currently being used"""
    workspaceId: str
    """The workspace identifier"""

class GtmCreateCrossSectionSectionOrigin(TypedDict):
    """[TODO: Add description for sectionOrigin]"""

    x: float
    """X coordinate of the section origin"""
    y: float
    """Y coordinate of the section origin"""
    z: float
    """Z coordinate of the section origin"""

class GtmCreateCrossSectionResult(TaskResult):
    """Results of the task."""

    created: list[str]
    """Array of created object references"""
    deleted: list[str]
    """Array of deleted object references"""
    modified: list[str]
    """Array of modified object references"""

class SyncGtmCreateCrossSectionResult(SyncTaskResult):
    """Results of the task."""

    created: list[str]
    """Array of created object references"""
    deleted: list[str]
    """Array of deleted object references"""
    modified: list[str]
    """Array of modified object references"""

class GtmCreateGeologicalModelMeshesGooseContext(TypedDict):
    """Context information for the Goose API"""

    apiToken: str
    """Authentication token for the API"""
    host: str
    """The hostname for the API"""
    hubUrl: NotRequired[str]
    """Hub URL currently being used"""
    workspaceId: str
    """The workspace identifier"""

class GtmCreateGeologicalModelMeshesResult(TaskResult):
    """Results of the task."""

    created: list[str]
    """Array of created object references"""
    deleted: list[str]
    """Array of deleted object references"""
    modified: list[str]
    """Array of modified object references"""

class SyncGtmCreateGeologicalModelMeshesResult(SyncTaskResult):
    """Results of the task."""

    created: list[str]
    """Array of created object references"""
    deleted: list[str]
    """Array of deleted object references"""
    modified: list[str]
    """Array of modified object references"""

class GtmDetectDegenerateTrisGooseContext(TypedDict):
    """Context information for the Goose API"""

    apiToken: str
    """Authentication token for the API"""
    host: str
    """The hostname for the API"""
    hubUrl: NotRequired[str]
    """Hub URL currently being used"""
    workspaceId: str
    """The workspace identifier"""

class GtmDetectDegenerateTrisResultResultIssuesItem(ResultNode):
    """A single detection result containing vertex indices, edges and triangle indices."""

    edges: list[list[float]]
    """Array of 2-element arrays representing edge endpoint indices (v1, v2)."""
    points: list[float]
    """Flat list of vertex indices in the mesh."""
    triangles: list[float]
    """List of triangle indices in the mesh."""

class GtmDetectDegenerateTrisResult(TaskResult):
    """Detection results containing issues found."""

    issues: list[GtmDetectDegenerateTrisResultResultIssuesItem]
    """Array of detection issues [{},{},...]."""

class SyncGtmDetectDegenerateTrisResultResultIssuesItem(SyncResultNode):
    """A single detection result containing vertex indices, edges and triangle indices."""

    edges: list[list[float]]
    """Array of 2-element arrays representing edge endpoint indices (v1, v2)."""
    points: list[float]
    """Flat list of vertex indices in the mesh."""
    triangles: list[float]
    """List of triangle indices in the mesh."""

class SyncGtmDetectDegenerateTrisResult(SyncTaskResult):
    """Detection results containing issues found."""

    issues: list[SyncGtmDetectDegenerateTrisResultResultIssuesItem]
    """Array of detection issues [{},{},...]."""

class GtmDetectDuplicatePointsGooseContext(TypedDict):
    """Context information for the Goose API"""

    apiToken: str
    """Authentication token for the API"""
    host: str
    """The hostname for the API"""
    hubUrl: NotRequired[str]
    """Hub URL currently being used"""
    workspaceId: str
    """The workspace identifier"""

class GtmDetectDuplicatePointsResultResultIssuesItem(ResultNode):
    """A single detection result containing vertex indices, edges and triangle indices."""

    edges: list[list[float]]
    """Array of 2-element arrays representing edge endpoint indices (v1, v2)."""
    points: list[float]
    """Flat list of vertex indices in the mesh."""
    triangles: list[float]
    """List of triangle indices in the mesh."""

class GtmDetectDuplicatePointsResult(TaskResult):
    """Detection results containing issues found."""

    issues: list[GtmDetectDuplicatePointsResultResultIssuesItem]
    """Array of detection issues [{},{},...]."""

class SyncGtmDetectDuplicatePointsResultResultIssuesItem(SyncResultNode):
    """A single detection result containing vertex indices, edges and triangle indices."""

    edges: list[list[float]]
    """Array of 2-element arrays representing edge endpoint indices (v1, v2)."""
    points: list[float]
    """Flat list of vertex indices in the mesh."""
    triangles: list[float]
    """List of triangle indices in the mesh."""

class SyncGtmDetectDuplicatePointsResult(SyncTaskResult):
    """Detection results containing issues found."""

    issues: list[SyncGtmDetectDuplicatePointsResultResultIssuesItem]
    """Array of detection issues [{},{},...]."""

class GtmDetectDuplicateTrisGooseContext(TypedDict):
    """Context information for the Goose API"""

    apiToken: str
    """Authentication token for the API"""
    host: str
    """The hostname for the API"""
    hubUrl: NotRequired[str]
    """Hub URL currently being used"""
    workspaceId: str
    """The workspace identifier"""

class GtmDetectDuplicateTrisResultResultIssuesItem(ResultNode):
    """A single detection result containing vertex indices, edges and triangle indices."""

    edges: list[list[float]]
    """Array of 2-element arrays representing edge endpoint indices (v1, v2)."""
    points: list[float]
    """Flat list of vertex indices in the mesh."""
    triangles: list[float]
    """List of triangle indices in the mesh."""

class GtmDetectDuplicateTrisResult(TaskResult):
    """Detection results containing issues found."""

    issues: list[GtmDetectDuplicateTrisResultResultIssuesItem]
    """Array of detection issues [{},{},...]."""

class SyncGtmDetectDuplicateTrisResultResultIssuesItem(SyncResultNode):
    """A single detection result containing vertex indices, edges and triangle indices."""

    edges: list[list[float]]
    """Array of 2-element arrays representing edge endpoint indices (v1, v2)."""
    points: list[float]
    """Flat list of vertex indices in the mesh."""
    triangles: list[float]
    """List of triangle indices in the mesh."""

class SyncGtmDetectDuplicateTrisResult(SyncTaskResult):
    """Detection results containing issues found."""

    issues: list[SyncGtmDetectDuplicateTrisResultResultIssuesItem]
    """Array of detection issues [{},{},...]."""

class GtmDetectFinsGooseContext(TypedDict):
    """Context information for the Goose API"""

    apiToken: str
    """Authentication token for the API"""
    host: str
    """The hostname for the API"""
    hubUrl: NotRequired[str]
    """Hub URL currently being used"""
    workspaceId: str
    """The workspace identifier"""

class GtmDetectFinsResultResultIssuesItem(ResultNode):
    """A single detection result containing vertex indices, edges and triangle indices."""

    edges: list[list[float]]
    """Array of 2-element arrays representing edge endpoint indices (v1, v2)."""
    points: list[float]
    """Flat list of vertex indices in the mesh."""
    triangles: list[float]
    """List of triangle indices in the mesh."""

class GtmDetectFinsResult(TaskResult):
    """Detection results containing issues found."""

    issues: list[GtmDetectFinsResultResultIssuesItem]
    """Array of detection issues [{},{},...]."""

class SyncGtmDetectFinsResultResultIssuesItem(SyncResultNode):
    """A single detection result containing vertex indices, edges and triangle indices."""

    edges: list[list[float]]
    """Array of 2-element arrays representing edge endpoint indices (v1, v2)."""
    points: list[float]
    """Flat list of vertex indices in the mesh."""
    triangles: list[float]
    """List of triangle indices in the mesh."""

class SyncGtmDetectFinsResult(SyncTaskResult):
    """Detection results containing issues found."""

    issues: list[SyncGtmDetectFinsResultResultIssuesItem]
    """Array of detection issues [{},{},...]."""

class GtmDetectHolesGooseContext(TypedDict):
    """Context information for the Goose API"""

    apiToken: str
    """Authentication token for the API"""
    host: str
    """The hostname for the API"""
    hubUrl: NotRequired[str]
    """Hub URL currently being used"""
    workspaceId: str
    """The workspace identifier"""

class GtmDetectHolesResultResultIssuesItem(ResultNode):
    """A single detection result containing vertex indices, edges and triangle indices."""

    edges: list[list[float]]
    """Array of 2-element arrays representing edge endpoint indices (v1, v2)."""
    points: list[float]
    """Flat list of vertex indices in the mesh."""
    triangles: list[float]
    """List of triangle indices in the mesh."""

class GtmDetectHolesResult(TaskResult):
    """Detection results containing issues found."""

    issues: list[GtmDetectHolesResultResultIssuesItem]
    """Array of detection issues [{},{},...]."""

class SyncGtmDetectHolesResultResultIssuesItem(SyncResultNode):
    """A single detection result containing vertex indices, edges and triangle indices."""

    edges: list[list[float]]
    """Array of 2-element arrays representing edge endpoint indices (v1, v2)."""
    points: list[float]
    """Flat list of vertex indices in the mesh."""
    triangles: list[float]
    """List of triangle indices in the mesh."""

class SyncGtmDetectHolesResult(SyncTaskResult):
    """Detection results containing issues found."""

    issues: list[SyncGtmDetectHolesResultResultIssuesItem]
    """Array of detection issues [{},{},...]."""

class GtmDetectInconsistentlyOrientedTrisGooseContext(TypedDict):
    """Context information for the Goose API"""

    apiToken: str
    """Authentication token for the API"""
    host: str
    """The hostname for the API"""
    hubUrl: NotRequired[str]
    """Hub URL currently being used"""
    workspaceId: str
    """The workspace identifier"""

class GtmDetectInconsistentlyOrientedTrisResultResultIssuesItem(ResultNode):
    """A single detection result containing vertex indices, edges and triangle indices."""

    edges: list[list[float]]
    """Array of 2-element arrays representing edge endpoint indices (v1, v2)."""
    points: list[float]
    """Flat list of vertex indices in the mesh."""
    triangles: list[float]
    """List of triangle indices in the mesh."""

class GtmDetectInconsistentlyOrientedTrisResult(TaskResult):
    """Detection results containing issues found."""

    issues: list[GtmDetectInconsistentlyOrientedTrisResultResultIssuesItem]
    """Array of detection issues [{},{},...]."""

class SyncGtmDetectInconsistentlyOrientedTrisResultResultIssuesItem(SyncResultNode):
    """A single detection result containing vertex indices, edges and triangle indices."""

    edges: list[list[float]]
    """Array of 2-element arrays representing edge endpoint indices (v1, v2)."""
    points: list[float]
    """Flat list of vertex indices in the mesh."""
    triangles: list[float]
    """List of triangle indices in the mesh."""

class SyncGtmDetectInconsistentlyOrientedTrisResult(SyncTaskResult):
    """Detection results containing issues found."""

    issues: list[SyncGtmDetectInconsistentlyOrientedTrisResultResultIssuesItem]
    """Array of detection issues [{},{},...]."""

class GtmDetectNonManifoldEdgesGooseContext(TypedDict):
    """Context information for the Goose API"""

    apiToken: str
    """Authentication token for the API"""
    host: str
    """The hostname for the API"""
    hubUrl: NotRequired[str]
    """Hub URL currently being used"""
    workspaceId: str
    """The workspace identifier"""

class GtmDetectNonManifoldEdgesResultResultIssuesItem(ResultNode):
    """A single detection result containing vertex indices, edges and triangle indices."""

    edges: list[list[float]]
    """Array of 2-element arrays representing edge endpoint indices (v1, v2)."""
    points: list[float]
    """Flat list of vertex indices in the mesh."""
    triangles: list[float]
    """List of triangle indices in the mesh."""

class GtmDetectNonManifoldEdgesResult(TaskResult):
    """Detection results containing issues found."""

    issues: list[GtmDetectNonManifoldEdgesResultResultIssuesItem]
    """Array of detection issues [{},{},...]."""

class SyncGtmDetectNonManifoldEdgesResultResultIssuesItem(SyncResultNode):
    """A single detection result containing vertex indices, edges and triangle indices."""

    edges: list[list[float]]
    """Array of 2-element arrays representing edge endpoint indices (v1, v2)."""
    points: list[float]
    """Flat list of vertex indices in the mesh."""
    triangles: list[float]
    """List of triangle indices in the mesh."""

class SyncGtmDetectNonManifoldEdgesResult(SyncTaskResult):
    """Detection results containing issues found."""

    issues: list[SyncGtmDetectNonManifoldEdgesResultResultIssuesItem]
    """Array of detection issues [{},{},...]."""

class GtmDetectNonManifoldVerticesGooseContext(TypedDict):
    """Context information for the Goose API"""

    apiToken: str
    """Authentication token for the API"""
    host: str
    """The hostname for the API"""
    hubUrl: NotRequired[str]
    """Hub URL currently being used"""
    workspaceId: str
    """The workspace identifier"""

class GtmDetectNonManifoldVerticesResultResultIssuesItem(ResultNode):
    """A single detection result containing vertex indices, edges and triangle indices."""

    edges: list[list[float]]
    """Array of 2-element arrays representing edge endpoint indices (v1, v2)."""
    points: list[float]
    """Flat list of vertex indices in the mesh."""
    triangles: list[float]
    """List of triangle indices in the mesh."""

class GtmDetectNonManifoldVerticesResult(TaskResult):
    """Detection results containing issues found."""

    issues: list[GtmDetectNonManifoldVerticesResultResultIssuesItem]
    """Array of detection issues [{},{},...]."""

class SyncGtmDetectNonManifoldVerticesResultResultIssuesItem(SyncResultNode):
    """A single detection result containing vertex indices, edges and triangle indices."""

    edges: list[list[float]]
    """Array of 2-element arrays representing edge endpoint indices (v1, v2)."""
    points: list[float]
    """Flat list of vertex indices in the mesh."""
    triangles: list[float]
    """List of triangle indices in the mesh."""

class SyncGtmDetectNonManifoldVerticesResult(SyncTaskResult):
    """Detection results containing issues found."""

    issues: list[SyncGtmDetectNonManifoldVerticesResultResultIssuesItem]
    """Array of detection issues [{},{},...]."""

class GtmDetectRedundantSurfacesGooseContext(TypedDict):
    """Context information for the Goose API"""

    apiToken: str
    """Authentication token for the API"""
    host: str
    """The hostname for the API"""
    hubUrl: NotRequired[str]
    """Hub URL currently being used"""
    workspaceId: str
    """The workspace identifier"""

class GtmDetectRedundantSurfacesResultResultIssuesItem(ResultNode):
    """A single detection result containing vertex indices, edges and triangle indices."""

    edges: list[list[float]]
    """Array of 2-element arrays representing edge endpoint indices (v1, v2)."""
    points: list[float]
    """Flat list of vertex indices in the mesh."""
    triangles: list[float]
    """List of triangle indices in the mesh."""

class GtmDetectRedundantSurfacesResult(TaskResult):
    """Detection results containing issues found."""

    issues: list[GtmDetectRedundantSurfacesResultResultIssuesItem]
    """Array of detection issues [{},{},...]."""

class SyncGtmDetectRedundantSurfacesResultResultIssuesItem(SyncResultNode):
    """A single detection result containing vertex indices, edges and triangle indices."""

    edges: list[list[float]]
    """Array of 2-element arrays representing edge endpoint indices (v1, v2)."""
    points: list[float]
    """Flat list of vertex indices in the mesh."""
    triangles: list[float]
    """List of triangle indices in the mesh."""

class SyncGtmDetectRedundantSurfacesResult(SyncTaskResult):
    """Detection results containing issues found."""

    issues: list[SyncGtmDetectRedundantSurfacesResultResultIssuesItem]
    """Array of detection issues [{},{},...]."""

class GtmDetectSelfIntersectionsGooseContext(TypedDict):
    """Context information for the Goose API"""

    apiToken: str
    """Authentication token for the API"""
    host: str
    """The hostname for the API"""
    hubUrl: NotRequired[str]
    """Hub URL currently being used"""
    workspaceId: str
    """The workspace identifier"""

class GtmDetectSelfIntersectionsResultResultIssuesItem(ResultNode):
    """A single detection result containing vertex indices, edges and triangle indices."""

    edges: list[list[float]]
    """Array of 2-element arrays representing edge endpoint indices (v1, v2)."""
    points: list[float]
    """Flat list of vertex indices in the mesh."""
    triangles: list[float]
    """List of triangle indices in the mesh."""

class GtmDetectSelfIntersectionsResult(TaskResult):
    """Detection results containing issues found."""

    issues: list[GtmDetectSelfIntersectionsResultResultIssuesItem]
    """Array of detection issues [{},{},...]."""

class SyncGtmDetectSelfIntersectionsResultResultIssuesItem(SyncResultNode):
    """A single detection result containing vertex indices, edges and triangle indices."""

    edges: list[list[float]]
    """Array of 2-element arrays representing edge endpoint indices (v1, v2)."""
    points: list[float]
    """Flat list of vertex indices in the mesh."""
    triangles: list[float]
    """List of triangle indices in the mesh."""

class SyncGtmDetectSelfIntersectionsResult(SyncTaskResult):
    """Detection results containing issues found."""

    issues: list[SyncGtmDetectSelfIntersectionsResultResultIssuesItem]
    """Array of detection issues [{},{},...]."""

class GtmFillHolesGooseContext(TypedDict):
    """Context information for the Goose API"""

    apiToken: str
    """Authentication token for the API"""
    host: str
    """The hostname for the API"""
    hubUrl: NotRequired[str]
    """Hub URL currently being used"""
    workspaceId: str
    """The workspace identifier"""

class GtmFillHolesResult(TaskResult):
    """Results of the task."""

    created: list[str]
    """Array of created object references"""
    deleted: list[str]
    """Array of deleted object references"""
    modified: list[str]
    """Array of modified object references"""

class SyncGtmFillHolesResult(SyncTaskResult):
    """Results of the task."""

    created: list[str]
    """Array of created object references"""
    deleted: list[str]
    """Array of deleted object references"""
    modified: list[str]
    """Array of modified object references"""

class GtmFixSelfIntersectionsGooseContext(TypedDict):
    """Context information for the Goose API"""

    apiToken: str
    """Authentication token for the API"""
    host: str
    """The hostname for the API"""
    hubUrl: NotRequired[str]
    """Hub URL currently being used"""
    workspaceId: str
    """The workspace identifier"""

class GtmFixSelfIntersectionsResult(TaskResult):
    """Results of the task."""

    created: list[str]
    """Array of created object references"""
    deleted: list[str]
    """Array of deleted object references"""
    modified: list[str]
    """Array of modified object references"""

class SyncGtmFixSelfIntersectionsResult(SyncTaskResult):
    """Results of the task."""

    created: list[str]
    """Array of created object references"""
    deleted: list[str]
    """Array of deleted object references"""
    modified: list[str]
    """Array of modified object references"""

class GtmInitAggregateGeomGooseContext(TypedDict):
    """Context information for the Goose API"""

    apiToken: str
    """Authentication token for the API"""
    host: str
    """The hostname for the API"""
    hubUrl: NotRequired[str]
    """Hub URL currently being used"""
    workspaceId: str
    """The workspace identifier"""

class GtmInitAggregateGeomMinPoint(TypedDict):
    """[TODO: Add description for minPoint]"""

    x: float
    """X coordinate of the min point"""
    y: float
    """Y coordinate of the min point"""
    z: float
    """Z coordinate of the min point"""

class GtmInitAggregateGeomMaxPoint(TypedDict):
    """[TODO: Add description for maxPoint]"""

    x: float
    """X coordinate of the max point"""
    y: float
    """Y coordinate of the max point"""
    z: float
    """Z coordinate of the max point"""

class GtmInitAggregateGeomResult(TaskResult):
    """Results of the task."""

    created: list[str]
    """Array of created object references"""
    deleted: list[str]
    """Array of deleted object references"""
    modified: list[str]
    """Array of modified object references"""

class SyncGtmInitAggregateGeomResult(SyncTaskResult):
    """Results of the task."""

    created: list[str]
    """Array of created object references"""
    deleted: list[str]
    """Array of deleted object references"""
    modified: list[str]
    """Array of modified object references"""

class GtmIntersectGooseContext(TypedDict):
    """Context information for the Goose API"""

    apiToken: str
    """Authentication token for the API"""
    host: str
    """The hostname for the API"""
    hubUrl: NotRequired[str]
    """Hub URL currently being used"""
    workspaceId: str
    """The workspace identifier"""

class GtmIntersectResult(TaskResult):
    """Results of the task."""

    created: list[str]
    """Array of created object references"""
    deleted: list[str]
    """Array of deleted object references"""
    modified: list[str]
    """Array of modified object references"""

class SyncGtmIntersectResult(SyncTaskResult):
    """Results of the task."""

    created: list[str]
    """Array of created object references"""
    deleted: list[str]
    """Array of deleted object references"""
    modified: list[str]
    """Array of modified object references"""

class GtmLocalRemeshGooseContext(TypedDict):
    """Context information for the Goose API"""

    apiToken: str
    """Authentication token for the API"""
    host: str
    """The hostname for the API"""
    hubUrl: NotRequired[str]
    """Hub URL currently being used"""
    workspaceId: str
    """The workspace identifier"""

class GtmLocalRemeshResult(TaskResult):
    """Results of the task."""

    created: list[str]
    """Array of created object references"""
    deleted: list[str]
    """Array of deleted object references"""
    modified: list[str]
    """Array of modified object references"""

class SyncGtmLocalRemeshResult(SyncTaskResult):
    """Results of the task."""

    created: list[str]
    """Array of created object references"""
    deleted: list[str]
    """Array of deleted object references"""
    modified: list[str]
    """Array of modified object references"""

class GtmMakeGmmIntoIntersectedGeometryGooseContext(TypedDict):
    """Context information for the Goose API"""

    apiToken: str
    """Authentication token for the API"""
    host: str
    """The hostname for the API"""
    hubUrl: NotRequired[str]
    """Hub URL currently being used"""
    workspaceId: str
    """The workspace identifier"""

class GtmMakeGmmIntoIntersectedGeometryMinPoint(TypedDict):
    """Lower corner of the clipping bounding box."""

    x: float
    """X coordinate of the bounds minimum point for clipping"""
    y: float
    """Y coordinate of the bounds minimum point for clipping"""
    z: float
    """Z coordinate of the bounds minimum point for clipping"""

class GtmMakeGmmIntoIntersectedGeometryMaxPoint(TypedDict):
    """Upper corner of the clipping bounding box."""

    x: float
    """X coordinate of the bounds maximum point for clipping"""
    y: float
    """Y coordinate of the bounds maximum point for clipping"""
    z: float
    """Z coordinate of the bounds maximum point for clipping"""

class GtmMakeGmmIntoIntersectedGeometryResult(TaskResult):
    """Results of the task."""

    created: list[str]
    """Array of created object references"""
    deleted: list[str]
    """Array of deleted object references"""
    modified: list[str]
    """Array of modified object references"""

class SyncGtmMakeGmmIntoIntersectedGeometryResult(SyncTaskResult):
    """Results of the task."""

    created: list[str]
    """Array of created object references"""
    deleted: list[str]
    """Array of deleted object references"""
    modified: list[str]
    """Array of modified object references"""

class GtmMakeIntoModelInputGooseContext(TypedDict):
    """Context information for the Goose API"""

    apiToken: str
    """Authentication token for the API"""
    host: str
    """The hostname for the API"""
    hubUrl: NotRequired[str]
    """Hub URL currently being used"""
    workspaceId: str
    """The workspace identifier"""

class GtmMakeIntoModelInputMinPoint(TypedDict):
    """[TODO: Add description for minPoint]"""

    x: float
    """X coordinate of the min point"""
    y: float
    """Y coordinate of the min point"""
    z: float
    """Z coordinate of the min point"""

class GtmMakeIntoModelInputMaxPoint(TypedDict):
    """[TODO: Add description for maxPoint]"""

    x: float
    """X coordinate of the max point"""
    y: float
    """Y coordinate of the max point"""
    z: float
    """Z coordinate of the max point"""

class GtmMakeIntoModelInputResult(TaskResult):
    """Results of the task."""

    created: list[str]
    """Array of created object references"""
    deleted: list[str]
    """Array of deleted object references"""
    modified: list[str]
    """Array of modified object references"""

class SyncGtmMakeIntoModelInputResult(SyncTaskResult):
    """Results of the task."""

    created: list[str]
    """Array of created object references"""
    deleted: list[str]
    """Array of deleted object references"""
    modified: list[str]
    """Array of modified object references"""

class GtmMergePointsGooseContext(TypedDict):
    """Context information for the Goose API"""

    apiToken: str
    """Authentication token for the API"""
    host: str
    """The hostname for the API"""
    hubUrl: NotRequired[str]
    """Hub URL currently being used"""
    workspaceId: str
    """The workspace identifier"""

class GtmMergePointsResult(TaskResult):
    """Results of the task."""

    created: list[str]
    """Array of created object references"""
    deleted: list[str]
    """Array of deleted object references"""
    modified: list[str]
    """Array of modified object references"""

class SyncGtmMergePointsResult(SyncTaskResult):
    """Results of the task."""

    created: list[str]
    """Array of created object references"""
    deleted: list[str]
    """Array of deleted object references"""
    modified: list[str]
    """Array of modified object references"""

class GtmOrientTrisGooseContext(TypedDict):
    """Context information for the Goose API"""

    apiToken: str
    """Authentication token for the API"""
    host: str
    """The hostname for the API"""
    hubUrl: NotRequired[str]
    """Hub URL currently being used"""
    workspaceId: str
    """The workspace identifier"""

class GtmOrientTrisResult(TaskResult):
    """Results of the task."""

    created: list[str]
    """Array of created object references"""
    deleted: list[str]
    """Array of deleted object references"""
    modified: list[str]
    """Array of modified object references"""

class SyncGtmOrientTrisResult(SyncTaskResult):
    """Results of the task."""

    created: list[str]
    """Array of created object references"""
    deleted: list[str]
    """Array of deleted object references"""
    modified: list[str]
    """Array of modified object references"""

class GtmParameterizeVolumesGooseContext(TypedDict):
    """Context information for the Goose API"""

    apiToken: str
    """Authentication token for the API"""
    host: str
    """The hostname for the API"""
    hubUrl: NotRequired[str]
    """Hub URL currently being used"""
    workspaceId: str
    """The workspace identifier"""

class GtmParameterizeVolumesResult(TaskResult):
    """Results of the task."""

    created: list[str]
    """Array of created object references"""
    deleted: list[str]
    """Array of deleted object references"""
    modified: list[str]
    """Array of modified object references"""

class SyncGtmParameterizeVolumesResult(SyncTaskResult):
    """Results of the task."""

    created: list[str]
    """Array of created object references"""
    deleted: list[str]
    """Array of deleted object references"""
    modified: list[str]
    """Array of modified object references"""

class GtmRemeshGooseContext(TypedDict):
    """Context information for the Goose API"""

    apiToken: str
    """Authentication token for the API"""
    host: str
    """The hostname for the API"""
    hubUrl: NotRequired[str]
    """Hub URL currently being used"""
    workspaceId: str
    """The workspace identifier"""

class GtmRemeshResult(TaskResult):
    """Results of the task."""

    created: list[str]
    """Array of created object references"""
    deleted: list[str]
    """Array of deleted object references"""
    modified: list[str]
    """Array of modified object references"""

class SyncGtmRemeshResult(SyncTaskResult):
    """Results of the task."""

    created: list[str]
    """Array of created object references"""
    deleted: list[str]
    """Array of deleted object references"""
    modified: list[str]
    """Array of modified object references"""

class GtmRemoveDegenerateTrisGooseContext(TypedDict):
    """Context information for the Goose API"""

    apiToken: str
    """Authentication token for the API"""
    host: str
    """The hostname for the API"""
    hubUrl: NotRequired[str]
    """Hub URL currently being used"""
    workspaceId: str
    """The workspace identifier"""

class GtmRemoveDegenerateTrisResult(TaskResult):
    """Results of the task."""

    created: list[str]
    """Array of created object references"""
    deleted: list[str]
    """Array of deleted object references"""
    modified: list[str]
    """Array of modified object references"""

class SyncGtmRemoveDegenerateTrisResult(SyncTaskResult):
    """Results of the task."""

    created: list[str]
    """Array of created object references"""
    deleted: list[str]
    """Array of deleted object references"""
    modified: list[str]
    """Array of modified object references"""

class GtmRemoveTrisGooseContext(TypedDict):
    """Context information for the Goose API"""

    apiToken: str
    """Authentication token for the API"""
    host: str
    """The hostname for the API"""
    hubUrl: NotRequired[str]
    """Hub URL currently being used"""
    workspaceId: str
    """The workspace identifier"""

class GtmRemoveTrisResult(TaskResult):
    """Results of the task."""

    created: list[str]
    """Array of created object references"""
    deleted: list[str]
    """Array of deleted object references"""
    modified: list[str]
    """Array of modified object references"""

class SyncGtmRemoveTrisResult(SyncTaskResult):
    """Results of the task."""

    created: list[str]
    """Array of created object references"""
    deleted: list[str]
    """Array of deleted object references"""
    modified: list[str]
    """Array of modified object references"""

class GtmRemoveUnusedPointsGooseContext(TypedDict):
    """Context information for the Goose API"""

    apiToken: str
    """Authentication token for the API"""
    host: str
    """The hostname for the API"""
    hubUrl: NotRequired[str]
    """Hub URL currently being used"""
    workspaceId: str
    """The workspace identifier"""

class GtmRemoveUnusedPointsResult(TaskResult):
    """Results of the task."""

    created: list[str]
    """Array of created object references"""
    deleted: list[str]
    """Array of deleted object references"""
    modified: list[str]
    """Array of modified object references"""

class SyncGtmRemoveUnusedPointsResult(SyncTaskResult):
    """Results of the task."""

    created: list[str]
    """Array of created object references"""
    deleted: list[str]
    """Array of deleted object references"""
    modified: list[str]
    """Array of modified object references"""

class GtmRotateGooseContext(TypedDict):
    """Context information for the Goose API"""

    apiToken: str
    """Authentication token for the API"""
    host: str
    """The hostname for the API"""
    hubUrl: NotRequired[str]
    """Hub URL currently being used"""
    workspaceId: str
    """The workspace identifier"""

class GtmRotateAngles(TypedDict):
    """[TODO: Add description for angles]"""

    x: float
    """[TODO: Add description for x]"""
    y: float
    """[TODO: Add description for y]"""
    z: float
    """[TODO: Add description for z]"""

class GtmRotateResult(TaskResult):
    """Results of the task."""

    created: list[str]
    """Array of created object references"""
    deleted: list[str]
    """Array of deleted object references"""
    modified: list[str]
    """Array of modified object references"""

class SyncGtmRotateResult(SyncTaskResult):
    """Results of the task."""

    created: list[str]
    """Array of created object references"""
    deleted: list[str]
    """Array of deleted object references"""
    modified: list[str]
    """Array of modified object references"""

class GtmSplitComponentsGooseContext(TypedDict):
    """Context information for the Goose API"""

    apiToken: str
    """Authentication token for the API"""
    host: str
    """The hostname for the API"""
    hubUrl: NotRequired[str]
    """Hub URL currently being used"""
    workspaceId: str
    """The workspace identifier"""

class GtmSplitComponentsResult(TaskResult):
    """Results of the task."""

    created: list[str]
    """Array of created object references"""
    deleted: list[str]
    """Array of deleted object references"""
    modified: list[str]
    """Array of modified object references"""

class SyncGtmSplitComponentsResult(SyncTaskResult):
    """Results of the task."""

    created: list[str]
    """Array of created object references"""
    deleted: list[str]
    """Array of deleted object references"""
    modified: list[str]
    """Array of modified object references"""

class GtmSubsumeEmbeddedVolumeInGmmGooseContext(TypedDict):
    """Context information for the Goose API"""

    apiToken: str
    """Authentication token for the API"""
    host: str
    """The hostname for the API"""
    hubUrl: NotRequired[str]
    """Hub URL currently being used"""
    workspaceId: str
    """The workspace identifier"""

class GtmSubsumeEmbeddedVolumeInGmmSubsumeTargetSubsumeByIndex(TypedDict):
    """Provide a specific embedded volume index to subsume into."""

    subsumeByIndex: int
    """Index of the embedded volume to subsume into."""
    type: Literal["subsumeByIndex"]
    """Indicates that a specific target index is being provided."""

class GtmSubsumeEmbeddedVolumeInGmmSubsumeTargetSubsumeAutomaticallyByStrategy(TypedDict):
    """Choose a strategy to automatically select the target embedding."""

    subsumeAutomaticallyByStrategy: Literal["LargestNeighbouringVolume", "LargestNeighbouringContactArea"]
    """Strategy to automatically select the target volume."""
    type: Literal["subsumeAutomaticallyByStrategy"]
    """Indicates that a selection strategy is being provided."""

class GtmSubsumeEmbeddedVolumeInGmmResult(TaskResult):
    """Results of the task."""

    created: list[str]
    """Array of created object references"""
    deleted: list[str]
    """Array of deleted object references"""
    modified: list[str]
    """Array of modified object references"""

class SyncGtmSubsumeEmbeddedVolumeInGmmResult(SyncTaskResult):
    """Results of the task."""

    created: list[str]
    """Array of created object references"""
    deleted: list[str]
    """Array of deleted object references"""
    modified: list[str]
    """Array of modified object references"""

class GtmTranslateGooseContext(TypedDict):
    """Context information for the Goose API"""

    apiToken: str
    """Authentication token for the API"""
    host: str
    """The hostname for the API"""
    hubUrl: NotRequired[str]
    """Hub URL currently being used"""
    workspaceId: str
    """The workspace identifier"""

class GtmTranslateDelta(TypedDict):
    """[TODO: Add description for delta]"""

    x: float
    """X coordinate of the delta"""
    y: float
    """Y coordinate of the delta"""
    z: float
    """Z coordinate of the delta"""

class GtmTranslateResult(TaskResult):
    """Results of the task."""

    created: list[str]
    """Array of created object references"""
    deleted: list[str]
    """Array of deleted object references"""
    modified: list[str]
    """Array of modified object references"""

class SyncGtmTranslateResult(SyncTaskResult):
    """Results of the task."""

    created: list[str]
    """Array of created object references"""
    deleted: list[str]
    """Array of deleted object references"""
    modified: list[str]
    """Array of modified object references"""

class GtmUploadMeshDataGooseContext(TypedDict):
    """Context information for the Goose API"""

    apiToken: str
    """Authentication token for the API"""
    host: str
    """The hostname for the API"""
    hubUrl: NotRequired[str]
    """Hub URL currently being used"""
    workspaceId: str
    """The workspace identifier"""

class GtmUploadMeshDataResult(TaskResult):
    """Results of the task."""

    created: list[str]
    """Array of created object references"""
    deleted: list[str]
    """Array of deleted object references"""
    modified: list[str]
    """Array of modified object references"""

class SyncGtmUploadMeshDataResult(SyncTaskResult):
    """Results of the task."""

    created: list[str]
    """Array of created object references"""
    deleted: list[str]
    """Array of deleted object references"""
    modified: list[str]
    """Array of modified object references"""

class GtmValidateGmmAsIntersectedGeometryGooseContext(TypedDict):
    """Context information for the Goose API"""

    apiToken: str
    """Authentication token for the API"""
    host: str
    """The hostname for the API"""
    hubUrl: NotRequired[str]
    """Hub URL currently being used"""
    workspaceId: str
    """The workspace identifier"""

class GtmValidateGmmAsIntersectedGeometryResult(TaskResult):
    """Validation results containing issues found."""

    issues: list[str]
    """A list of validation issues (errors) found. An empty list means the object is valid as an intersected geometry."""

class SyncGtmValidateGmmAsIntersectedGeometryResult(SyncTaskResult):
    """Validation results containing issues found."""

    issues: list[str]
    """A list of validation issues (errors) found. An empty list means the object is valid as an intersected geometry."""

class IsosurfacingCategoryCategorySourceInput(TypedDict):
    """Source grid and category attribute"""

    attribute: AttributeInput
    """Category attribute to generate surface from"""
    object: ObjectInput
    """Input grid containing the category attribute to generate surface from"""

class IsosurfacingCategoryTarget(TypedDict):
    """Where to create the output Triangle Mesh object"""

    description: NotRequired[str | None]
    """Description to put into the target object."""
    overwrite: NotRequired[bool]
    """Whether to overwrite the existing object."""
    reference: ObjectInput
    """Reference to the targeted object."""
    tags: NotRequired[dict[str, str]]
    """Tags to put into the target object."""

class IsosurfacingCategoryCategorySettings(TypedDict):
    """Optional settings for surface generation"""

    flip_triangles: NotRequired[bool]
    """Reverse triangle winding order (flip normals)"""

class IsosurfacingCategoryResultResultOutput(ResultNode):
    """Reference to the created Triangle Mesh object"""

    description: str | None
    """The description of the geoscience object."""
    name: str
    """The name of the geoscience object."""
    reference: str
    """Reference to a geoscience object."""
    schema_id: str
    """The ID of the Geoscience Object schema."""

class IsosurfacingCategoryResultCategorySummary(ResultNode):
    """Summary statistics for the generated surface"""

    has_non_manifold_edges: bool
    """Whether the mesh has non-manifold edges (quality check)"""
    has_unused_vertices: bool
    """Whether the mesh has orphan vertices (quality check)"""
    is_closed: bool
    """Whether the mesh forms a closed volume"""
    num_triangles: int
    """Number of triangles in the generated surface"""
    num_vertices: int
    """Number of unique vertices in the mesh"""

class IsosurfacingCategoryResult(TaskResult):
    """Results of the isosurfacing task."""

    output: IsosurfacingCategoryResultResultOutput
    """Reference to the created Triangle Mesh object"""
    summary: IsosurfacingCategoryResultCategorySummary
    """Summary statistics for the generated surface"""

class SyncIsosurfacingCategoryResultResultOutput(SyncResultNode):
    """Reference to the created Triangle Mesh object"""

    description: str | None
    """The description of the geoscience object."""
    name: str
    """The name of the geoscience object."""
    reference: str
    """Reference to a geoscience object."""
    schema_id: str
    """The ID of the Geoscience Object schema."""

class SyncIsosurfacingCategoryResultCategorySummary(SyncResultNode):
    """Summary statistics for the generated surface"""

    has_non_manifold_edges: bool
    """Whether the mesh has non-manifold edges (quality check)"""
    has_unused_vertices: bool
    """Whether the mesh has orphan vertices (quality check)"""
    is_closed: bool
    """Whether the mesh forms a closed volume"""
    num_triangles: int
    """Number of triangles in the generated surface"""
    num_vertices: int
    """Number of unique vertices in the mesh"""

class SyncIsosurfacingCategoryResult(SyncTaskResult):
    """Results of the isosurfacing task."""

    output: SyncIsosurfacingCategoryResultResultOutput
    """Reference to the created Triangle Mesh object"""
    summary: SyncIsosurfacingCategoryResultCategorySummary
    """Summary statistics for the generated surface"""

class IsosurfacingNumericNumericSourceInput(TypedDict):
    """Source grid and attribute"""

    attribute: AttributeInput
    """Numeric attribute to generate isosurface from"""
    object: ObjectInput
    """Input grid containing the numeric attribute to generate isosurface from"""

class IsosurfacingNumericTarget(TypedDict):
    """Where to create the output Triangle Mesh object"""

    description: NotRequired[str | None]
    """Description to put into the target object."""
    overwrite: NotRequired[bool]
    """Whether to overwrite the existing object."""
    reference: ObjectInput
    """Reference to the targeted object."""
    tags: NotRequired[dict[str, str]]
    """Tags to put into the target object."""

class IsosurfacingNumericNumericSettings(TypedDict):
    """Optional settings for isosurface generation"""

    cluster_vertices: NotRequired[bool]
    """Merge close vertices on intersected edges to improve surface quality (structured grids only)"""
    flip_triangles: NotRequired[bool]
    """Reverse triangle winding order (flip normals)"""

class IsosurfacingNumericResultResultOutput(ResultNode):
    """Reference to the created Triangle Mesh object"""

    description: str | None
    """The description of the geoscience object."""
    name: str
    """The name of the geoscience object."""
    reference: str
    """Reference to a geoscience object."""
    schema_id: str
    """The ID of the Geoscience Object schema."""

class IsosurfacingNumericResultNumericSummary(ResultNode):
    """Summary statistics for the generated isosurface"""

    has_non_manifold_edges: bool
    """Whether the mesh has non-manifold edges (quality check)"""
    has_unused_vertices: bool
    """Whether the mesh has orphan vertices (quality check)"""
    num_triangles: int
    """Number of triangles in the generated surface"""
    num_vertices: int
    """Number of unique vertices in the mesh"""
    threshold: float
    """The threshold value used"""

class IsosurfacingNumericResult(TaskResult):
    """Results of the isosurfacing-numeric task."""

    output: IsosurfacingNumericResultResultOutput
    """Reference to the created Triangle Mesh object"""
    summary: IsosurfacingNumericResultNumericSummary
    """Summary statistics for the generated isosurface"""

class SyncIsosurfacingNumericResultResultOutput(SyncResultNode):
    """Reference to the created Triangle Mesh object"""

    description: str | None
    """The description of the geoscience object."""
    name: str
    """The name of the geoscience object."""
    reference: str
    """Reference to a geoscience object."""
    schema_id: str
    """The ID of the Geoscience Object schema."""

class SyncIsosurfacingNumericResultNumericSummary(SyncResultNode):
    """Summary statistics for the generated isosurface"""

    has_non_manifold_edges: bool
    """Whether the mesh has non-manifold edges (quality check)"""
    has_unused_vertices: bool
    """Whether the mesh has orphan vertices (quality check)"""
    num_triangles: int
    """Number of triangles in the generated surface"""
    num_vertices: int
    """Number of unique vertices in the mesh"""
    threshold: float
    """The threshold value used"""

class SyncIsosurfacingNumericResult(SyncTaskResult):
    """Results of the isosurfacing-numeric task."""

    output: SyncIsosurfacingNumericResultResultOutput
    """Reference to the created Triangle Mesh object"""
    summary: SyncIsosurfacingNumericResultNumericSummary
    """Summary statistics for the generated isosurface"""

class UtilitiesCompressResultArchive(ResultNode):
    """Reference to the uploaded archive, which can be downloaded via the File API."""

    name: str
    """The name of the file, typically the terminal segment of the path."""
    reference: str
    """Reference to a file."""

class UtilitiesCompressResult(TaskResult):
    """Results for the compress compute task."""

    output: UtilitiesCompressResultArchive
    """Reference to the uploaded archive, which can be downloaded via the File API."""

class SyncUtilitiesCompressResultArchive(SyncResultNode):
    """Reference to the uploaded archive, which can be downloaded via the File API."""

    name: str
    """The name of the file, typically the terminal segment of the path."""
    reference: str
    """Reference to a file."""

class SyncUtilitiesCompressResult(SyncTaskResult):
    """Results for the compress compute task."""

    output: SyncUtilitiesCompressResultArchive
    """Reference to the uploaded archive, which can be downloaded via the File API."""

class VisServiceTilingResult(TaskResult):
    """Output results from the visualization tiling task"""

    message: str
    """Human-readable result message"""
    status: str
    """Task outcome: success, failed, or error"""

class SyncVisServiceTilingResult(SyncTaskResult):
    """Output results from the visualization tiling task"""

    message: str
    """Human-readable result message"""
    status: str
    """Task outcome: success, failed, or error"""

class _ConverterDetectConverter:
    """Accepts one or more file references, inspects each file's extension, and returns which converter task(s) and geoscience object schema(s) are available for importing each file."""

    async def run(
        self,
        *,
        files: list[FileInput],
        preview: bool = True,
    ) -> ConverterDetectConverterResult:
        """Accepts one or more file references, inspects each file's extension, and returns which converter task(s) and geoscience object schema(s) are available for importing each file."""
        ...

class _SyncConverterDetectConverter:
    """Accepts one or more file references, inspects each file's extension, and returns which converter task(s) and geoscience object schema(s) are available for importing each file."""

    def run(
        self,
        *,
        files: list[FileInput],
        preview: bool = True,
    ) -> SyncConverterDetectConverterResult:
        """Accepts one or more file references, inspects each file's extension, and returns which converter task(s) and geoscience object schema(s) are available for importing each file."""
        ...

class _ConverterDgnImport:
    """Import DGN files into Evo as line segments, with fallback to triangle mesh or pointset."""

    async def run(
        self,
        *,
        file: FileInput,
        target: ConverterDgnImportTarget,
        epsg_code: int | None = ...,
        include_fills: bool = ...,
        include_text_anchors: bool = ...,
        preview: bool = True,
    ) -> ConverterDgnImportResult:
        """Import DGN files into Evo as line segments, with fallback to triangle mesh or pointset."""
        ...

class _SyncConverterDgnImport:
    """Import DGN files into Evo as line segments, with fallback to triangle mesh or pointset."""

    def run(
        self,
        *,
        file: FileInput,
        target: ConverterDgnImportTarget,
        epsg_code: int | None = ...,
        include_fills: bool = ...,
        include_text_anchors: bool = ...,
        preview: bool = True,
    ) -> SyncConverterDgnImportResult:
        """Import DGN files into Evo as line segments, with fallback to triangle mesh or pointset."""
        ...

class _ConverterErsImport:
    """Import ER Mapper Raster (.ers) files into Evo as Regular-2D geoscience objects."""

    async def run(
        self,
        *,
        file: FileInput,
        file_data: FileInput,
        target: ConverterErsImportTarget,
        preview: bool = True,
    ) -> ConverterErsImportResult:
        """Import ER Mapper Raster (.ers) files into Evo as Regular-2D geoscience objects."""
        ...

class _SyncConverterErsImport:
    """Import ER Mapper Raster (.ers) files into Evo as Regular-2D geoscience objects."""

    def run(
        self,
        *,
        file: FileInput,
        file_data: FileInput,
        target: ConverterErsImportTarget,
        preview: bool = True,
    ) -> SyncConverterErsImportResult:
        """Import ER Mapper Raster (.ers) files into Evo as Regular-2D geoscience objects."""
        ...

class _ConverterGeosoftGridImport:
    """Import Geosoft Grid (.grd/.grd.gi) files into Evo as Regular-2D geoscience objects."""

    async def run(
        self,
        *,
        file: FileInput,
        target: ConverterGeosoftGridImportTarget,
        file_gi: FileInput | None = ...,
        preview: bool = True,
    ) -> ConverterGeosoftGridImportResult:
        """Import Geosoft Grid (.grd/.grd.gi) files into Evo as Regular-2D geoscience objects."""
        ...

class _SyncConverterGeosoftGridImport:
    """Import Geosoft Grid (.grd/.grd.gi) files into Evo as Regular-2D geoscience objects."""

    def run(
        self,
        *,
        file: FileInput,
        target: ConverterGeosoftGridImportTarget,
        file_gi: FileInput | None = ...,
        preview: bool = True,
    ) -> SyncConverterGeosoftGridImportResult:
        """Import Geosoft Grid (.grd/.grd.gi) files into Evo as Regular-2D geoscience objects."""
        ...

class _ConverterGeosoftSurfaceImport:
    """Import Geosoft Surface (.geosoft_surface) files into Evo as GeologicalModelMeshes geoscience objects."""

    async def run(
        self,
        *,
        file: FileInput,
        target: ConverterGeosoftSurfaceImportTarget,
        preview: bool = True,
    ) -> ConverterGeosoftSurfaceImportResult:
        """Import Geosoft Surface (.geosoft_surface) files into Evo as GeologicalModelMeshes geoscience objects."""
        ...

class _SyncConverterGeosoftSurfaceImport:
    """Import Geosoft Surface (.geosoft_surface) files into Evo as GeologicalModelMeshes geoscience objects."""

    def run(
        self,
        *,
        file: FileInput,
        target: ConverterGeosoftSurfaceImportTarget,
        preview: bool = True,
    ) -> SyncConverterGeosoftSurfaceImportResult:
        """Import Geosoft Surface (.geosoft_surface) files into Evo as GeologicalModelMeshes geoscience objects."""
        ...

class _ConverterGeosoftVoxelImport:
    """Import Geosoft Voxel (.geosoft_voxel) files into Evo as either Regular-3D or Tensor-3D geoscience objects."""

    async def run(
        self,
        *,
        file: FileInput,
        target: ConverterGeosoftVoxelImportTarget,
        preview: bool = True,
    ) -> ConverterGeosoftVoxelImportResult:
        """Import Geosoft Voxel (.geosoft_voxel) files into Evo as either Regular-3D or Tensor-3D geoscience objects."""
        ...

class _SyncConverterGeosoftVoxelImport:
    """Import Geosoft Voxel (.geosoft_voxel) files into Evo as either Regular-3D or Tensor-3D geoscience objects."""

    def run(
        self,
        *,
        file: FileInput,
        target: ConverterGeosoftVoxelImportTarget,
        preview: bool = True,
    ) -> SyncConverterGeosoftVoxelImportResult:
        """Import Geosoft Voxel (.geosoft_voxel) files into Evo as either Regular-3D or Tensor-3D geoscience objects."""
        ...

class _ConverterImageImport:
    """Import image files into Evo as Regular-2D geoscience objects."""

    async def run(
        self,
        *,
        file: FileInput,
        target: ConverterImageImportTarget,
        cell_size: list[float] | None = ...,
        coordinate_reference_system: str = ...,
        origin: list[float] | None = ...,
        preview: bool = True,
    ) -> ConverterImageImportResult:
        """Import image files into Evo as Regular-2D geoscience objects."""
        ...

class _SyncConverterImageImport:
    """Import image files into Evo as Regular-2D geoscience objects."""

    def run(
        self,
        *,
        file: FileInput,
        target: ConverterImageImportTarget,
        cell_size: list[float] | None = ...,
        coordinate_reference_system: str = ...,
        origin: list[float] | None = ...,
        preview: bool = True,
    ) -> SyncConverterImageImportResult:
        """Import image files into Evo as Regular-2D geoscience objects."""
        ...

class _ConverterObjImport:
    """Import OBJ (Wavefront) files into Evo as Triangle Mesh geoscience objects. Supports triangles, quads (auto-triangulated), and multiple objects within a single file."""

    async def run(
        self,
        *,
        file: FileInput,
        target: ConverterObjImportTarget,
        coordinate_reference_system: str | None = ...,
        preview: bool = True,
    ) -> ConverterObjImportResult:
        """Import OBJ (Wavefront) files into Evo as Triangle Mesh geoscience objects. Supports triangles, quads (auto-triangulated), and multiple objects within a single file."""
        ...

class _SyncConverterObjImport:
    """Import OBJ (Wavefront) files into Evo as Triangle Mesh geoscience objects. Supports triangles, quads (auto-triangulated), and multiple objects within a single file."""

    def run(
        self,
        *,
        file: FileInput,
        target: ConverterObjImportTarget,
        coordinate_reference_system: str | None = ...,
        preview: bool = True,
    ) -> SyncConverterObjImportResult:
        """Import OBJ (Wavefront) files into Evo as Triangle Mesh geoscience objects. Supports triangles, quads (auto-triangulated), and multiple objects within a single file."""
        ...

class _ConverterShpImport:
    """Task to import SHP files into Evo as Triangle Mesh geoscience objects."""

    async def run(
        self,
        *,
        file: FileInput,
        file_shx: FileInput,
        file_dbf: FileInput,
        target: ConverterShpImportTarget,
        file_cpg: FileInput | None = ...,
        file_prj: FileInput | None = ...,
        file_sbn: FileInput | None = ...,
        file_sbx: FileInput | None = ...,
        file_xml: FileInput | None = ...,
        preview: bool = True,
    ) -> ConverterShpImportResult:
        """Task to import SHP files into Evo as Triangle Mesh geoscience objects."""
        ...

class _SyncConverterShpImport:
    """Task to import SHP files into Evo as Triangle Mesh geoscience objects."""

    def run(
        self,
        *,
        file: FileInput,
        file_shx: FileInput,
        file_dbf: FileInput,
        target: ConverterShpImportTarget,
        file_cpg: FileInput | None = ...,
        file_prj: FileInput | None = ...,
        file_sbn: FileInput | None = ...,
        file_sbx: FileInput | None = ...,
        file_xml: FileInput | None = ...,
        preview: bool = True,
    ) -> SyncConverterShpImportResult:
        """Task to import SHP files into Evo as Triangle Mesh geoscience objects."""
        ...

class _ConverterUbcImport:
    """Task to import UBC Grids into Evo as Tensor-3D geoscience objects."""

    async def run(
        self,
        *,
        file: FileInput,
        file_mod: FileInput,
        target: ConverterUbcImportTarget,
        coordinate_reference_system: str = ...,
        preview: bool = True,
    ) -> ConverterUbcImportResult:
        """Task to import UBC Grids into Evo as Tensor-3D geoscience objects."""
        ...

class _SyncConverterUbcImport:
    """Task to import UBC Grids into Evo as Tensor-3D geoscience objects."""

    def run(
        self,
        *,
        file: FileInput,
        file_mod: FileInput,
        target: ConverterUbcImportTarget,
        coordinate_reference_system: str = ...,
        preview: bool = True,
    ) -> SyncConverterUbcImportResult:
        """Task to import UBC Grids into Evo as Tensor-3D geoscience objects."""
        ...

class _ConverterVtkImport:
    """VTK Import compute task"""

    async def run(
        self,
        *,
        files: list[FileInput],
        preview: bool = True,
    ) -> ConverterVtkImportResult:
        """VTK Import compute task"""
        ...

class _SyncConverterVtkImport:
    """VTK Import compute task"""

    def run(
        self,
        *,
        files: list[FileInput],
        preview: bool = True,
    ) -> SyncConverterVtkImportResult:
        """VTK Import compute task"""
        ...

class _ConverterXyzImport:
    """Task to import XYZ files into Evo as Pointset geoscience objects."""

    async def run(
        self,
        *,
        file: FileInput,
        target: ConverterXyzImportTarget,
        epsg_code: int | None = ...,
        x_index: int | None = ...,
        y_index: int | None = ...,
        z_index: int | None = ...,
        preview: bool = True,
    ) -> ConverterXyzImportResult:
        """Task to import XYZ files into Evo as Pointset geoscience objects."""
        ...

class _SyncConverterXyzImport:
    """Task to import XYZ files into Evo as Pointset geoscience objects."""

    def run(
        self,
        *,
        file: FileInput,
        target: ConverterXyzImportTarget,
        epsg_code: int | None = ...,
        x_index: int | None = ...,
        y_index: int | None = ...,
        z_index: int | None = ...,
        preview: bool = True,
    ) -> SyncConverterXyzImportResult:
        """Task to import XYZ files into Evo as Pointset geoscience objects."""
        ...

class _CptToBoreholeCptReader:
    """CPT reader task"""

    async def run(
        self,
        *,
        cpt_file: FileInput,
        target_downhole_collection: CptToBoreholeCptReaderTargetDownholeCollection,
        preview: bool = True,
    ) -> CptToBoreholeCptReaderResult:
        """CPT reader task"""
        ...

class _SyncCptToBoreholeCptReader:
    """CPT reader task"""

    def run(
        self,
        *,
        cpt_file: FileInput,
        target_downhole_collection: CptToBoreholeCptReaderTargetDownholeCollection,
        preview: bool = True,
    ) -> SyncCptToBoreholeCptReaderResult:
        """CPT reader task"""
        ...

class _CptToBoreholeCreateClusters:
    """Create clusters of related soil regions based on the stratification performed by the stratify task."""

    async def run(
        self,
        *,
        downhole_collection: ObjectInput,
        features_option: Literal["qtAndRf", "QtnAndFr"] = ...,
        hole_ids: list[str] | None = ...,
        number_of_clusters: int | None = ...,
        uncluster: bool = ...,
        preview: bool = True,
    ) -> CptToBoreholeCreateClustersResult:
        """Create clusters of related soil regions based on the stratification performed by the stratify task."""
        ...

class _SyncCptToBoreholeCreateClusters:
    """Create clusters of related soil regions based on the stratification performed by the stratify task."""

    def run(
        self,
        *,
        downhole_collection: ObjectInput,
        features_option: Literal["qtAndRf", "QtnAndFr"] = ...,
        hole_ids: list[str] | None = ...,
        number_of_clusters: int | None = ...,
        uncluster: bool = ...,
        preview: bool = True,
    ) -> SyncCptToBoreholeCreateClustersResult:
        """Create clusters of related soil regions based on the stratification performed by the stratify task."""
        ...

class _CptToBoreholePublish:
    """Publish a finalized downhole collection as a new geoscience object for downstream consumption."""

    async def run(
        self,
        *,
        downhole_collection: ObjectInput,
        target_downhole_collection: CptToBoreholePublishTargetDownholeCollection,
        include_clusters: bool,
        cluster_metadata: list[CptToBoreholePublishClusterMetadata] | None = ...,
        exclude_collections: list[Literal["measured_cpt_readings", "derived_cpt_readings", "geology_layers"]] = ...,
        preview: bool = True,
    ) -> CptToBoreholePublishResult:
        """Publish a finalized downhole collection as a new geoscience object for downstream consumption."""
        ...

class _SyncCptToBoreholePublish:
    """Publish a finalized downhole collection as a new geoscience object for downstream consumption."""

    def run(
        self,
        *,
        downhole_collection: ObjectInput,
        target_downhole_collection: CptToBoreholePublishTargetDownholeCollection,
        include_clusters: bool,
        cluster_metadata: list[CptToBoreholePublishClusterMetadata] | None = ...,
        exclude_collections: list[Literal["measured_cpt_readings", "derived_cpt_readings", "geology_layers"]] = ...,
        preview: bool = True,
    ) -> SyncCptToBoreholePublishResult:
        """Publish a finalized downhole collection as a new geoscience object for downstream consumption."""
        ...

class _CptToBoreholeStratify:
    """Classifies and stratifies CPT data into geological layers based on predefined criteria and algorithms, facilitating geotechnical analysis and modeling."""

    async def run(
        self,
        *,
        input_downhole_collections: list[ObjectInput],
        target_downhole_collection: CptToBoreholeStratifyTargetDownholeCollection,
        cpt_parameters: list[CptToBoreholeStratifyPerHoleParameters] | None = ...,
        execution_mode: Literal["inline", "fan_out", "auto"] = ...,
        preview: bool = True,
    ) -> CptToBoreholeStratifyResult:
        """Classifies and stratifies CPT data into geological layers based on predefined criteria and algorithms, facilitating geotechnical analysis and modeling."""
        ...

class _SyncCptToBoreholeStratify:
    """Classifies and stratifies CPT data into geological layers based on predefined criteria and algorithms, facilitating geotechnical analysis and modeling."""

    def run(
        self,
        *,
        input_downhole_collections: list[ObjectInput],
        target_downhole_collection: CptToBoreholeStratifyTargetDownholeCollection,
        cpt_parameters: list[CptToBoreholeStratifyPerHoleParameters] | None = ...,
        execution_mode: Literal["inline", "fan_out", "auto"] = ...,
        preview: bool = True,
    ) -> SyncCptToBoreholeStratifyResult:
        """Classifies and stratifies CPT data into geological layers based on predefined criteria and algorithms, facilitating geotechnical analysis and modeling."""
        ...

class _DriverDriverGeneralCompute:
    """Driver general compute task"""

    async def run(
        self,
        *,
        worker_message_body_json: str,
        _backend_url: str = ...,
        preview: bool = True,
    ) -> DriverDriverGeneralComputeResult:
        """Driver general compute task"""
        ...

class _SyncDriverDriverGeneralCompute:
    """Driver general compute task"""

    def run(
        self,
        *,
        worker_message_body_json: str,
        _backend_url: str = ...,
        preview: bool = True,
    ) -> SyncDriverDriverGeneralComputeResult:
        """Driver general compute task"""
        ...

class _GeophysicsGravityTerrainCorrection:
    """Gravity terrain correction compute task for geophysical surveys"""

    async def run(
        self,
        *,
        surfaces: ObjectInput,
        observation_points: ObjectInput,
        correction_radius: float,
        terrain_layer: GeophysicsGravityTerrainCorrectionTerrainLayer,
        water_layer: GeophysicsGravityTerrainCorrectionWaterLayer | None = ...,
        preview: bool = True,
    ) -> GeophysicsGravityTerrainCorrectionResult:
        """Gravity terrain correction compute task for geophysical surveys"""
        ...

class _SyncGeophysicsGravityTerrainCorrection:
    """Gravity terrain correction compute task for geophysical surveys"""

    def run(
        self,
        *,
        surfaces: ObjectInput,
        observation_points: ObjectInput,
        correction_radius: float,
        terrain_layer: GeophysicsGravityTerrainCorrectionTerrainLayer,
        water_layer: GeophysicsGravityTerrainCorrectionWaterLayer | None = ...,
        preview: bool = True,
    ) -> SyncGeophysicsGravityTerrainCorrectionResult:
        """Gravity terrain correction compute task for geophysical surveys"""
        ...

class _GeophysicsRegular2dGridToTrimesh:
    """Convert a regular 2D grid to a triangle mesh using a cell-centered or vertex-centered scalar attribute as Z values. Supports optional NaN filtering."""

    async def run(
        self,
        *,
        source: GeophysicsRegular2dGridToTrimeshSourceInput | AnyTypedAttribute,
        target: GeophysicsRegular2dGridToTrimeshTarget,
        settings: GeophysicsRegular2dGridToTrimeshSettings = ...,
        preview: bool = True,
    ) -> GeophysicsRegular2dGridToTrimeshResult:
        """Convert a regular 2D grid to a triangle mesh using a cell-centered or vertex-centered scalar attribute as Z values. Supports optional NaN filtering."""
        ...

class _SyncGeophysicsRegular2dGridToTrimesh:
    """Convert a regular 2D grid to a triangle mesh using a cell-centered or vertex-centered scalar attribute as Z values. Supports optional NaN filtering."""

    def run(
        self,
        *,
        source: GeophysicsRegular2dGridToTrimeshSourceInput | AnyTypedAttribute,
        target: GeophysicsRegular2dGridToTrimeshTarget,
        settings: GeophysicsRegular2dGridToTrimeshSettings = ...,
        preview: bool = True,
    ) -> SyncGeophysicsRegular2dGridToTrimeshResult:
        """Convert a regular 2D grid to a triangle mesh using a cell-centered or vertex-centered scalar attribute as Z values. Supports optional NaN filtering."""
        ...

class _GeostatisticsBreakTies:
    """For more information, please read the <a href='/docs/guides/geostatistics-tasks/tasks/break-ties'>guide</a>"""

    async def run(
        self,
        *,
        source: GeostatisticsBreakTiesSource | AnyTypedAttribute,
        target: GeostatisticsBreakTiesTarget | AnyTypedAttribute,
        neighborhood: GeostatisticsBreakTiesBreakTiesSearch,
        seed: int = ...,
        preview: bool = True,
    ) -> GeostatisticsBreakTiesResult:
        """For more information, please read the <a href='/docs/guides/geostatistics-tasks/tasks/break-ties'>guide</a>"""
        ...

class _SyncGeostatisticsBreakTies:
    """For more information, please read the <a href='/docs/guides/geostatistics-tasks/tasks/break-ties'>guide</a>"""

    def run(
        self,
        *,
        source: GeostatisticsBreakTiesSource | AnyTypedAttribute,
        target: GeostatisticsBreakTiesTarget | AnyTypedAttribute,
        neighborhood: GeostatisticsBreakTiesBreakTiesSearch,
        seed: int = ...,
        preview: bool = True,
    ) -> SyncGeostatisticsBreakTiesResult:
        """For more information, please read the <a href='/docs/guides/geostatistics-tasks/tasks/break-ties'>guide</a>"""
        ...

class _GeostatisticsConditionalTurningBands:
    """Runs the turning band simulator using an existing continuous distribution. For more information, please read the <a href='/docs/guides/geostatistics-tasks/tasks/conditional-turning-bands'>guide</a>"""

    async def run(
        self,
        *,
        source: ObjectInput,
        source_attribute: AttributeInput,
        target: ObjectInput,
        neighborhood: GeostatisticsConditionalTurningBandsNeighborhood,
        distribution: ObjectInput,
        variogram_model: ObjectInput,
        block_discretization: GeostatisticsConditionalTurningBandsBlockDiscretization,
        filter: GeostatisticsConditionalTurningBandsFilter | None = ...,
        kriging_method: Literal["simple", "ordinary"] = ...,
        number_of_lines: int = ...,
        random_seed: int = ...,
        realizations: int = ...,
        source_filter: GeostatisticsConditionalTurningBandsSourceFilter | None = ...,
        preview: bool = True,
    ) -> GeostatisticsConditionalTurningBandsResult:
        """Runs the turning band simulator using an existing continuous distribution. For more information, please read the <a href='/docs/guides/geostatistics-tasks/tasks/conditional-turning-bands'>guide</a>"""
        ...

class _SyncGeostatisticsConditionalTurningBands:
    """Runs the turning band simulator using an existing continuous distribution. For more information, please read the <a href='/docs/guides/geostatistics-tasks/tasks/conditional-turning-bands'>guide</a>"""

    def run(
        self,
        *,
        source: ObjectInput,
        source_attribute: AttributeInput,
        target: ObjectInput,
        neighborhood: GeostatisticsConditionalTurningBandsNeighborhood,
        distribution: ObjectInput,
        variogram_model: ObjectInput,
        block_discretization: GeostatisticsConditionalTurningBandsBlockDiscretization,
        filter: GeostatisticsConditionalTurningBandsFilter | None = ...,
        kriging_method: Literal["simple", "ordinary"] = ...,
        number_of_lines: int = ...,
        random_seed: int = ...,
        realizations: int = ...,
        source_filter: GeostatisticsConditionalTurningBandsSourceFilter | None = ...,
        preview: bool = True,
    ) -> SyncGeostatisticsConditionalTurningBandsResult:
        """Runs the turning band simulator using an existing continuous distribution. For more information, please read the <a href='/docs/guides/geostatistics-tasks/tasks/conditional-turning-bands'>guide</a>"""
        ...

class _GeostatisticsConsim:
    """For more information, please read the <a href='/docs/guides/geostatistics-tasks/tasks/conditional-simulation-workflow'>guide</a>"""

    async def run(
        self,
        *,
        source_object: ObjectInput,
        source_attribute: AttributeInput,
        target_object: ObjectInput,
        neighborhood: GeostatisticsConsimNeighborhood,
        variogram_model: ObjectInput,
        block_discretization: GeostatisticsConsimBlockDiscretization,
        distribution: GeostatisticsConsimContinuousDistributionParameters | None = ...,
        filter: GeostatisticsConsimFilter | None = ...,
        kriging_method: Literal["simple", "ordinary"] = ...,
        location_wise_quantiles: list[float] = ...,
        loss_calculation: GeostatisticsConsimLossCalculation | None = ...,
        mean_above_cutoff: list[float] | None = ...,
        number_of_lines: int = ...,
        number_of_simulations: int = ...,
        number_of_simulations_to_save: int = ...,
        perform_validation: bool = ...,
        probability_above_cutoff: list[float] | None = ...,
        random_seed: int = ...,
        report_context: GeostatisticsConsimReportContext | None = ...,
        report_mean_thresholds: GeostatisticsConsimReportMeanThresholds | None = ...,
        source_filter: GeostatisticsConsimSourceFilter | None = ...,
        preview: bool = False,
    ) -> GeostatisticsConsimResult:
        """For more information, please read the <a href='/docs/guides/geostatistics-tasks/tasks/conditional-simulation-workflow'>guide</a>"""
        ...

class _SyncGeostatisticsConsim:
    """For more information, please read the <a href='/docs/guides/geostatistics-tasks/tasks/conditional-simulation-workflow'>guide</a>"""

    def run(
        self,
        *,
        source_object: ObjectInput,
        source_attribute: AttributeInput,
        target_object: ObjectInput,
        neighborhood: GeostatisticsConsimNeighborhood,
        variogram_model: ObjectInput,
        block_discretization: GeostatisticsConsimBlockDiscretization,
        distribution: GeostatisticsConsimContinuousDistributionParameters | None = ...,
        filter: GeostatisticsConsimFilter | None = ...,
        kriging_method: Literal["simple", "ordinary"] = ...,
        location_wise_quantiles: list[float] = ...,
        loss_calculation: GeostatisticsConsimLossCalculation | None = ...,
        mean_above_cutoff: list[float] | None = ...,
        number_of_lines: int = ...,
        number_of_simulations: int = ...,
        number_of_simulations_to_save: int = ...,
        perform_validation: bool = ...,
        probability_above_cutoff: list[float] | None = ...,
        random_seed: int = ...,
        report_context: GeostatisticsConsimReportContext | None = ...,
        report_mean_thresholds: GeostatisticsConsimReportMeanThresholds | None = ...,
        source_filter: GeostatisticsConsimSourceFilter | None = ...,
        preview: bool = False,
    ) -> SyncGeostatisticsConsimResult:
        """For more information, please read the <a href='/docs/guides/geostatistics-tasks/tasks/conditional-simulation-workflow'>guide</a>"""
        ...

class _GeostatisticsContinuousDistribution:
    """For more information, please read the <a href='/docs/guides/geostatistics-tasks/tasks/continuous-distribution'>guide</a>"""

    async def run(
        self,
        *,
        source: GeostatisticsContinuousDistributionSource | AnyTypedAttribute,
        target: GeostatisticsContinuousDistributionTarget,
        tail_extrapolation: GeostatisticsContinuousDistributionTailExtrapolation | None = ...,
        weights: GeostatisticsContinuousDistributionWeights | None | AnyTypedAttribute = ...,
        preview: bool = True,
    ) -> GeostatisticsContinuousDistributionResult:
        """For more information, please read the <a href='/docs/guides/geostatistics-tasks/tasks/continuous-distribution'>guide</a>"""
        ...

class _SyncGeostatisticsContinuousDistribution:
    """For more information, please read the <a href='/docs/guides/geostatistics-tasks/tasks/continuous-distribution'>guide</a>"""

    def run(
        self,
        *,
        source: GeostatisticsContinuousDistributionSource | AnyTypedAttribute,
        target: GeostatisticsContinuousDistributionTarget,
        tail_extrapolation: GeostatisticsContinuousDistributionTailExtrapolation | None = ...,
        weights: GeostatisticsContinuousDistributionWeights | None | AnyTypedAttribute = ...,
        preview: bool = True,
    ) -> SyncGeostatisticsContinuousDistributionResult:
        """For more information, please read the <a href='/docs/guides/geostatistics-tasks/tasks/continuous-distribution'>guide</a>"""
        ...

class _GeostatisticsDeclustering:
    """Computes grid-based declustering weights by measuring each sample's influence on evaluation locations. Supports both KNN (arithmetic mean) and IDW (inverse-distance weighted) modes via an optional power parameter."""

    async def run(
        self,
        *,
        source: GeostatisticsDeclusteringSource | ObjectInput,
        grid: GeostatisticsDeclusteringGrid | ObjectInput,
        target: GeostatisticsDeclusteringTarget | AnyTypedAttribute,
        neighborhood: GeostatisticsDeclusteringExtendedNeighborhood,
        power: float | None = ...,
        preview: bool = True,
    ) -> GeostatisticsDeclusteringResult:
        """Computes grid-based declustering weights by measuring each sample's influence on evaluation locations. Supports both KNN (arithmetic mean) and IDW (inverse-distance weighted) modes via an optional power parameter."""
        ...

class _SyncGeostatisticsDeclustering:
    """Computes grid-based declustering weights by measuring each sample's influence on evaluation locations. Supports both KNN (arithmetic mean) and IDW (inverse-distance weighted) modes via an optional power parameter."""

    def run(
        self,
        *,
        source: GeostatisticsDeclusteringSource | ObjectInput,
        grid: GeostatisticsDeclusteringGrid | ObjectInput,
        target: GeostatisticsDeclusteringTarget | AnyTypedAttribute,
        neighborhood: GeostatisticsDeclusteringExtendedNeighborhood,
        power: float | None = ...,
        preview: bool = True,
    ) -> SyncGeostatisticsDeclusteringResult:
        """Computes grid-based declustering weights by measuring each sample's influence on evaluation locations. Supports both KNN (arithmetic mean) and IDW (inverse-distance weighted) modes via an optional power parameter."""
        ...

class _GeostatisticsExperimentalVariogram:
    """Computes one or more experimental (sample) variograms from a points or downhole intervals source object and writes them to a new experimental variogram object. A single call can mix omnidirectional, directional (azimuth/dip tolerance cone with optional bandwidth/bandheight) and downhole variograms, each with its own lag definition."""

    async def run(
        self,
        *,
        source: GeostatisticsExperimentalVariogramSource | AnyTypedAttribute,
        directions: list[
            GeostatisticsExperimentalVariogramOmnidirectionalVariogram
            | GeostatisticsExperimentalVariogramDirectionalVariogram
            | GeostatisticsExperimentalVariogramDownholeVariogram
        ],
        target: GeostatisticsExperimentalVariogramTarget,
        preview: bool = True,
    ) -> GeostatisticsExperimentalVariogramResult:
        """Computes one or more experimental (sample) variograms from a points or downhole intervals source object and writes them to a new experimental variogram object. A single call can mix omnidirectional, directional (azimuth/dip tolerance cone with optional bandwidth/bandheight) and downhole variograms, each with its own lag definition."""
        ...

class _SyncGeostatisticsExperimentalVariogram:
    """Computes one or more experimental (sample) variograms from a points or downhole intervals source object and writes them to a new experimental variogram object. A single call can mix omnidirectional, directional (azimuth/dip tolerance cone with optional bandwidth/bandheight) and downhole variograms, each with its own lag definition."""

    def run(
        self,
        *,
        source: GeostatisticsExperimentalVariogramSource | AnyTypedAttribute,
        directions: list[
            GeostatisticsExperimentalVariogramOmnidirectionalVariogram
            | GeostatisticsExperimentalVariogramDirectionalVariogram
            | GeostatisticsExperimentalVariogramDownholeVariogram
        ],
        target: GeostatisticsExperimentalVariogramTarget,
        preview: bool = True,
    ) -> SyncGeostatisticsExperimentalVariogramResult:
        """Computes one or more experimental (sample) variograms from a points or downhole intervals source object and writes them to a new experimental variogram object. A single call can mix omnidirectional, directional (azimuth/dip tolerance cone with optional bandwidth/bandheight) and downhole variograms, each with its own lag definition."""
        ...

class _GeostatisticsIdw:
    """Inverse distance weighting estimation using weighted average of nearby sample values, where weights are inversely proportional to distance raised to a specified power."""

    async def run(
        self,
        *,
        source: GeostatisticsIdwSource | AnyTypedAttribute,
        target: GeostatisticsIdwTarget | AnyTypedAttribute,
        neighborhood: GeostatisticsIdwExtendedNeighborhood,
        power: float,
        preview: bool = True,
    ) -> GeostatisticsIdwResult:
        """Inverse distance weighting estimation using weighted average of nearby sample values, where weights are inversely proportional to distance raised to a specified power."""
        ...

class _SyncGeostatisticsIdw:
    """Inverse distance weighting estimation using weighted average of nearby sample values, where weights are inversely proportional to distance raised to a specified power."""

    def run(
        self,
        *,
        source: GeostatisticsIdwSource | AnyTypedAttribute,
        target: GeostatisticsIdwTarget | AnyTypedAttribute,
        neighborhood: GeostatisticsIdwExtendedNeighborhood,
        power: float,
        preview: bool = True,
    ) -> SyncGeostatisticsIdwResult:
        """Inverse distance weighting estimation using weighted average of nearby sample values, where weights are inversely proportional to distance raised to a specified power."""
        ...

class _GeostatisticsKnn:
    """Nearest neighbor estimation using arithmetic mean of nearby sample values."""

    async def run(
        self,
        *,
        source: GeostatisticsKnnSource | AnyTypedAttribute,
        target: GeostatisticsKnnTarget | AnyTypedAttribute,
        neighborhood: GeostatisticsKnnExtendedNeighborhood,
        preview: bool = True,
    ) -> GeostatisticsKnnResult:
        """Nearest neighbor estimation using arithmetic mean of nearby sample values."""
        ...

class _SyncGeostatisticsKnn:
    """Nearest neighbor estimation using arithmetic mean of nearby sample values."""

    def run(
        self,
        *,
        source: GeostatisticsKnnSource | AnyTypedAttribute,
        target: GeostatisticsKnnTarget | AnyTypedAttribute,
        neighborhood: GeostatisticsKnnExtendedNeighborhood,
        preview: bool = True,
    ) -> SyncGeostatisticsKnnResult:
        """Nearest neighbor estimation using arithmetic mean of nearby sample values."""
        ...

class _GeostatisticsLocationWise:
    """For more information, please read the <a href='/docs/guides/geostatistics-tasks/tasks/location-wise'>guide</a>"""

    async def run(
        self,
        *,
        source: GeostatisticsLocationWiseSource | AnyTypedAttribute,
        target: GeostatisticsLocationWiseTarget | ObjectInput,
        mean_above_cutoff: GeostatisticsLocationWiseMeanAboveCutoff | None = ...,
        probability_above_cutoff: GeostatisticsLocationWiseProbabilityAboveCutoff | None = ...,
        quantiles: list[float] | None = ...,
        summary: bool | None = ...,
        preview: bool = True,
    ) -> GeostatisticsLocationWiseResult:
        """For more information, please read the <a href='/docs/guides/geostatistics-tasks/tasks/location-wise'>guide</a>"""
        ...

class _SyncGeostatisticsLocationWise:
    """For more information, please read the <a href='/docs/guides/geostatistics-tasks/tasks/location-wise'>guide</a>"""

    def run(
        self,
        *,
        source: GeostatisticsLocationWiseSource | AnyTypedAttribute,
        target: GeostatisticsLocationWiseTarget | ObjectInput,
        mean_above_cutoff: GeostatisticsLocationWiseMeanAboveCutoff | None = ...,
        probability_above_cutoff: GeostatisticsLocationWiseProbabilityAboveCutoff | None = ...,
        quantiles: list[float] | None = ...,
        summary: bool | None = ...,
        preview: bool = True,
    ) -> SyncGeostatisticsLocationWiseResult:
        """For more information, please read the <a href='/docs/guides/geostatistics-tasks/tasks/location-wise'>guide</a>"""
        ...

class _GeostatisticsLossCalculation:
    """Calculate loss given block model and economic parameters"""

    async def run(
        self,
        *,
        source: GeostatisticsLossCalculationSource | AnyTypedAttribute,
        target: GeostatisticsLossCalculationTarget | AnyTypedAttribute,
        material_categories: list[GeostatisticsLossCalculationMaterialCategories],
        processing_cost: float,
        mining_waste_cost: float,
        mining_ore_cost: float,
        metal_recovery_fraction: float,
        preview: bool = True,
    ) -> GeostatisticsLossCalculationResult:
        """Calculate loss given block model and economic parameters"""
        ...

class _SyncGeostatisticsLossCalculation:
    """Calculate loss given block model and economic parameters"""

    def run(
        self,
        *,
        source: GeostatisticsLossCalculationSource | AnyTypedAttribute,
        target: GeostatisticsLossCalculationTarget | AnyTypedAttribute,
        material_categories: list[GeostatisticsLossCalculationMaterialCategories],
        processing_cost: float,
        mining_waste_cost: float,
        mining_ore_cost: float,
        metal_recovery_fraction: float,
        preview: bool = True,
    ) -> SyncGeostatisticsLossCalculationResult:
        """Calculate loss given block model and economic parameters"""
        ...

class _GeostatisticsNormalScore:
    """For more information, please read the <a href='/docs/guides/geostatistics-tasks/tasks/normal-score'>guide</a>"""

    async def run(
        self,
        *,
        method: Literal["forward", "backward"],
        source: GeostatisticsNormalScoreSource | AnyTypedAttribute,
        distribution: ObjectInput,
        target: GeostatisticsNormalScoreTarget | AnyTypedAttribute,
        preview: bool = True,
    ) -> GeostatisticsNormalScoreResult:
        """For more information, please read the <a href='/docs/guides/geostatistics-tasks/tasks/normal-score'>guide</a>"""
        ...

class _SyncGeostatisticsNormalScore:
    """For more information, please read the <a href='/docs/guides/geostatistics-tasks/tasks/normal-score'>guide</a>"""

    def run(
        self,
        *,
        method: Literal["forward", "backward"],
        source: GeostatisticsNormalScoreSource | AnyTypedAttribute,
        distribution: ObjectInput,
        target: GeostatisticsNormalScoreTarget | AnyTypedAttribute,
        preview: bool = True,
    ) -> SyncGeostatisticsNormalScoreResult:
        """For more information, please read the <a href='/docs/guides/geostatistics-tasks/tasks/normal-score'>guide</a>"""
        ...

class _GeostatisticsProfitCalculation:
    """Calculate profit given block model and economic parameters"""

    async def run(
        self,
        *,
        source: GeostatisticsProfitCalculationSource | AnyTypedAttribute,
        target: GeostatisticsProfitCalculationTarget | AnyTypedAttribute,
        material_categories: list[GeostatisticsProfitCalculationMaterialCategories],
        processing_cost: float,
        mining_waste_cost: float,
        mining_ore_cost: float,
        metal_recovery_fraction: float,
        preview: bool = True,
    ) -> GeostatisticsProfitCalculationResult:
        """Calculate profit given block model and economic parameters"""
        ...

class _SyncGeostatisticsProfitCalculation:
    """Calculate profit given block model and economic parameters"""

    def run(
        self,
        *,
        source: GeostatisticsProfitCalculationSource | AnyTypedAttribute,
        target: GeostatisticsProfitCalculationTarget | AnyTypedAttribute,
        material_categories: list[GeostatisticsProfitCalculationMaterialCategories],
        processing_cost: float,
        mining_waste_cost: float,
        mining_ore_cost: float,
        metal_recovery_fraction: float,
        preview: bool = True,
    ) -> SyncGeostatisticsProfitCalculationResult:
        """Calculate profit given block model and economic parameters"""
        ...

class _GeostatisticsSimulationReport:
    """For more information, please read the <a href='/docs/guides/geostatistics-tasks/tasks/simulation-report'>guide</a>"""

    async def run(
        self,
        *,
        simulation_source: ObjectInput,
        source_attribute: AttributeInput,
        simulation_target: ObjectInput,
        point_simulations: AttributeInput,
        point_simulations_normal_score: AttributeInput,
        block_simulations: AttributeInput,
        block_simulations_normal_score: AttributeInput,
        variogram_model: ObjectInput,
        neighborhood: GeostatisticsSimulationReportNeighborhood,
        block_discretization: GeostatisticsSimulationReportBlockDiscretization,
        number_of_simulations: int,
        distribution: GeostatisticsSimulationReportContinuousDistributionParameters | None = ...,
        kriging_method: Literal["simple", "ordinary"] = ...,
        material: GeostatisticsSimulationReportMaterial | None = ...,
        number_of_lines: int = ...,
        random_seed: int = ...,
        report_context: GeostatisticsSimulationReportReportContext | None = ...,
        report_mean_thresholds: GeostatisticsSimulationReportReportMeanThresholds | None = ...,
        preview: bool = True,
    ) -> GeostatisticsSimulationReportResult:
        """For more information, please read the <a href='/docs/guides/geostatistics-tasks/tasks/simulation-report'>guide</a>"""
        ...

class _SyncGeostatisticsSimulationReport:
    """For more information, please read the <a href='/docs/guides/geostatistics-tasks/tasks/simulation-report'>guide</a>"""

    def run(
        self,
        *,
        simulation_source: ObjectInput,
        source_attribute: AttributeInput,
        simulation_target: ObjectInput,
        point_simulations: AttributeInput,
        point_simulations_normal_score: AttributeInput,
        block_simulations: AttributeInput,
        block_simulations_normal_score: AttributeInput,
        variogram_model: ObjectInput,
        neighborhood: GeostatisticsSimulationReportNeighborhood,
        block_discretization: GeostatisticsSimulationReportBlockDiscretization,
        number_of_simulations: int,
        distribution: GeostatisticsSimulationReportContinuousDistributionParameters | None = ...,
        kriging_method: Literal["simple", "ordinary"] = ...,
        material: GeostatisticsSimulationReportMaterial | None = ...,
        number_of_lines: int = ...,
        random_seed: int = ...,
        report_context: GeostatisticsSimulationReportReportContext | None = ...,
        report_mean_thresholds: GeostatisticsSimulationReportReportMeanThresholds | None = ...,
        preview: bool = True,
    ) -> SyncGeostatisticsSimulationReportResult:
        """For more information, please read the <a href='/docs/guides/geostatistics-tasks/tasks/simulation-report'>guide</a>"""
        ...

class _GeotechAddToIntersectedGeometry:
    """Adds one or more triangle mesh objects to an existing intersected Geological Model Meshes object."""

    async def run(
        self,
        *,
        source: ObjectInput,
        meshes_to_add: list[ObjectInput],
        target: GeotechAddToIntersectedGeometryTarget,
        tolerance: float,
        max_point: GeotechAddToIntersectedGeometryPoint3D = ...,
        min_point: GeotechAddToIntersectedGeometryPoint3D2 = ...,
        rotation_angle: float = ...,
        preview: bool = True,
    ) -> GeotechAddToIntersectedGeometryResult:
        """Adds one or more triangle mesh objects to an existing intersected Geological Model Meshes object."""
        ...

class _SyncGeotechAddToIntersectedGeometry:
    """Adds one or more triangle mesh objects to an existing intersected Geological Model Meshes object."""

    def run(
        self,
        *,
        source: ObjectInput,
        meshes_to_add: list[ObjectInput],
        target: GeotechAddToIntersectedGeometryTarget,
        tolerance: float,
        max_point: GeotechAddToIntersectedGeometryPoint3D = ...,
        min_point: GeotechAddToIntersectedGeometryPoint3D2 = ...,
        rotation_angle: float = ...,
        preview: bool = True,
    ) -> SyncGeotechAddToIntersectedGeometryResult:
        """Adds one or more triangle mesh objects to an existing intersected Geological Model Meshes object."""
        ...

class _GeotechCalculateMeshQualityHistogram:
    """Calculates a normalized inverse condition number histogram for triangles in a Triangle Mesh or Geological Model Meshes object."""

    async def run(
        self,
        *,
        source: ObjectInput,
        bin_width: float = ...,
        preview: bool = True,
    ) -> GeotechCalculateMeshQualityHistogramResult:
        """Calculates a normalized inverse condition number histogram for triangles in a Triangle Mesh or Geological Model Meshes object."""
        ...

class _SyncGeotechCalculateMeshQualityHistogram:
    """Calculates a normalized inverse condition number histogram for triangles in a Triangle Mesh or Geological Model Meshes object."""

    def run(
        self,
        *,
        source: ObjectInput,
        bin_width: float = ...,
        preview: bool = True,
    ) -> SyncGeotechCalculateMeshQualityHistogramResult:
        """Calculates a normalized inverse condition number histogram for triangles in a Triangle Mesh or Geological Model Meshes object."""
        ...

class _GeotechCheckTriangleOrientation:
    """Identifies triangles with inconsistent normal orientation relative to their neighbors in a Triangle Mesh or Geological Model Meshes object."""

    async def run(
        self,
        *,
        source: ObjectInput,
        preview: bool = True,
    ) -> GeotechCheckTriangleOrientationResult:
        """Identifies triangles with inconsistent normal orientation relative to their neighbors in a Triangle Mesh or Geological Model Meshes object."""
        ...

class _SyncGeotechCheckTriangleOrientation:
    """Identifies triangles with inconsistent normal orientation relative to their neighbors in a Triangle Mesh or Geological Model Meshes object."""

    def run(
        self,
        *,
        source: ObjectInput,
        preview: bool = True,
    ) -> SyncGeotechCheckTriangleOrientationResult:
        """Identifies triangles with inconsistent normal orientation relative to their neighbors in a Triangle Mesh or Geological Model Meshes object."""
        ...

class _GeotechCopyGeologicalModelMeshes:
    """Copies an existing set of geological model meshes, optionally removing external volumes from the result."""

    async def run(
        self,
        *,
        source: ObjectInput,
        target: GeotechCopyGeologicalModelMeshesTarget,
        remove_external_volumes: bool = ...,
        preview: bool = True,
    ) -> GeotechCopyGeologicalModelMeshesResult:
        """Copies an existing set of geological model meshes, optionally removing external volumes from the result."""
        ...

class _SyncGeotechCopyGeologicalModelMeshes:
    """Copies an existing set of geological model meshes, optionally removing external volumes from the result."""

    def run(
        self,
        *,
        source: ObjectInput,
        target: GeotechCopyGeologicalModelMeshesTarget,
        remove_external_volumes: bool = ...,
        preview: bool = True,
    ) -> SyncGeotechCopyGeologicalModelMeshesResult:
        """Copies an existing set of geological model meshes, optionally removing external volumes from the result."""
        ...

class _GeotechCreateCrossSection:
    """Intersects parametric volumes in a 3D Design Geometry with a vertical plane defined by a section origin and dip azimuth, producing a 2D Design Geometry with polyline boundaries."""

    async def run(
        self,
        *,
        source: ObjectInput,
        target: GeotechCreateCrossSectionTarget,
        section_origin: GeotechCreateCrossSectionPoint3D,
        section_dip_azimuth_degrees: float,
        preview: bool = True,
    ) -> GeotechCreateCrossSectionResult:
        """Intersects parametric volumes in a 3D Design Geometry with a vertical plane defined by a section origin and dip azimuth, producing a 2D Design Geometry with polyline boundaries."""
        ...

class _SyncGeotechCreateCrossSection:
    """Intersects parametric volumes in a 3D Design Geometry with a vertical plane defined by a section origin and dip azimuth, producing a 2D Design Geometry with polyline boundaries."""

    def run(
        self,
        *,
        source: ObjectInput,
        target: GeotechCreateCrossSectionTarget,
        section_origin: GeotechCreateCrossSectionPoint3D,
        section_dip_azimuth_degrees: float,
        preview: bool = True,
    ) -> SyncGeotechCreateCrossSectionResult:
        """Intersects parametric volumes in a 3D Design Geometry with a vertical plane defined by a section origin and dip azimuth, producing a 2D Design Geometry with polyline boundaries."""
        ...

class _GeotechCreateGeologicalModelMeshes:
    """Decomposes an intersected mesh or existing GMM into per-volume geological model meshes, optionally removing the top (external) volume."""

    async def run(
        self,
        *,
        source: ObjectInput,
        target: GeotechCreateGeologicalModelMeshesTarget,
        preview: bool = True,
    ) -> GeotechCreateGeologicalModelMeshesResult:
        """Decomposes an intersected mesh or existing GMM into per-volume geological model meshes, optionally removing the top (external) volume."""
        ...

class _SyncGeotechCreateGeologicalModelMeshes:
    """Decomposes an intersected mesh or existing GMM into per-volume geological model meshes, optionally removing the top (external) volume."""

    def run(
        self,
        *,
        source: ObjectInput,
        target: GeotechCreateGeologicalModelMeshesTarget,
        preview: bool = True,
    ) -> SyncGeotechCreateGeologicalModelMeshesResult:
        """Decomposes an intersected mesh or existing GMM into per-volume geological model meshes, optionally removing the top (external) volume."""
        ...

class _GeotechDetectDegenerateTriangles:
    """Identifies needle and cap triangles in a Triangle Mesh or Geological Model Meshes object."""

    async def run(
        self,
        *,
        source: ObjectInput,
        cap_min_angle_degrees: float = ...,
        needle_threshold_ratio: float = ...,
        preview: bool = True,
    ) -> GeotechDetectDegenerateTrianglesResult:
        """Identifies needle and cap triangles in a Triangle Mesh or Geological Model Meshes object."""
        ...

class _SyncGeotechDetectDegenerateTriangles:
    """Identifies needle and cap triangles in a Triangle Mesh or Geological Model Meshes object."""

    def run(
        self,
        *,
        source: ObjectInput,
        cap_min_angle_degrees: float = ...,
        needle_threshold_ratio: float = ...,
        preview: bool = True,
    ) -> SyncGeotechDetectDegenerateTrianglesResult:
        """Identifies needle and cap triangles in a Triangle Mesh or Geological Model Meshes object."""
        ...

class _GeotechDetectDuplicatePoints:
    """Identifies exact duplicate vertices in a Triangle Mesh or Geological Model Meshes object."""

    async def run(
        self,
        *,
        source: ObjectInput,
        preview: bool = True,
    ) -> GeotechDetectDuplicatePointsResult:
        """Identifies exact duplicate vertices in a Triangle Mesh or Geological Model Meshes object."""
        ...

class _SyncGeotechDetectDuplicatePoints:
    """Identifies exact duplicate vertices in a Triangle Mesh or Geological Model Meshes object."""

    def run(
        self,
        *,
        source: ObjectInput,
        preview: bool = True,
    ) -> SyncGeotechDetectDuplicatePointsResult:
        """Identifies exact duplicate vertices in a Triangle Mesh or Geological Model Meshes object."""
        ...

class _GeotechDetectDuplicateTriangles:
    """Identifies duplicate triangles (same vertex indices regardless of order) in a Triangle Mesh or Geological Model Meshes object."""

    async def run(
        self,
        *,
        source: ObjectInput,
        preview: bool = True,
    ) -> GeotechDetectDuplicateTrianglesResult:
        """Identifies duplicate triangles (same vertex indices regardless of order) in a Triangle Mesh or Geological Model Meshes object."""
        ...

class _SyncGeotechDetectDuplicateTriangles:
    """Identifies duplicate triangles (same vertex indices regardless of order) in a Triangle Mesh or Geological Model Meshes object."""

    def run(
        self,
        *,
        source: ObjectInput,
        preview: bool = True,
    ) -> SyncGeotechDetectDuplicateTrianglesResult:
        """Identifies duplicate triangles (same vertex indices regardless of order) in a Triangle Mesh or Geological Model Meshes object."""
        ...

class _GeotechDetectFins:
    """Identifies fin-like connected components without closed boundaries in a Triangle Mesh or Geological Model Meshes object."""

    async def run(
        self,
        *,
        source: ObjectInput,
        max_area_ratio: float = ...,
        preview: bool = True,
    ) -> GeotechDetectFinsResult:
        """Identifies fin-like connected components without closed boundaries in a Triangle Mesh or Geological Model Meshes object."""
        ...

class _SyncGeotechDetectFins:
    """Identifies fin-like connected components without closed boundaries in a Triangle Mesh or Geological Model Meshes object."""

    def run(
        self,
        *,
        source: ObjectInput,
        max_area_ratio: float = ...,
        preview: bool = True,
    ) -> SyncGeotechDetectFinsResult:
        """Identifies fin-like connected components without closed boundaries in a Triangle Mesh or Geological Model Meshes object."""
        ...

class _GeotechDetectHoles:
    """Identifies holes in a Triangle Mesh or Geological Model Meshes object based on boundary edge analysis."""

    async def run(
        self,
        *,
        source: ObjectInput,
        hole_size_ratio_tolerance: float = ...,
        preview: bool = True,
    ) -> GeotechDetectHolesResult:
        """Identifies holes in a Triangle Mesh or Geological Model Meshes object based on boundary edge analysis."""
        ...

class _SyncGeotechDetectHoles:
    """Identifies holes in a Triangle Mesh or Geological Model Meshes object based on boundary edge analysis."""

    def run(
        self,
        *,
        source: ObjectInput,
        hole_size_ratio_tolerance: float = ...,
        preview: bool = True,
    ) -> SyncGeotechDetectHolesResult:
        """Identifies holes in a Triangle Mesh or Geological Model Meshes object based on boundary edge analysis."""
        ...

class _GeotechDetectNonManifoldEdges:
    """Identifies edges shared by more than two triangles in a Triangle Mesh or Geological Model Meshes object."""

    async def run(
        self,
        *,
        source: ObjectInput,
        preview: bool = True,
    ) -> GeotechDetectNonManifoldEdgesResult:
        """Identifies edges shared by more than two triangles in a Triangle Mesh or Geological Model Meshes object."""
        ...

class _SyncGeotechDetectNonManifoldEdges:
    """Identifies edges shared by more than two triangles in a Triangle Mesh or Geological Model Meshes object."""

    def run(
        self,
        *,
        source: ObjectInput,
        preview: bool = True,
    ) -> SyncGeotechDetectNonManifoldEdgesResult:
        """Identifies edges shared by more than two triangles in a Triangle Mesh or Geological Model Meshes object."""
        ...

class _GeotechDetectNonManifoldVertices:
    """Identifies vertices with non-manifold topology in a Triangle Mesh or Geological Model Meshes object."""

    async def run(
        self,
        *,
        source: ObjectInput,
        preview: bool = True,
    ) -> GeotechDetectNonManifoldVerticesResult:
        """Identifies vertices with non-manifold topology in a Triangle Mesh or Geological Model Meshes object."""
        ...

class _SyncGeotechDetectNonManifoldVertices:
    """Identifies vertices with non-manifold topology in a Triangle Mesh or Geological Model Meshes object."""

    def run(
        self,
        *,
        source: ObjectInput,
        preview: bool = True,
    ) -> SyncGeotechDetectNonManifoldVerticesResult:
        """Identifies vertices with non-manifold topology in a Triangle Mesh or Geological Model Meshes object."""
        ...

class _GeotechDetectNonPartitioningSurfaces:
    """Identifies non-partitioning surfaces that are neither fins nor surfaces with holes in a Triangle Mesh or Geological Model Meshes object."""

    async def run(
        self,
        *,
        source: ObjectInput,
        hole_size_ratio_tolerance: float = ...,
        max_area_ratio: float = ...,
        preview: bool = True,
    ) -> GeotechDetectNonPartitioningSurfacesResult:
        """Identifies non-partitioning surfaces that are neither fins nor surfaces with holes in a Triangle Mesh or Geological Model Meshes object."""
        ...

class _SyncGeotechDetectNonPartitioningSurfaces:
    """Identifies non-partitioning surfaces that are neither fins nor surfaces with holes in a Triangle Mesh or Geological Model Meshes object."""

    def run(
        self,
        *,
        source: ObjectInput,
        hole_size_ratio_tolerance: float = ...,
        max_area_ratio: float = ...,
        preview: bool = True,
    ) -> SyncGeotechDetectNonPartitioningSurfacesResult:
        """Identifies non-partitioning surfaces that are neither fins nor surfaces with holes in a Triangle Mesh or Geological Model Meshes object."""
        ...

class _GeotechDetectSelfIntersections:
    """Identifies self-intersecting triangles in a Triangle Mesh or Geological Model Meshes object."""

    async def run(
        self,
        *,
        source: ObjectInput,
        tolerance: float = ...,
        preview: bool = True,
    ) -> GeotechDetectSelfIntersectionsResult:
        """Identifies self-intersecting triangles in a Triangle Mesh or Geological Model Meshes object."""
        ...

class _SyncGeotechDetectSelfIntersections:
    """Identifies self-intersecting triangles in a Triangle Mesh or Geological Model Meshes object."""

    def run(
        self,
        *,
        source: ObjectInput,
        tolerance: float = ...,
        preview: bool = True,
    ) -> SyncGeotechDetectSelfIntersectionsResult:
        """Identifies self-intersecting triangles in a Triangle Mesh or Geological Model Meshes object."""
        ...

class _GeotechFillHoles:
    """Fills holes in the triangle mesh of a GMM object using specified edge identifiers and a configurable fill mode strategy."""

    async def run(
        self,
        *,
        source: ObjectInput,
        target: GeotechFillHolesTarget,
        edges: list[list[int]],
        fill_mode_selection: Literal["Fill", "FillAndRefine", "FillRefineAndFair"],
        preview: bool = True,
    ) -> GeotechFillHolesResult:
        """Fills holes in the triangle mesh of a GMM object using specified edge identifiers and a configurable fill mode strategy."""
        ...

class _SyncGeotechFillHoles:
    """Fills holes in the triangle mesh of a GMM object using specified edge identifiers and a configurable fill mode strategy."""

    def run(
        self,
        *,
        source: ObjectInput,
        target: GeotechFillHolesTarget,
        edges: list[list[int]],
        fill_mode_selection: Literal["Fill", "FillAndRefine", "FillRefineAndFair"],
        preview: bool = True,
    ) -> SyncGeotechFillHolesResult:
        """Fills holes in the triangle mesh of a GMM object using specified edge identifiers and a configurable fill mode strategy."""
        ...

class _GeotechFixSelfIntersections:
    """Fixes self-intersections in the triangle mesh of a GMM object using configurable intersection detection settings."""

    async def run(
        self,
        *,
        source: ObjectInput,
        target: GeotechFixSelfIntersectionsTarget,
        intersect_settings: GeotechFixSelfIntersectionsIntersectSettings,
        preview: bool = True,
    ) -> GeotechFixSelfIntersectionsResult:
        """Fixes self-intersections in the triangle mesh of a GMM object using configurable intersection detection settings."""
        ...

class _SyncGeotechFixSelfIntersections:
    """Fixes self-intersections in the triangle mesh of a GMM object using configurable intersection detection settings."""

    def run(
        self,
        *,
        source: ObjectInput,
        target: GeotechFixSelfIntersectionsTarget,
        intersect_settings: GeotechFixSelfIntersectionsIntersectSettings,
        preview: bool = True,
    ) -> SyncGeotechFixSelfIntersectionsResult:
        """Fixes self-intersections in the triangle mesh of a GMM object using configurable intersection detection settings."""
        ...

class _GeotechInitAggregateGeomGmm:
    """Creates a rotated bounding box as a Geological Model Meshes object, used as the initial intersected geometry when creating a model."""

    async def run(
        self,
        *,
        target: GeotechInitAggregateGeomGmmTarget,
        min_point: GeotechInitAggregateGeomGmmPoint3D,
        max_point: GeotechInitAggregateGeomGmmPoint3D2,
        coordinate_reference_system: str | None = ...,
        rotation_angle: float = ...,
        preview: bool = True,
    ) -> GeotechInitAggregateGeomGmmResult:
        """Creates a rotated bounding box as a Geological Model Meshes object, used as the initial intersected geometry when creating a model."""
        ...

class _SyncGeotechInitAggregateGeomGmm:
    """Creates a rotated bounding box as a Geological Model Meshes object, used as the initial intersected geometry when creating a model."""

    def run(
        self,
        *,
        target: GeotechInitAggregateGeomGmmTarget,
        min_point: GeotechInitAggregateGeomGmmPoint3D,
        max_point: GeotechInitAggregateGeomGmmPoint3D2,
        coordinate_reference_system: str | None = ...,
        rotation_angle: float = ...,
        preview: bool = True,
    ) -> SyncGeotechInitAggregateGeomGmmResult:
        """Creates a rotated bounding box as a Geological Model Meshes object, used as the initial intersected geometry when creating a model."""
        ...

class _GeotechLocalRemesh:
    """Locally remeshes the region around selected seed triangles in a Geological Model Meshes object using configurable remeshing parameters and radius-based region growth."""

    async def run(
        self,
        *,
        source: ObjectInput,
        target: GeotechLocalRemeshTarget,
        seed_triangle_indices: list[int],
        radius: float = ...,
        remesh_settings: GeotechLocalRemeshRemeshSettings = ...,
        preview: bool = True,
    ) -> GeotechLocalRemeshResult:
        """Locally remeshes the region around selected seed triangles in a Geological Model Meshes object using configurable remeshing parameters and radius-based region growth."""
        ...

class _SyncGeotechLocalRemesh:
    """Locally remeshes the region around selected seed triangles in a Geological Model Meshes object using configurable remeshing parameters and radius-based region growth."""

    def run(
        self,
        *,
        source: ObjectInput,
        target: GeotechLocalRemeshTarget,
        seed_triangle_indices: list[int],
        radius: float = ...,
        remesh_settings: GeotechLocalRemeshRemeshSettings = ...,
        preview: bool = True,
    ) -> SyncGeotechLocalRemeshResult:
        """Locally remeshes the region around selected seed triangles in a Geological Model Meshes object using configurable remeshing parameters and radius-based region growth."""
        ...

class _GeotechMakeGmmIntoIntersectedGeometry:
    """Clips a Geological Model Meshes object and processes it to create an intersected geometry mesh."""

    async def run(
        self,
        *,
        source: ObjectInput,
        target: GeotechMakeGmmIntoIntersectedGeometryTarget,
        clip_tolerance: float,
        attempt_auto_fix_on_failure: bool = ...,
        attempt_conformalization: bool = ...,
        collapse_settings: GeotechMakeGmmIntoIntersectedGeometryCollapseOverlapsSettings = ...,
        intersection_tolerance_for_auto_fix: float = ...,
        max_point: GeotechMakeGmmIntoIntersectedGeometryPoint3D = ...,
        min_point: GeotechMakeGmmIntoIntersectedGeometryPoint3D2 = ...,
        remesh_settings: GeotechMakeGmmIntoIntersectedGeometryRemeshSettings = ...,
        rotation_angle: float = ...,
        preview: bool = True,
    ) -> GeotechMakeGmmIntoIntersectedGeometryResult:
        """Clips a Geological Model Meshes object and processes it to create an intersected geometry mesh."""
        ...

class _SyncGeotechMakeGmmIntoIntersectedGeometry:
    """Clips a Geological Model Meshes object and processes it to create an intersected geometry mesh."""

    def run(
        self,
        *,
        source: ObjectInput,
        target: GeotechMakeGmmIntoIntersectedGeometryTarget,
        clip_tolerance: float,
        attempt_auto_fix_on_failure: bool = ...,
        attempt_conformalization: bool = ...,
        collapse_settings: GeotechMakeGmmIntoIntersectedGeometryCollapseOverlapsSettings = ...,
        intersection_tolerance_for_auto_fix: float = ...,
        max_point: GeotechMakeGmmIntoIntersectedGeometryPoint3D = ...,
        min_point: GeotechMakeGmmIntoIntersectedGeometryPoint3D2 = ...,
        remesh_settings: GeotechMakeGmmIntoIntersectedGeometryRemeshSettings = ...,
        rotation_angle: float = ...,
        preview: bool = True,
    ) -> SyncGeotechMakeGmmIntoIntersectedGeometryResult:
        """Clips a Geological Model Meshes object and processes it to create an intersected geometry mesh."""
        ...

class _GeotechMakeIntoModelInput:
    """Clips a triangle mesh loosely to the specified bounds, merges duplicate points, and translates the result so it can be used as model input."""

    async def run(
        self,
        *,
        source: ObjectInput,
        target: GeotechMakeIntoModelInputTarget,
        max_point: GeotechMakeIntoModelInputMaxPoint = ...,
        min_point: GeotechMakeIntoModelInputMinPoint = ...,
        rotation_angle: float = ...,
        tolerance: float = ...,
        preview: bool = True,
    ) -> GeotechMakeIntoModelInputResult:
        """Clips a triangle mesh loosely to the specified bounds, merges duplicate points, and translates the result so it can be used as model input."""
        ...

class _SyncGeotechMakeIntoModelInput:
    """Clips a triangle mesh loosely to the specified bounds, merges duplicate points, and translates the result so it can be used as model input."""

    def run(
        self,
        *,
        source: ObjectInput,
        target: GeotechMakeIntoModelInputTarget,
        max_point: GeotechMakeIntoModelInputMaxPoint = ...,
        min_point: GeotechMakeIntoModelInputMinPoint = ...,
        rotation_angle: float = ...,
        tolerance: float = ...,
        preview: bool = True,
    ) -> SyncGeotechMakeIntoModelInputResult:
        """Clips a triangle mesh loosely to the specified bounds, merges duplicate points, and translates the result so it can be used as model input."""
        ...

class _GeotechMarkVolumesForRemoval:
    """Marks specified volumes in a Geological Model Meshes for removal by setting their material to external, or clears the marking."""

    async def run(
        self,
        *,
        source: ObjectInput,
        target: GeotechMarkVolumesForRemovalTarget,
        settings: GeotechMarkVolumesForRemovalSettings,
        preview: bool = True,
    ) -> GeotechMarkVolumesForRemovalResult:
        """Marks specified volumes in a Geological Model Meshes for removal by setting their material to external, or clears the marking."""
        ...

class _SyncGeotechMarkVolumesForRemoval:
    """Marks specified volumes in a Geological Model Meshes for removal by setting their material to external, or clears the marking."""

    def run(
        self,
        *,
        source: ObjectInput,
        target: GeotechMarkVolumesForRemovalTarget,
        settings: GeotechMarkVolumesForRemovalSettings,
        preview: bool = True,
    ) -> SyncGeotechMarkVolumesForRemovalResult:
        """Marks specified volumes in a Geological Model Meshes for removal by setting their material to external, or clears the marking."""
        ...

class _GeotechParameterizeVolumes:
    """Parameterizes closed volumes from Triangle Mesh or Geological Model Meshes inputs and writes the result as a Design Geometry object."""

    async def run(
        self,
        *,
        source: ObjectInput,
        target: GeotechParameterizeVolumesTarget,
        distance_unit: Literal[
            "m",
            "angstrom",
            "chain",
            "chain[BnA]",
            "chain[BnB]",
            "chain[Cla]",
            "chain[Ind37]",
            "chain[Se]",
            "chain[SeT]",
            "chain[US]",
            "fathom",
            "ft",
            "ft[BnA]",
            "ft[BnB]",
            "ft[Br36]",
            "ft[Br65]",
            "ft[Cla]",
            "ft[GC]",
            "ft[Ind]",
            "ft[Ind37]",
            "ft[Ind62]",
            "ft[Ind75]",
            "ft[Se]",
            "ft[SeT]",
            "ft[US]",
            "fur[US]",
            "in",
            "in[US]",
            "link",
            "link[BnA]",
            "link[BnB]",
            "link[Cla]",
            "link[Se]",
            "link[SeT]",
            "link[US]",
            "m[Ger]",
            "mi",
            "mi[naut]",
            "mi[nautUK]",
            "mi[US]",
            "mil",
            "rod[US]",
            "yd",
            "yd[BnA]",
            "yd[BnB]",
            "yd[Cla]",
            "yd[Ind]",
            "yd[Ind37]",
            "yd[Ind62]",
            "yd[Ind75]",
            "yd[Se]",
            "yd[SeT]",
            "yd[US]",
            "m3/m2",
            "0.1 ft",
            "0.1 ft[US]",
            "0.1 in",
            "0.1 yd",
            "1/16 in",
            "1/2 ft",
            "1/32 in",
            "1/64 in",
            "10 ft",
            "10 in",
            "10 km",
            "100 ft",
            "100 km",
            "1000 ft",
            "30 ft",
            "30 m",
            "1E6 bbl/acre",
            "bbl/acre",
            "ft3/ft2",
            "cm",
            "dam",
            "dm",
            "Em",
            "fm",
            "Gm",
            "hm",
            "km",
            "Mm",
            "mm",
            "nm",
            "pm",
            "Tm",
            "um",
        ],
        include_hard_edges: bool = ...,
        max_repair_iterations: int = ...,
        preview: bool = True,
    ) -> GeotechParameterizeVolumesResult:
        """Parameterizes closed volumes from Triangle Mesh or Geological Model Meshes inputs and writes the result as a Design Geometry object."""
        ...

class _SyncGeotechParameterizeVolumes:
    """Parameterizes closed volumes from Triangle Mesh or Geological Model Meshes inputs and writes the result as a Design Geometry object."""

    def run(
        self,
        *,
        source: ObjectInput,
        target: GeotechParameterizeVolumesTarget,
        distance_unit: Literal[
            "m",
            "angstrom",
            "chain",
            "chain[BnA]",
            "chain[BnB]",
            "chain[Cla]",
            "chain[Ind37]",
            "chain[Se]",
            "chain[SeT]",
            "chain[US]",
            "fathom",
            "ft",
            "ft[BnA]",
            "ft[BnB]",
            "ft[Br36]",
            "ft[Br65]",
            "ft[Cla]",
            "ft[GC]",
            "ft[Ind]",
            "ft[Ind37]",
            "ft[Ind62]",
            "ft[Ind75]",
            "ft[Se]",
            "ft[SeT]",
            "ft[US]",
            "fur[US]",
            "in",
            "in[US]",
            "link",
            "link[BnA]",
            "link[BnB]",
            "link[Cla]",
            "link[Se]",
            "link[SeT]",
            "link[US]",
            "m[Ger]",
            "mi",
            "mi[naut]",
            "mi[nautUK]",
            "mi[US]",
            "mil",
            "rod[US]",
            "yd",
            "yd[BnA]",
            "yd[BnB]",
            "yd[Cla]",
            "yd[Ind]",
            "yd[Ind37]",
            "yd[Ind62]",
            "yd[Ind75]",
            "yd[Se]",
            "yd[SeT]",
            "yd[US]",
            "m3/m2",
            "0.1 ft",
            "0.1 ft[US]",
            "0.1 in",
            "0.1 yd",
            "1/16 in",
            "1/2 ft",
            "1/32 in",
            "1/64 in",
            "10 ft",
            "10 in",
            "10 km",
            "100 ft",
            "100 km",
            "1000 ft",
            "30 ft",
            "30 m",
            "1E6 bbl/acre",
            "bbl/acre",
            "ft3/ft2",
            "cm",
            "dam",
            "dm",
            "Em",
            "fm",
            "Gm",
            "hm",
            "km",
            "Mm",
            "mm",
            "nm",
            "pm",
            "Tm",
            "um",
        ],
        include_hard_edges: bool = ...,
        max_repair_iterations: int = ...,
        preview: bool = True,
    ) -> SyncGeotechParameterizeVolumesResult:
        """Parameterizes closed volumes from Triangle Mesh or Geological Model Meshes inputs and writes the result as a Design Geometry object."""
        ...

class _GeotechRemesh:
    """Remeshes the full Geological Model Meshes object using configurable remeshing parameters to improve triangle quality while preserving the model structure."""

    async def run(
        self,
        *,
        source: ObjectInput,
        target: GeotechRemeshTarget,
        initial_cleanup: bool = ...,
        is_closed: bool = ...,
        line_proximity_detection: bool = ...,
        max_chordal_error: float = ...,
        optimize: bool = ...,
        patch_angle_tolerance: float = ...,
        regularize_mode: bool = ...,
        shape_quality_weight: float = ...,
        strain_tolerance: float = ...,
        target_h: float = ...,
        preview: bool = True,
    ) -> GeotechRemeshResult:
        """Remeshes the full Geological Model Meshes object using configurable remeshing parameters to improve triangle quality while preserving the model structure."""
        ...

class _SyncGeotechRemesh:
    """Remeshes the full Geological Model Meshes object using configurable remeshing parameters to improve triangle quality while preserving the model structure."""

    def run(
        self,
        *,
        source: ObjectInput,
        target: GeotechRemeshTarget,
        initial_cleanup: bool = ...,
        is_closed: bool = ...,
        line_proximity_detection: bool = ...,
        max_chordal_error: float = ...,
        optimize: bool = ...,
        patch_angle_tolerance: float = ...,
        regularize_mode: bool = ...,
        shape_quality_weight: float = ...,
        strain_tolerance: float = ...,
        target_h: float = ...,
        preview: bool = True,
    ) -> SyncGeotechRemeshResult:
        """Remeshes the full Geological Model Meshes object using configurable remeshing parameters to improve triangle quality while preserving the model structure."""
        ...

class _GeotechRemoveTriangles:
    """Removes the selected triangle indices from a Geological Model Meshes object and uploads the updated mesh as a new object."""

    async def run(
        self,
        *,
        source: ObjectInput,
        target: GeotechRemoveTrianglesTarget,
        triangle_indices: list[int],
        preview: bool = True,
    ) -> GeotechRemoveTrianglesResult:
        """Removes the selected triangle indices from a Geological Model Meshes object and uploads the updated mesh as a new object."""
        ...

class _SyncGeotechRemoveTriangles:
    """Removes the selected triangle indices from a Geological Model Meshes object and uploads the updated mesh as a new object."""

    def run(
        self,
        *,
        source: ObjectInput,
        target: GeotechRemoveTrianglesTarget,
        triangle_indices: list[int],
        preview: bool = True,
    ) -> SyncGeotechRemoveTrianglesResult:
        """Removes the selected triangle indices from a Geological Model Meshes object and uploads the updated mesh as a new object."""
        ...

class _GeotechSubsumeEmbeddedVolumeInGmm:
    """Merges a specified source embedded volume into a target volume in a GMM, either by explicit index or by automatic selection strategy."""

    async def run(
        self,
        *,
        source: ObjectInput,
        target: GeotechSubsumeEmbeddedVolumeInGmmTarget,
        subsume_source: int,
        target_selection: GeotechSubsumeEmbeddedVolumeInGmmExplicitTargetSelection
        | GeotechSubsumeEmbeddedVolumeInGmmLargestNeighbouringVolumeTargetSelection
        | GeotechSubsumeEmbeddedVolumeInGmmLargestNeighbouringContactAreaTargetSelection,
        preview: bool = True,
    ) -> GeotechSubsumeEmbeddedVolumeInGmmResult:
        """Merges a specified source embedded volume into a target volume in a GMM, either by explicit index or by automatic selection strategy."""
        ...

class _SyncGeotechSubsumeEmbeddedVolumeInGmm:
    """Merges a specified source embedded volume into a target volume in a GMM, either by explicit index or by automatic selection strategy."""

    def run(
        self,
        *,
        source: ObjectInput,
        target: GeotechSubsumeEmbeddedVolumeInGmmTarget,
        subsume_source: int,
        target_selection: GeotechSubsumeEmbeddedVolumeInGmmExplicitTargetSelection
        | GeotechSubsumeEmbeddedVolumeInGmmLargestNeighbouringVolumeTargetSelection
        | GeotechSubsumeEmbeddedVolumeInGmmLargestNeighbouringContactAreaTargetSelection,
        preview: bool = True,
    ) -> SyncGeotechSubsumeEmbeddedVolumeInGmmResult:
        """Merges a specified source embedded volume into a target volume in a GMM, either by explicit index or by automatic selection strategy."""
        ...

class _GeotechValidateGmmAsIntersectedGeometry:
    """Checks whether a Geological Model Meshes object is valid as intersected geometry and returns human-readable descriptions of any issues found."""

    async def run(
        self,
        *,
        source: ObjectInput,
        preview: bool = True,
    ) -> GeotechValidateGmmAsIntersectedGeometryResult:
        """Checks whether a Geological Model Meshes object is valid as intersected geometry and returns human-readable descriptions of any issues found."""
        ...

class _SyncGeotechValidateGmmAsIntersectedGeometry:
    """Checks whether a Geological Model Meshes object is valid as intersected geometry and returns human-readable descriptions of any issues found."""

    def run(
        self,
        *,
        source: ObjectInput,
        preview: bool = True,
    ) -> SyncGeotechValidateGmmAsIntersectedGeometryResult:
        """Checks whether a Geological Model Meshes object is valid as intersected geometry and returns human-readable descriptions of any issues found."""
        ...

class _GtmAddToIntersectedGeometry:
    """[TODO: Add description] Performs Add To Intersected Geometry operation on geotechnical objects."""

    async def run(
        self,
        *,
        gooseContext: GtmAddToIntersectedGeometryGooseContext,
        action: Literal["add-to-intersected-geometry"],
        inputObjects: list[ObjectInput],
        intersectedGeomId: ObjectInput,
        tolerance: float,
        minPoint: GtmAddToIntersectedGeometryMinPoint,
        maxPoint: GtmAddToIntersectedGeometryMaxPoint,
        rotationAngle: float,
        sessionId: str = ...,
        preview: bool = False,
    ) -> GtmAddToIntersectedGeometryResult:
        """[TODO: Add description] Performs Add To Intersected Geometry operation on geotechnical objects."""
        ...

class _SyncGtmAddToIntersectedGeometry:
    """[TODO: Add description] Performs Add To Intersected Geometry operation on geotechnical objects."""

    def run(
        self,
        *,
        gooseContext: GtmAddToIntersectedGeometryGooseContext,
        action: Literal["add-to-intersected-geometry"],
        inputObjects: list[ObjectInput],
        intersectedGeomId: ObjectInput,
        tolerance: float,
        minPoint: GtmAddToIntersectedGeometryMinPoint,
        maxPoint: GtmAddToIntersectedGeometryMaxPoint,
        rotationAngle: float,
        sessionId: str = ...,
        preview: bool = False,
    ) -> SyncGtmAddToIntersectedGeometryResult:
        """[TODO: Add description] Performs Add To Intersected Geometry operation on geotechnical objects."""
        ...

class _GtmClip:
    """[TODO: Add description] Performs Clip operation on geotechnical objects."""

    async def run(
        self,
        *,
        gooseContext: GtmClipGooseContext,
        action: Literal["clip"],
        inputObjects: list[ObjectInput],
        minPoint: GtmClipMinPoint,
        maxPoint: GtmClipMaxPoint,
        rotationAngle: float,
        sessionId: str = ...,
        preview: bool = False,
    ) -> GtmClipResult:
        """[TODO: Add description] Performs Clip operation on geotechnical objects."""
        ...

class _SyncGtmClip:
    """[TODO: Add description] Performs Clip operation on geotechnical objects."""

    def run(
        self,
        *,
        gooseContext: GtmClipGooseContext,
        action: Literal["clip"],
        inputObjects: list[ObjectInput],
        minPoint: GtmClipMinPoint,
        maxPoint: GtmClipMaxPoint,
        rotationAngle: float,
        sessionId: str = ...,
        preview: bool = False,
    ) -> SyncGtmClipResult:
        """[TODO: Add description] Performs Clip operation on geotechnical objects."""
        ...

class _GtmCreateCrossSection:
    """[TODO: Add description] Performs Create Cross Section operation on geotechnical objects."""

    async def run(
        self,
        *,
        gooseContext: GtmCreateCrossSectionGooseContext,
        action: Literal["create-cross-section"],
        inputObjects: list[ObjectInput],
        path: str,
        sectionOrigin: GtmCreateCrossSectionSectionOrigin,
        sectionDipAzimuthDegrees: float,
        sessionId: str = ...,
        preview: bool = False,
    ) -> GtmCreateCrossSectionResult:
        """[TODO: Add description] Performs Create Cross Section operation on geotechnical objects."""
        ...

class _SyncGtmCreateCrossSection:
    """[TODO: Add description] Performs Create Cross Section operation on geotechnical objects."""

    def run(
        self,
        *,
        gooseContext: GtmCreateCrossSectionGooseContext,
        action: Literal["create-cross-section"],
        inputObjects: list[ObjectInput],
        path: str,
        sectionOrigin: GtmCreateCrossSectionSectionOrigin,
        sectionDipAzimuthDegrees: float,
        sessionId: str = ...,
        preview: bool = False,
    ) -> SyncGtmCreateCrossSectionResult:
        """[TODO: Add description] Performs Create Cross Section operation on geotechnical objects."""
        ...

class _GtmCreateGeologicalModelMeshes:
    """[TODO: Add description] Performs Create Geological Model Meshes operation on geotechnical objects."""

    async def run(
        self,
        *,
        gooseContext: GtmCreateGeologicalModelMeshesGooseContext,
        action: Literal["create-geological-model-meshes"],
        inputObjects: list[ObjectInput],
        path: str,
        sessionId: str = ...,
        preview: bool = False,
    ) -> GtmCreateGeologicalModelMeshesResult:
        """[TODO: Add description] Performs Create Geological Model Meshes operation on geotechnical objects."""
        ...

class _SyncGtmCreateGeologicalModelMeshes:
    """[TODO: Add description] Performs Create Geological Model Meshes operation on geotechnical objects."""

    def run(
        self,
        *,
        gooseContext: GtmCreateGeologicalModelMeshesGooseContext,
        action: Literal["create-geological-model-meshes"],
        inputObjects: list[ObjectInput],
        path: str,
        sessionId: str = ...,
        preview: bool = False,
    ) -> SyncGtmCreateGeologicalModelMeshesResult:
        """[TODO: Add description] Performs Create Geological Model Meshes operation on geotechnical objects."""
        ...

class _GtmDetectDegenerateTris:
    """[TODO: Add description] Performs Detect Degenerate Tris operation on geotechnical objects."""

    async def run(
        self,
        *,
        gooseContext: GtmDetectDegenerateTrisGooseContext,
        action: Literal["detect-degenerate-tris"],
        inputObjects: list[ObjectInput],
        needleThresholdRatio: float,
        capMinAngleDegrees: float,
        sessionId: str = ...,
        preview: bool = False,
    ) -> GtmDetectDegenerateTrisResult:
        """[TODO: Add description] Performs Detect Degenerate Tris operation on geotechnical objects."""
        ...

class _SyncGtmDetectDegenerateTris:
    """[TODO: Add description] Performs Detect Degenerate Tris operation on geotechnical objects."""

    def run(
        self,
        *,
        gooseContext: GtmDetectDegenerateTrisGooseContext,
        action: Literal["detect-degenerate-tris"],
        inputObjects: list[ObjectInput],
        needleThresholdRatio: float,
        capMinAngleDegrees: float,
        sessionId: str = ...,
        preview: bool = False,
    ) -> SyncGtmDetectDegenerateTrisResult:
        """[TODO: Add description] Performs Detect Degenerate Tris operation on geotechnical objects."""
        ...

class _GtmDetectDuplicatePoints:
    """[TODO: Add description] Performs Detect Duplicate Points operation on geotechnical objects."""

    async def run(
        self,
        *,
        gooseContext: GtmDetectDuplicatePointsGooseContext,
        action: Literal["detect-duplicate-points"],
        inputObjects: list[ObjectInput],
        sessionId: str = ...,
        preview: bool = False,
    ) -> GtmDetectDuplicatePointsResult:
        """[TODO: Add description] Performs Detect Duplicate Points operation on geotechnical objects."""
        ...

class _SyncGtmDetectDuplicatePoints:
    """[TODO: Add description] Performs Detect Duplicate Points operation on geotechnical objects."""

    def run(
        self,
        *,
        gooseContext: GtmDetectDuplicatePointsGooseContext,
        action: Literal["detect-duplicate-points"],
        inputObjects: list[ObjectInput],
        sessionId: str = ...,
        preview: bool = False,
    ) -> SyncGtmDetectDuplicatePointsResult:
        """[TODO: Add description] Performs Detect Duplicate Points operation on geotechnical objects."""
        ...

class _GtmDetectDuplicateTris:
    """[TODO: Add description] Performs Detect Duplicate Tris operation on geotechnical objects."""

    async def run(
        self,
        *,
        gooseContext: GtmDetectDuplicateTrisGooseContext,
        action: Literal["detect-duplicate-tris"],
        inputObjects: list[ObjectInput],
        sessionId: str = ...,
        preview: bool = False,
    ) -> GtmDetectDuplicateTrisResult:
        """[TODO: Add description] Performs Detect Duplicate Tris operation on geotechnical objects."""
        ...

class _SyncGtmDetectDuplicateTris:
    """[TODO: Add description] Performs Detect Duplicate Tris operation on geotechnical objects."""

    def run(
        self,
        *,
        gooseContext: GtmDetectDuplicateTrisGooseContext,
        action: Literal["detect-duplicate-tris"],
        inputObjects: list[ObjectInput],
        sessionId: str = ...,
        preview: bool = False,
    ) -> SyncGtmDetectDuplicateTrisResult:
        """[TODO: Add description] Performs Detect Duplicate Tris operation on geotechnical objects."""
        ...

class _GtmDetectFins:
    """[TODO: Add description] Performs Detect Fins operation on geotechnical objects."""

    async def run(
        self,
        *,
        gooseContext: GtmDetectFinsGooseContext,
        action: Literal["detect-fins"],
        inputObjects: list[ObjectInput],
        maxAreaRatio: float = ...,
        sessionId: str = ...,
        preview: bool = False,
    ) -> GtmDetectFinsResult:
        """[TODO: Add description] Performs Detect Fins operation on geotechnical objects."""
        ...

class _SyncGtmDetectFins:
    """[TODO: Add description] Performs Detect Fins operation on geotechnical objects."""

    def run(
        self,
        *,
        gooseContext: GtmDetectFinsGooseContext,
        action: Literal["detect-fins"],
        inputObjects: list[ObjectInput],
        maxAreaRatio: float = ...,
        sessionId: str = ...,
        preview: bool = False,
    ) -> SyncGtmDetectFinsResult:
        """[TODO: Add description] Performs Detect Fins operation on geotechnical objects."""
        ...

class _GtmDetectHoles:
    """[TODO: Add description] Performs Detect Holes operation on geotechnical objects."""

    async def run(
        self,
        *,
        gooseContext: GtmDetectHolesGooseContext,
        action: Literal["detect-holes"],
        inputObjects: list[ObjectInput],
        holeSizeRatioTolerance: float = ...,
        sessionId: str = ...,
        preview: bool = False,
    ) -> GtmDetectHolesResult:
        """[TODO: Add description] Performs Detect Holes operation on geotechnical objects."""
        ...

class _SyncGtmDetectHoles:
    """[TODO: Add description] Performs Detect Holes operation on geotechnical objects."""

    def run(
        self,
        *,
        gooseContext: GtmDetectHolesGooseContext,
        action: Literal["detect-holes"],
        inputObjects: list[ObjectInput],
        holeSizeRatioTolerance: float = ...,
        sessionId: str = ...,
        preview: bool = False,
    ) -> SyncGtmDetectHolesResult:
        """[TODO: Add description] Performs Detect Holes operation on geotechnical objects."""
        ...

class _GtmDetectInconsistentlyOrientedTris:
    """[TODO: Add description] Performs Detect Inconsistently Oriented Tris operation on geotechnical objects."""

    async def run(
        self,
        *,
        gooseContext: GtmDetectInconsistentlyOrientedTrisGooseContext,
        action: Literal["detect-inconsistently-oriented-tris"],
        inputObjects: list[ObjectInput],
        sessionId: str = ...,
        preview: bool = False,
    ) -> GtmDetectInconsistentlyOrientedTrisResult:
        """[TODO: Add description] Performs Detect Inconsistently Oriented Tris operation on geotechnical objects."""
        ...

class _SyncGtmDetectInconsistentlyOrientedTris:
    """[TODO: Add description] Performs Detect Inconsistently Oriented Tris operation on geotechnical objects."""

    def run(
        self,
        *,
        gooseContext: GtmDetectInconsistentlyOrientedTrisGooseContext,
        action: Literal["detect-inconsistently-oriented-tris"],
        inputObjects: list[ObjectInput],
        sessionId: str = ...,
        preview: bool = False,
    ) -> SyncGtmDetectInconsistentlyOrientedTrisResult:
        """[TODO: Add description] Performs Detect Inconsistently Oriented Tris operation on geotechnical objects."""
        ...

class _GtmDetectNonManifoldEdges:
    """[TODO: Add description] Performs Detect Non Manifold Edges operation on geotechnical objects."""

    async def run(
        self,
        *,
        gooseContext: GtmDetectNonManifoldEdgesGooseContext,
        action: Literal["detect-non-manifold-edges"],
        inputObjects: list[ObjectInput],
        sessionId: str = ...,
        preview: bool = False,
    ) -> GtmDetectNonManifoldEdgesResult:
        """[TODO: Add description] Performs Detect Non Manifold Edges operation on geotechnical objects."""
        ...

class _SyncGtmDetectNonManifoldEdges:
    """[TODO: Add description] Performs Detect Non Manifold Edges operation on geotechnical objects."""

    def run(
        self,
        *,
        gooseContext: GtmDetectNonManifoldEdgesGooseContext,
        action: Literal["detect-non-manifold-edges"],
        inputObjects: list[ObjectInput],
        sessionId: str = ...,
        preview: bool = False,
    ) -> SyncGtmDetectNonManifoldEdgesResult:
        """[TODO: Add description] Performs Detect Non Manifold Edges operation on geotechnical objects."""
        ...

class _GtmDetectNonManifoldVertices:
    """[TODO: Add description] Performs Detect Non Manifold Vertices operation on geotechnical objects."""

    async def run(
        self,
        *,
        gooseContext: GtmDetectNonManifoldVerticesGooseContext,
        action: Literal["detect-non-manifold-vertices"],
        inputObjects: list[ObjectInput],
        sessionId: str = ...,
        preview: bool = False,
    ) -> GtmDetectNonManifoldVerticesResult:
        """[TODO: Add description] Performs Detect Non Manifold Vertices operation on geotechnical objects."""
        ...

class _SyncGtmDetectNonManifoldVertices:
    """[TODO: Add description] Performs Detect Non Manifold Vertices operation on geotechnical objects."""

    def run(
        self,
        *,
        gooseContext: GtmDetectNonManifoldVerticesGooseContext,
        action: Literal["detect-non-manifold-vertices"],
        inputObjects: list[ObjectInput],
        sessionId: str = ...,
        preview: bool = False,
    ) -> SyncGtmDetectNonManifoldVerticesResult:
        """[TODO: Add description] Performs Detect Non Manifold Vertices operation on geotechnical objects."""
        ...

class _GtmDetectRedundantSurfaces:
    """[TODO: Add description] Performs Detect Redundant Surfaces operation on geotechnical objects."""

    async def run(
        self,
        *,
        gooseContext: GtmDetectRedundantSurfacesGooseContext,
        action: Literal["detect-redundant-surfaces"],
        inputObjects: list[ObjectInput],
        holeSizeRatioTolerance: float = ...,
        maxAreaRatio: float = ...,
        sessionId: str = ...,
        preview: bool = False,
    ) -> GtmDetectRedundantSurfacesResult:
        """[TODO: Add description] Performs Detect Redundant Surfaces operation on geotechnical objects."""
        ...

class _SyncGtmDetectRedundantSurfaces:
    """[TODO: Add description] Performs Detect Redundant Surfaces operation on geotechnical objects."""

    def run(
        self,
        *,
        gooseContext: GtmDetectRedundantSurfacesGooseContext,
        action: Literal["detect-redundant-surfaces"],
        inputObjects: list[ObjectInput],
        holeSizeRatioTolerance: float = ...,
        maxAreaRatio: float = ...,
        sessionId: str = ...,
        preview: bool = False,
    ) -> SyncGtmDetectRedundantSurfacesResult:
        """[TODO: Add description] Performs Detect Redundant Surfaces operation on geotechnical objects."""
        ...

class _GtmDetectSelfIntersections:
    """[TODO: Add description] Performs Detect Self Intersections operation on geotechnical objects."""

    async def run(
        self,
        *,
        gooseContext: GtmDetectSelfIntersectionsGooseContext,
        action: Literal["detect-self-intersections"],
        inputObjects: list[ObjectInput],
        tolerance: float,
        sessionId: str = ...,
        preview: bool = False,
    ) -> GtmDetectSelfIntersectionsResult:
        """[TODO: Add description] Performs Detect Self Intersections operation on geotechnical objects."""
        ...

class _SyncGtmDetectSelfIntersections:
    """[TODO: Add description] Performs Detect Self Intersections operation on geotechnical objects."""

    def run(
        self,
        *,
        gooseContext: GtmDetectSelfIntersectionsGooseContext,
        action: Literal["detect-self-intersections"],
        inputObjects: list[ObjectInput],
        tolerance: float,
        sessionId: str = ...,
        preview: bool = False,
    ) -> SyncGtmDetectSelfIntersectionsResult:
        """[TODO: Add description] Performs Detect Self Intersections operation on geotechnical objects."""
        ...

class _GtmFillHoles:
    """[TODO: Add description] Performs Fill Holes operation on geotechnical objects."""

    async def run(
        self,
        *,
        gooseContext: GtmFillHolesGooseContext,
        action: Literal["fill-holes"],
        inputObjects: list[ObjectInput],
        fillMode: Literal["Fill", "FillAndRefine", "FillRefineAndFair"],
        edges: list[list[float]] = ...,
        sessionId: str = ...,
        preview: bool = False,
    ) -> GtmFillHolesResult:
        """[TODO: Add description] Performs Fill Holes operation on geotechnical objects."""
        ...

class _SyncGtmFillHoles:
    """[TODO: Add description] Performs Fill Holes operation on geotechnical objects."""

    def run(
        self,
        *,
        gooseContext: GtmFillHolesGooseContext,
        action: Literal["fill-holes"],
        inputObjects: list[ObjectInput],
        fillMode: Literal["Fill", "FillAndRefine", "FillRefineAndFair"],
        edges: list[list[float]] = ...,
        sessionId: str = ...,
        preview: bool = False,
    ) -> SyncGtmFillHolesResult:
        """[TODO: Add description] Performs Fill Holes operation on geotechnical objects."""
        ...

class _GtmFixSelfIntersections:
    """[TODO: Add description] Performs Fix Self Intersections operation on geotechnical objects."""

    async def run(
        self,
        *,
        gooseContext: GtmFixSelfIntersectionsGooseContext,
        action: Literal["fix-self-intersections"],
        inputObjects: list[ObjectInput],
        tolerance: float,
        sessionId: str = ...,
        preview: bool = False,
    ) -> GtmFixSelfIntersectionsResult:
        """[TODO: Add description] Performs Fix Self Intersections operation on geotechnical objects."""
        ...

class _SyncGtmFixSelfIntersections:
    """[TODO: Add description] Performs Fix Self Intersections operation on geotechnical objects."""

    def run(
        self,
        *,
        gooseContext: GtmFixSelfIntersectionsGooseContext,
        action: Literal["fix-self-intersections"],
        inputObjects: list[ObjectInput],
        tolerance: float,
        sessionId: str = ...,
        preview: bool = False,
    ) -> SyncGtmFixSelfIntersectionsResult:
        """[TODO: Add description] Performs Fix Self Intersections operation on geotechnical objects."""
        ...

class _GtmInitAggregateGeom:
    """[TODO: Add description] Performs Init Aggregate Geom operation on geotechnical objects."""

    async def run(
        self,
        *,
        gooseContext: GtmInitAggregateGeomGooseContext,
        action: Literal["init-aggregate-geom"],
        path: str,
        minPoint: GtmInitAggregateGeomMinPoint,
        maxPoint: GtmInitAggregateGeomMaxPoint,
        rotationAngle: float,
        outputGMM: bool = ...,
        sessionId: str = ...,
        preview: bool = False,
    ) -> GtmInitAggregateGeomResult:
        """[TODO: Add description] Performs Init Aggregate Geom operation on geotechnical objects."""
        ...

class _SyncGtmInitAggregateGeom:
    """[TODO: Add description] Performs Init Aggregate Geom operation on geotechnical objects."""

    def run(
        self,
        *,
        gooseContext: GtmInitAggregateGeomGooseContext,
        action: Literal["init-aggregate-geom"],
        path: str,
        minPoint: GtmInitAggregateGeomMinPoint,
        maxPoint: GtmInitAggregateGeomMaxPoint,
        rotationAngle: float,
        outputGMM: bool = ...,
        sessionId: str = ...,
        preview: bool = False,
    ) -> SyncGtmInitAggregateGeomResult:
        """[TODO: Add description] Performs Init Aggregate Geom operation on geotechnical objects."""
        ...

class _GtmIntersect:
    """[TODO: Add description] Performs Intersect operation on geotechnical objects."""

    async def run(
        self,
        *,
        gooseContext: GtmIntersectGooseContext,
        action: Literal["intersect"],
        inputObjects: list[ObjectInput],
        tolerance: float,
        assumeNoSelfIntersectionsInParts: bool,
        sessionId: str = ...,
        preview: bool = False,
    ) -> GtmIntersectResult:
        """[TODO: Add description] Performs Intersect operation on geotechnical objects."""
        ...

class _SyncGtmIntersect:
    """[TODO: Add description] Performs Intersect operation on geotechnical objects."""

    def run(
        self,
        *,
        gooseContext: GtmIntersectGooseContext,
        action: Literal["intersect"],
        inputObjects: list[ObjectInput],
        tolerance: float,
        assumeNoSelfIntersectionsInParts: bool,
        sessionId: str = ...,
        preview: bool = False,
    ) -> SyncGtmIntersectResult:
        """[TODO: Add description] Performs Intersect operation on geotechnical objects."""
        ...

class _GtmLocalRemesh:
    """[TODO: Add description] Performs Local Remesh operation on geotechnical objects."""

    async def run(
        self,
        *,
        gooseContext: GtmLocalRemeshGooseContext,
        action: Literal["local-remesh"],
        inputObjects: list[ObjectInput],
        patchAngleTolerance: float,
        maxChordalError: float,
        isClosed: bool,
        initialCleanup: bool,
        lineProximityDetection: bool,
        optimize: bool,
        shapeQualityWeight: float,
        targetH: float,
        regularizeMode: bool,
        radius: float,
        seedTriInds: list[int],
        sessionId: str = ...,
        strainTolerance: float = ...,
        preview: bool = False,
    ) -> GtmLocalRemeshResult:
        """[TODO: Add description] Performs Local Remesh operation on geotechnical objects."""
        ...

class _SyncGtmLocalRemesh:
    """[TODO: Add description] Performs Local Remesh operation on geotechnical objects."""

    def run(
        self,
        *,
        gooseContext: GtmLocalRemeshGooseContext,
        action: Literal["local-remesh"],
        inputObjects: list[ObjectInput],
        patchAngleTolerance: float,
        maxChordalError: float,
        isClosed: bool,
        initialCleanup: bool,
        lineProximityDetection: bool,
        optimize: bool,
        shapeQualityWeight: float,
        targetH: float,
        regularizeMode: bool,
        radius: float,
        seedTriInds: list[int],
        sessionId: str = ...,
        strainTolerance: float = ...,
        preview: bool = False,
    ) -> SyncGtmLocalRemeshResult:
        """[TODO: Add description] Performs Local Remesh operation on geotechnical objects."""
        ...

class _GtmMakeGmmIntoIntersectedGeometry:
    """Prepares a Geological Model Meshes object to be used as an Intersected Geometry. This involves removing all open surfaces, translating to origin, collapsing overlapping triangles into shared triangles, and clipping the geometry to the given bounds."""

    async def run(
        self,
        *,
        gooseContext: GtmMakeGmmIntoIntersectedGeometryGooseContext,
        action: Literal["make-gmm-into-intersected-geometry"],
        inputObjects: list[ObjectInput],
        path: str,
        minPoint: GtmMakeGmmIntoIntersectedGeometryMinPoint,
        maxPoint: GtmMakeGmmIntoIntersectedGeometryMaxPoint,
        rotationAngle: float,
        attemptConformalization: bool = ...,
        clipTolerance: float = ...,
        collapseDistanceTolerance: float = ...,
        collapseOverlapThreshold: float = ...,
        sessionId: str = ...,
        preview: bool = False,
    ) -> GtmMakeGmmIntoIntersectedGeometryResult:
        """Prepares a Geological Model Meshes object to be used as an Intersected Geometry. This involves removing all open surfaces, translating to origin, collapsing overlapping triangles into shared triangles, and clipping the geometry to the given bounds."""
        ...

class _SyncGtmMakeGmmIntoIntersectedGeometry:
    """Prepares a Geological Model Meshes object to be used as an Intersected Geometry. This involves removing all open surfaces, translating to origin, collapsing overlapping triangles into shared triangles, and clipping the geometry to the given bounds."""

    def run(
        self,
        *,
        gooseContext: GtmMakeGmmIntoIntersectedGeometryGooseContext,
        action: Literal["make-gmm-into-intersected-geometry"],
        inputObjects: list[ObjectInput],
        path: str,
        minPoint: GtmMakeGmmIntoIntersectedGeometryMinPoint,
        maxPoint: GtmMakeGmmIntoIntersectedGeometryMaxPoint,
        rotationAngle: float,
        attemptConformalization: bool = ...,
        clipTolerance: float = ...,
        collapseDistanceTolerance: float = ...,
        collapseOverlapThreshold: float = ...,
        sessionId: str = ...,
        preview: bool = False,
    ) -> SyncGtmMakeGmmIntoIntersectedGeometryResult:
        """Prepares a Geological Model Meshes object to be used as an Intersected Geometry. This involves removing all open surfaces, translating to origin, collapsing overlapping triangles into shared triangles, and clipping the geometry to the given bounds."""
        ...

class _GtmMakeIntoModelInput:
    """[TODO: Add description] Performs Make Into Model Input operation on geotechnical objects."""

    async def run(
        self,
        *,
        gooseContext: GtmMakeIntoModelInputGooseContext,
        action: Literal["make-into-model-input"],
        inputObjects: list[ObjectInput],
        minPoint: GtmMakeIntoModelInputMinPoint,
        maxPoint: GtmMakeIntoModelInputMaxPoint,
        rotationAngle: float,
        parentPath: str,
        sessionId: str = ...,
        tolerance: float = ...,
        preview: bool = False,
    ) -> GtmMakeIntoModelInputResult:
        """[TODO: Add description] Performs Make Into Model Input operation on geotechnical objects."""
        ...

class _SyncGtmMakeIntoModelInput:
    """[TODO: Add description] Performs Make Into Model Input operation on geotechnical objects."""

    def run(
        self,
        *,
        gooseContext: GtmMakeIntoModelInputGooseContext,
        action: Literal["make-into-model-input"],
        inputObjects: list[ObjectInput],
        minPoint: GtmMakeIntoModelInputMinPoint,
        maxPoint: GtmMakeIntoModelInputMaxPoint,
        rotationAngle: float,
        parentPath: str,
        sessionId: str = ...,
        tolerance: float = ...,
        preview: bool = False,
    ) -> SyncGtmMakeIntoModelInputResult:
        """[TODO: Add description] Performs Make Into Model Input operation on geotechnical objects."""
        ...

class _GtmMergePoints:
    """[TODO: Add description] Performs Merge Points operation on geotechnical objects."""

    async def run(
        self,
        *,
        gooseContext: GtmMergePointsGooseContext,
        action: Literal["merge-points"],
        inputObjects: list[ObjectInput],
        mergeDistance: float,
        sessionId: str = ...,
        preview: bool = False,
    ) -> GtmMergePointsResult:
        """[TODO: Add description] Performs Merge Points operation on geotechnical objects."""
        ...

class _SyncGtmMergePoints:
    """[TODO: Add description] Performs Merge Points operation on geotechnical objects."""

    def run(
        self,
        *,
        gooseContext: GtmMergePointsGooseContext,
        action: Literal["merge-points"],
        inputObjects: list[ObjectInput],
        mergeDistance: float,
        sessionId: str = ...,
        preview: bool = False,
    ) -> SyncGtmMergePointsResult:
        """[TODO: Add description] Performs Merge Points operation on geotechnical objects."""
        ...

class _GtmOrientTris:
    """[TODO: Add description] Performs Orient Tris operation on geotechnical objects."""

    async def run(
        self,
        *,
        gooseContext: GtmOrientTrisGooseContext,
        action: Literal["orient-tris"],
        inputObjects: list[ObjectInput],
        sessionId: str = ...,
        preview: bool = False,
    ) -> GtmOrientTrisResult:
        """[TODO: Add description] Performs Orient Tris operation on geotechnical objects."""
        ...

class _SyncGtmOrientTris:
    """[TODO: Add description] Performs Orient Tris operation on geotechnical objects."""

    def run(
        self,
        *,
        gooseContext: GtmOrientTrisGooseContext,
        action: Literal["orient-tris"],
        inputObjects: list[ObjectInput],
        sessionId: str = ...,
        preview: bool = False,
    ) -> SyncGtmOrientTrisResult:
        """[TODO: Add description] Performs Orient Tris operation on geotechnical objects."""
        ...

class _GtmParameterizeVolumes:
    """[TODO: Add description] Performs Parameterize Volumes operation on geotechnical objects."""

    async def run(
        self,
        *,
        gooseContext: GtmParameterizeVolumesGooseContext,
        action: Literal["parameterize-volumes"],
        inputObjects: list[ObjectInput],
        path: str,
        distanceUnit: str,
        includeHardEdges: bool = ...,
        sessionId: str = ...,
        preview: bool = False,
    ) -> GtmParameterizeVolumesResult:
        """[TODO: Add description] Performs Parameterize Volumes operation on geotechnical objects."""
        ...

class _SyncGtmParameterizeVolumes:
    """[TODO: Add description] Performs Parameterize Volumes operation on geotechnical objects."""

    def run(
        self,
        *,
        gooseContext: GtmParameterizeVolumesGooseContext,
        action: Literal["parameterize-volumes"],
        inputObjects: list[ObjectInput],
        path: str,
        distanceUnit: str,
        includeHardEdges: bool = ...,
        sessionId: str = ...,
        preview: bool = False,
    ) -> SyncGtmParameterizeVolumesResult:
        """[TODO: Add description] Performs Parameterize Volumes operation on geotechnical objects."""
        ...

class _GtmRemesh:
    """[TODO: Add description] Performs Remesh operation on geotechnical objects."""

    async def run(
        self,
        *,
        gooseContext: GtmRemeshGooseContext,
        action: Literal["remesh"],
        inputObjects: list[ObjectInput],
        patchAngleTolerance: float,
        maxChordalError: float,
        isClosed: bool,
        initialCleanup: bool,
        lineProximityDetection: bool,
        optimize: bool,
        shapeQualityWeight: float,
        targetH: float,
        regularizeMode: bool,
        sessionId: str = ...,
        strainTolerance: float = ...,
        preview: bool = False,
    ) -> GtmRemeshResult:
        """[TODO: Add description] Performs Remesh operation on geotechnical objects."""
        ...

class _SyncGtmRemesh:
    """[TODO: Add description] Performs Remesh operation on geotechnical objects."""

    def run(
        self,
        *,
        gooseContext: GtmRemeshGooseContext,
        action: Literal["remesh"],
        inputObjects: list[ObjectInput],
        patchAngleTolerance: float,
        maxChordalError: float,
        isClosed: bool,
        initialCleanup: bool,
        lineProximityDetection: bool,
        optimize: bool,
        shapeQualityWeight: float,
        targetH: float,
        regularizeMode: bool,
        sessionId: str = ...,
        strainTolerance: float = ...,
        preview: bool = False,
    ) -> SyncGtmRemeshResult:
        """[TODO: Add description] Performs Remesh operation on geotechnical objects."""
        ...

class _GtmRemoveDegenerateTris:
    """[TODO: Add description] Performs Remove Degenerate Tris operation on geotechnical objects."""

    async def run(
        self,
        *,
        gooseContext: GtmRemoveDegenerateTrisGooseContext,
        action: Literal["remove-degenerate-tris"],
        inputObjects: list[ObjectInput],
        needleThresholdRatio: float,
        needleCollapseLength: float,
        capMinAngleDegrees: float,
        sessionId: str = ...,
        preview: bool = False,
    ) -> GtmRemoveDegenerateTrisResult:
        """[TODO: Add description] Performs Remove Degenerate Tris operation on geotechnical objects."""
        ...

class _SyncGtmRemoveDegenerateTris:
    """[TODO: Add description] Performs Remove Degenerate Tris operation on geotechnical objects."""

    def run(
        self,
        *,
        gooseContext: GtmRemoveDegenerateTrisGooseContext,
        action: Literal["remove-degenerate-tris"],
        inputObjects: list[ObjectInput],
        needleThresholdRatio: float,
        needleCollapseLength: float,
        capMinAngleDegrees: float,
        sessionId: str = ...,
        preview: bool = False,
    ) -> SyncGtmRemoveDegenerateTrisResult:
        """[TODO: Add description] Performs Remove Degenerate Tris operation on geotechnical objects."""
        ...

class _GtmRemoveTris:
    """[TODO: Add description] Performs Remove Tris operation on geotechnical objects."""

    async def run(
        self,
        *,
        gooseContext: GtmRemoveTrisGooseContext,
        action: Literal["remove-tris"],
        inputObjects: list[ObjectInput],
        triInds: list[float],
        sessionId: str = ...,
        preview: bool = False,
    ) -> GtmRemoveTrisResult:
        """[TODO: Add description] Performs Remove Tris operation on geotechnical objects."""
        ...

class _SyncGtmRemoveTris:
    """[TODO: Add description] Performs Remove Tris operation on geotechnical objects."""

    def run(
        self,
        *,
        gooseContext: GtmRemoveTrisGooseContext,
        action: Literal["remove-tris"],
        inputObjects: list[ObjectInput],
        triInds: list[float],
        sessionId: str = ...,
        preview: bool = False,
    ) -> SyncGtmRemoveTrisResult:
        """[TODO: Add description] Performs Remove Tris operation on geotechnical objects."""
        ...

class _GtmRemoveUnusedPoints:
    """[TODO: Add description] Performs Remove Unused Points operation on geotechnical objects."""

    async def run(
        self,
        *,
        gooseContext: GtmRemoveUnusedPointsGooseContext,
        action: Literal["remove-unused-points"],
        inputObjects: list[ObjectInput],
        sessionId: str = ...,
        preview: bool = False,
    ) -> GtmRemoveUnusedPointsResult:
        """[TODO: Add description] Performs Remove Unused Points operation on geotechnical objects."""
        ...

class _SyncGtmRemoveUnusedPoints:
    """[TODO: Add description] Performs Remove Unused Points operation on geotechnical objects."""

    def run(
        self,
        *,
        gooseContext: GtmRemoveUnusedPointsGooseContext,
        action: Literal["remove-unused-points"],
        inputObjects: list[ObjectInput],
        sessionId: str = ...,
        preview: bool = False,
    ) -> SyncGtmRemoveUnusedPointsResult:
        """[TODO: Add description] Performs Remove Unused Points operation on geotechnical objects."""
        ...

class _GtmRotate:
    """[TODO: Add description] Performs Rotate operation on geotechnical objects."""

    async def run(
        self,
        *,
        gooseContext: GtmRotateGooseContext,
        action: Literal["rotate"],
        inputObjects: list[ObjectInput],
        angles: GtmRotateAngles,
        sessionId: str = ...,
        preview: bool = False,
    ) -> GtmRotateResult:
        """[TODO: Add description] Performs Rotate operation on geotechnical objects."""
        ...

class _SyncGtmRotate:
    """[TODO: Add description] Performs Rotate operation on geotechnical objects."""

    def run(
        self,
        *,
        gooseContext: GtmRotateGooseContext,
        action: Literal["rotate"],
        inputObjects: list[ObjectInput],
        angles: GtmRotateAngles,
        sessionId: str = ...,
        preview: bool = False,
    ) -> SyncGtmRotateResult:
        """[TODO: Add description] Performs Rotate operation on geotechnical objects."""
        ...

class _GtmSplitComponents:
    """[TODO: Add description] Performs Split Components operation on geotechnical objects."""

    async def run(
        self,
        *,
        gooseContext: GtmSplitComponentsGooseContext,
        action: Literal["split-components"],
        inputObjects: list[ObjectInput],
        parentPath: str,
        namePrefix: str,
        sessionId: str = ...,
        preview: bool = False,
    ) -> GtmSplitComponentsResult:
        """[TODO: Add description] Performs Split Components operation on geotechnical objects."""
        ...

class _SyncGtmSplitComponents:
    """[TODO: Add description] Performs Split Components operation on geotechnical objects."""

    def run(
        self,
        *,
        gooseContext: GtmSplitComponentsGooseContext,
        action: Literal["split-components"],
        inputObjects: list[ObjectInput],
        parentPath: str,
        namePrefix: str,
        sessionId: str = ...,
        preview: bool = False,
    ) -> SyncGtmSplitComponentsResult:
        """[TODO: Add description] Performs Split Components operation on geotechnical objects."""
        ...

class _GtmSubsumeEmbeddedVolumeInGmm:
    """Subsume an embedded volume of a `geological-model-meshes` object into a neighbouring embedded volume."""

    async def run(
        self,
        *,
        gooseContext: GtmSubsumeEmbeddedVolumeInGmmGooseContext,
        action: Literal["subsume-embedded-volume-in-gmm"],
        inputObjects: list[ObjectInput],
        subsumeSource: int,
        sessionId: str = ...,
        subsumeTarget: GtmSubsumeEmbeddedVolumeInGmmSubsumeTargetSubsumeByIndex
        | GtmSubsumeEmbeddedVolumeInGmmSubsumeTargetSubsumeAutomaticallyByStrategy = ...,
        preview: bool = False,
    ) -> GtmSubsumeEmbeddedVolumeInGmmResult:
        """Subsume an embedded volume of a `geological-model-meshes` object into a neighbouring embedded volume."""
        ...

class _SyncGtmSubsumeEmbeddedVolumeInGmm:
    """Subsume an embedded volume of a `geological-model-meshes` object into a neighbouring embedded volume."""

    def run(
        self,
        *,
        gooseContext: GtmSubsumeEmbeddedVolumeInGmmGooseContext,
        action: Literal["subsume-embedded-volume-in-gmm"],
        inputObjects: list[ObjectInput],
        subsumeSource: int,
        sessionId: str = ...,
        subsumeTarget: GtmSubsumeEmbeddedVolumeInGmmSubsumeTargetSubsumeByIndex
        | GtmSubsumeEmbeddedVolumeInGmmSubsumeTargetSubsumeAutomaticallyByStrategy = ...,
        preview: bool = False,
    ) -> SyncGtmSubsumeEmbeddedVolumeInGmmResult:
        """Subsume an embedded volume of a `geological-model-meshes` object into a neighbouring embedded volume."""
        ...

class _GtmTranslate:
    """[TODO: Add description] Performs Translate operation on geotechnical objects."""

    async def run(
        self,
        *,
        gooseContext: GtmTranslateGooseContext,
        action: Literal["translate"],
        inputObjects: list[ObjectInput],
        delta: GtmTranslateDelta,
        sessionId: str = ...,
        preview: bool = False,
    ) -> GtmTranslateResult:
        """[TODO: Add description] Performs Translate operation on geotechnical objects."""
        ...

class _SyncGtmTranslate:
    """[TODO: Add description] Performs Translate operation on geotechnical objects."""

    def run(
        self,
        *,
        gooseContext: GtmTranslateGooseContext,
        action: Literal["translate"],
        inputObjects: list[ObjectInput],
        delta: GtmTranslateDelta,
        sessionId: str = ...,
        preview: bool = False,
    ) -> SyncGtmTranslateResult:
        """[TODO: Add description] Performs Translate operation on geotechnical objects."""
        ...

class _GtmUploadMeshData:
    """[TODO: Add description] Performs Upload Mesh Data operation on geotechnical objects."""

    async def run(
        self,
        *,
        gooseContext: GtmUploadMeshDataGooseContext,
        action: Literal["upload-mesh-data"],
        fileId: str,
        path: str,
        sessionId: str = ...,
        preview: bool = False,
    ) -> GtmUploadMeshDataResult:
        """[TODO: Add description] Performs Upload Mesh Data operation on geotechnical objects."""
        ...

class _SyncGtmUploadMeshData:
    """[TODO: Add description] Performs Upload Mesh Data operation on geotechnical objects."""

    def run(
        self,
        *,
        gooseContext: GtmUploadMeshDataGooseContext,
        action: Literal["upload-mesh-data"],
        fileId: str,
        path: str,
        sessionId: str = ...,
        preview: bool = False,
    ) -> SyncGtmUploadMeshDataResult:
        """[TODO: Add description] Performs Upload Mesh Data operation on geotechnical objects."""
        ...

class _GtmValidateGmmAsIntersectedGeometry:
    """Validates a Geological Model Meshes object to check if it is a valid representation of an intersected geometry. Returns a list of validation issues (errors). An empty list means the object is valid."""

    async def run(
        self,
        *,
        gooseContext: GtmValidateGmmAsIntersectedGeometryGooseContext,
        action: Literal["validate-gmm-as-intersected-geometry"],
        inputObjects: list[ObjectInput],
        sessionId: str = ...,
        preview: bool = False,
    ) -> GtmValidateGmmAsIntersectedGeometryResult:
        """Validates a Geological Model Meshes object to check if it is a valid representation of an intersected geometry. Returns a list of validation issues (errors). An empty list means the object is valid."""
        ...

class _SyncGtmValidateGmmAsIntersectedGeometry:
    """Validates a Geological Model Meshes object to check if it is a valid representation of an intersected geometry. Returns a list of validation issues (errors). An empty list means the object is valid."""

    def run(
        self,
        *,
        gooseContext: GtmValidateGmmAsIntersectedGeometryGooseContext,
        action: Literal["validate-gmm-as-intersected-geometry"],
        inputObjects: list[ObjectInput],
        sessionId: str = ...,
        preview: bool = False,
    ) -> SyncGtmValidateGmmAsIntersectedGeometryResult:
        """Validates a Geological Model Meshes object to check if it is a valid representation of an intersected geometry. Returns a list of validation issues (errors). An empty list means the object is valid."""
        ...

class _IsosurfacingCategory:
    """Generate closed volume meshes or boundary surfaces from category attributes on 3D grids. Supports single-category extraction."""

    async def run(
        self,
        *,
        source: IsosurfacingCategoryCategorySourceInput | AnyTypedAttribute,
        category: str,
        target: IsosurfacingCategoryTarget,
        settings: IsosurfacingCategoryCategorySettings = ...,
        preview: bool = False,
    ) -> IsosurfacingCategoryResult:
        """Generate closed volume meshes or boundary surfaces from category attributes on 3D grids. Supports single-category extraction."""
        ...

class _SyncIsosurfacingCategory:
    """Generate closed volume meshes or boundary surfaces from category attributes on 3D grids. Supports single-category extraction."""

    def run(
        self,
        *,
        source: IsosurfacingCategoryCategorySourceInput | AnyTypedAttribute,
        category: str,
        target: IsosurfacingCategoryTarget,
        settings: IsosurfacingCategoryCategorySettings = ...,
        preview: bool = False,
    ) -> SyncIsosurfacingCategoryResult:
        """Generate closed volume meshes or boundary surfaces from category attributes on 3D grids. Supports single-category extraction."""
        ...

class _IsosurfacingNumeric:
    """Generate an isosurface at a specified threshold value from numeric attributes on 3D grids. Supports regular, tensor, masked, block model, and unstructured hexahedral and tetrahedral grids."""

    async def run(
        self,
        *,
        source: IsosurfacingNumericNumericSourceInput | AnyTypedAttribute,
        threshold: float,
        target: IsosurfacingNumericTarget,
        settings: IsosurfacingNumericNumericSettings = ...,
        preview: bool = False,
    ) -> IsosurfacingNumericResult:
        """Generate an isosurface at a specified threshold value from numeric attributes on 3D grids. Supports regular, tensor, masked, block model, and unstructured hexahedral and tetrahedral grids."""
        ...

class _SyncIsosurfacingNumeric:
    """Generate an isosurface at a specified threshold value from numeric attributes on 3D grids. Supports regular, tensor, masked, block model, and unstructured hexahedral and tetrahedral grids."""

    def run(
        self,
        *,
        source: IsosurfacingNumericNumericSourceInput | AnyTypedAttribute,
        threshold: float,
        target: IsosurfacingNumericTarget,
        settings: IsosurfacingNumericNumericSettings = ...,
        preview: bool = False,
    ) -> SyncIsosurfacingNumericResult:
        """Generate an isosurface at a specified threshold value from numeric attributes on 3D grids. Supports regular, tensor, masked, block model, and unstructured hexahedral and tetrahedral grids."""
        ...

class _UtilitiesCompress:
    """Downloads a set of files referenced by File API URLs, bundles them into a single archive (ZIP or TAR with gzip/bzip2/xz compression), uploads the archive back to the File API, and returns a downloadable reference to it."""

    async def run(
        self,
        *,
        files: list[FileInput],
        destination: FileInput,
        archive_format: Literal["zip", "zip-stored", "tar", "tar.gz", "tar.bz2", "tar.xz"] = ...,
        compression_level: int | None = ...,
        output_name: str = ...,
        preview: bool = True,
    ) -> UtilitiesCompressResult:
        """Downloads a set of files referenced by File API URLs, bundles them into a single archive (ZIP or TAR with gzip/bzip2/xz compression), uploads the archive back to the File API, and returns a downloadable reference to it."""
        ...

class _SyncUtilitiesCompress:
    """Downloads a set of files referenced by File API URLs, bundles them into a single archive (ZIP or TAR with gzip/bzip2/xz compression), uploads the archive back to the File API, and returns a downloadable reference to it."""

    def run(
        self,
        *,
        files: list[FileInput],
        destination: FileInput,
        archive_format: Literal["zip", "zip-stored", "tar", "tar.gz", "tar.bz2", "tar.xz"] = ...,
        compression_level: int | None = ...,
        output_name: str = ...,
        preview: bool = True,
    ) -> SyncUtilitiesCompressResult:
        """Downloads a set of files referenced by File API URLs, bundles them into a single archive (ZIP or TAR with gzip/bzip2/xz compression), uploads the archive back to the File API, and returns a downloadable reference to it."""
        ...

class _VisServiceTiling:
    """Generates 3D Tiles from Geoscience Object Schema (GOS) objects for visualization"""

    async def run(
        self,
        *,
        object_url: str,
        request_id: str,
        bearer_token: str,
        crs: str,
        s2s_user_info: str,
        s2s_org_info: str,
        gcp_bucket_name: str,
        gcp_folder_prefix: str,
        gcp_oauth_token: str,
        gcp_oauth_token_expiry: str,
        preview: bool = False,
    ) -> VisServiceTilingResult:
        """Generates 3D Tiles from Geoscience Object Schema (GOS) objects for visualization"""
        ...

class _SyncVisServiceTiling:
    """Generates 3D Tiles from Geoscience Object Schema (GOS) objects for visualization"""

    def run(
        self,
        *,
        object_url: str,
        request_id: str,
        bearer_token: str,
        crs: str,
        s2s_user_info: str,
        s2s_org_info: str,
        gcp_bucket_name: str,
        gcp_folder_prefix: str,
        gcp_oauth_token: str,
        gcp_oauth_token_expiry: str,
        preview: bool = False,
    ) -> SyncVisServiceTilingResult:
        """Generates 3D Tiles from Geoscience Object Schema (GOS) objects for visualization"""
        ...

class _SyncGeostatisticsKriging:
    """The override's stand-in for the generic task proxy: same ``run(...)``, typed by hand."""

    def run(
        self,
        *,
        source: _SourceInput,
        target: _TargetInput,
        variogram: _ObjectInput,
        search: SearchNeighborhood,
        method: SimpleKriging | OrdinaryKriging | None = ...,
        source_filter: Filter | None = ...,
        target_filter: Filter | None = ...,
        block_discretisation: BlockDiscretisation | None = ...,
        preview: bool | None = ...,
    ) -> KrigingResult: ...

class _UnknownTask:
    """A task this stub does not describe. It still runs, with generic arguments."""

    async def run(self, **parameters: Any) -> TaskResult: ...

class _UnknownTopic:
    """A topic this stub does not describe. Its tasks still resolve at run time."""

    def __getattr__(self, name: str) -> _UnknownTask: ...

class _SyncUnknownTask:
    """A task this stub does not describe. It still runs, with generic arguments."""

    def run(self, **parameters: Any) -> SyncTaskResult: ...

class _SyncUnknownTopic:
    """A topic this stub does not describe. Its tasks still resolve at run time."""

    def __getattr__(self, name: str) -> _SyncUnknownTask: ...

class _ConverterTasks:
    """Tasks published under the ``converter`` topic."""

    detect_converter: _ConverterDetectConverter
    dgn_import: _ConverterDgnImport
    ers_import: _ConverterErsImport
    geosoft_grid_import: _ConverterGeosoftGridImport
    geosoft_surface_import: _ConverterGeosoftSurfaceImport
    geosoft_voxel_import: _ConverterGeosoftVoxelImport
    image_import: _ConverterImageImport
    obj_import: _ConverterObjImport
    shp_import: _ConverterShpImport
    ubc_import: _ConverterUbcImport
    vtk_import: _ConverterVtkImport
    xyz_import: _ConverterXyzImport
    def __getattr__(self, name: str) -> _UnknownTask: ...

class _CptToBoreholeTasks:
    """Tasks published under the ``cpt-to-borehole`` topic."""

    cpt_reader: _CptToBoreholeCptReader
    create_clusters: _CptToBoreholeCreateClusters
    publish: _CptToBoreholePublish
    stratify: _CptToBoreholeStratify
    def __getattr__(self, name: str) -> _UnknownTask: ...

class _DriverTasks:
    """Tasks published under the ``driver`` topic."""

    driver_general_compute: _DriverDriverGeneralCompute
    def __getattr__(self, name: str) -> _UnknownTask: ...

class _GeophysicsTasks:
    """Tasks published under the ``geophysics`` topic."""

    gravity_terrain_correction: _GeophysicsGravityTerrainCorrection
    regular_2d_grid_to_trimesh: _GeophysicsRegular2dGridToTrimesh
    def __getattr__(self, name: str) -> _UnknownTask: ...

class _GeostatisticsTasks:
    """Tasks published under the ``geostatistics`` topic."""

    break_ties: _GeostatisticsBreakTies
    conditional_turning_bands: _GeostatisticsConditionalTurningBands
    consim: _GeostatisticsConsim
    continuous_distribution: _GeostatisticsContinuousDistribution
    declustering: _GeostatisticsDeclustering
    experimental_variogram: _GeostatisticsExperimentalVariogram
    idw: _GeostatisticsIdw
    knn: _GeostatisticsKnn
    kriging: _GeostatisticsKriging
    location_wise: _GeostatisticsLocationWise
    loss_calculation: _GeostatisticsLossCalculation
    normal_score: _GeostatisticsNormalScore
    profit_calculation: _GeostatisticsProfitCalculation
    simulation_report: _GeostatisticsSimulationReport
    def __getattr__(self, name: str) -> _UnknownTask: ...

class _GeotechTasks:
    """Tasks published under the ``geotech`` topic."""

    add_to_intersected_geometry: _GeotechAddToIntersectedGeometry
    calculate_mesh_quality_histogram: _GeotechCalculateMeshQualityHistogram
    check_triangle_orientation: _GeotechCheckTriangleOrientation
    copy_geological_model_meshes: _GeotechCopyGeologicalModelMeshes
    create_cross_section: _GeotechCreateCrossSection
    create_geological_model_meshes: _GeotechCreateGeologicalModelMeshes
    detect_degenerate_triangles: _GeotechDetectDegenerateTriangles
    detect_duplicate_points: _GeotechDetectDuplicatePoints
    detect_duplicate_triangles: _GeotechDetectDuplicateTriangles
    detect_fins: _GeotechDetectFins
    detect_holes: _GeotechDetectHoles
    detect_non_manifold_edges: _GeotechDetectNonManifoldEdges
    detect_non_manifold_vertices: _GeotechDetectNonManifoldVertices
    detect_non_partitioning_surfaces: _GeotechDetectNonPartitioningSurfaces
    detect_self_intersections: _GeotechDetectSelfIntersections
    fill_holes: _GeotechFillHoles
    fix_self_intersections: _GeotechFixSelfIntersections
    init_aggregate_geom_gmm: _GeotechInitAggregateGeomGmm
    local_remesh: _GeotechLocalRemesh
    make_gmm_into_intersected_geometry: _GeotechMakeGmmIntoIntersectedGeometry
    make_into_model_input: _GeotechMakeIntoModelInput
    mark_volumes_for_removal: _GeotechMarkVolumesForRemoval
    parameterize_volumes: _GeotechParameterizeVolumes
    remesh: _GeotechRemesh
    remove_triangles: _GeotechRemoveTriangles
    subsume_embedded_volume_in_gmm: _GeotechSubsumeEmbeddedVolumeInGmm
    validate_gmm_as_intersected_geometry: _GeotechValidateGmmAsIntersectedGeometry
    def __getattr__(self, name: str) -> _UnknownTask: ...

class _GtmTasks:
    """Tasks published under the ``gtm`` topic."""

    add_to_intersected_geometry: _GtmAddToIntersectedGeometry
    clip: _GtmClip
    create_cross_section: _GtmCreateCrossSection
    create_geological_model_meshes: _GtmCreateGeologicalModelMeshes
    detect_degenerate_tris: _GtmDetectDegenerateTris
    detect_duplicate_points: _GtmDetectDuplicatePoints
    detect_duplicate_tris: _GtmDetectDuplicateTris
    detect_fins: _GtmDetectFins
    detect_holes: _GtmDetectHoles
    detect_inconsistently_oriented_tris: _GtmDetectInconsistentlyOrientedTris
    detect_non_manifold_edges: _GtmDetectNonManifoldEdges
    detect_non_manifold_vertices: _GtmDetectNonManifoldVertices
    detect_redundant_surfaces: _GtmDetectRedundantSurfaces
    detect_self_intersections: _GtmDetectSelfIntersections
    fill_holes: _GtmFillHoles
    fix_self_intersections: _GtmFixSelfIntersections
    init_aggregate_geom: _GtmInitAggregateGeom
    intersect: _GtmIntersect
    local_remesh: _GtmLocalRemesh
    make_gmm_into_intersected_geometry: _GtmMakeGmmIntoIntersectedGeometry
    make_into_model_input: _GtmMakeIntoModelInput
    merge_points: _GtmMergePoints
    orient_tris: _GtmOrientTris
    parameterize_volumes: _GtmParameterizeVolumes
    remesh: _GtmRemesh
    remove_degenerate_tris: _GtmRemoveDegenerateTris
    remove_tris: _GtmRemoveTris
    remove_unused_points: _GtmRemoveUnusedPoints
    rotate: _GtmRotate
    split_components: _GtmSplitComponents
    subsume_embedded_volume_in_gmm: _GtmSubsumeEmbeddedVolumeInGmm
    translate: _GtmTranslate
    upload_mesh_data: _GtmUploadMeshData
    validate_gmm_as_intersected_geometry: _GtmValidateGmmAsIntersectedGeometry
    def __getattr__(self, name: str) -> _UnknownTask: ...

class _IsosurfacingTasks:
    """Tasks published under the ``isosurfacing`` topic."""

    category: _IsosurfacingCategory
    numeric: _IsosurfacingNumeric
    def __getattr__(self, name: str) -> _UnknownTask: ...

class _UtilitiesTasks:
    """Tasks published under the ``utilities`` topic."""

    compress: _UtilitiesCompress
    def __getattr__(self, name: str) -> _UnknownTask: ...

class _VisServiceTasks:
    """Tasks published under the ``vis-service`` topic."""

    tiling: _VisServiceTiling
    def __getattr__(self, name: str) -> _UnknownTask: ...

class _SyncConverterTasks:
    """Tasks published under the ``converter`` topic."""

    detect_converter: _SyncConverterDetectConverter
    dgn_import: _SyncConverterDgnImport
    ers_import: _SyncConverterErsImport
    geosoft_grid_import: _SyncConverterGeosoftGridImport
    geosoft_surface_import: _SyncConverterGeosoftSurfaceImport
    geosoft_voxel_import: _SyncConverterGeosoftVoxelImport
    image_import: _SyncConverterImageImport
    obj_import: _SyncConverterObjImport
    shp_import: _SyncConverterShpImport
    ubc_import: _SyncConverterUbcImport
    vtk_import: _SyncConverterVtkImport
    xyz_import: _SyncConverterXyzImport
    def __getattr__(self, name: str) -> _SyncUnknownTask: ...

class _SyncCptToBoreholeTasks:
    """Tasks published under the ``cpt-to-borehole`` topic."""

    cpt_reader: _SyncCptToBoreholeCptReader
    create_clusters: _SyncCptToBoreholeCreateClusters
    publish: _SyncCptToBoreholePublish
    stratify: _SyncCptToBoreholeStratify
    def __getattr__(self, name: str) -> _SyncUnknownTask: ...

class _SyncDriverTasks:
    """Tasks published under the ``driver`` topic."""

    driver_general_compute: _SyncDriverDriverGeneralCompute
    def __getattr__(self, name: str) -> _SyncUnknownTask: ...

class _SyncGeophysicsTasks:
    """Tasks published under the ``geophysics`` topic."""

    gravity_terrain_correction: _SyncGeophysicsGravityTerrainCorrection
    regular_2d_grid_to_trimesh: _SyncGeophysicsRegular2dGridToTrimesh
    def __getattr__(self, name: str) -> _SyncUnknownTask: ...

class _SyncGeostatisticsTasks:
    """Tasks published under the ``geostatistics`` topic."""

    break_ties: _SyncGeostatisticsBreakTies
    conditional_turning_bands: _SyncGeostatisticsConditionalTurningBands
    consim: _SyncGeostatisticsConsim
    continuous_distribution: _SyncGeostatisticsContinuousDistribution
    declustering: _SyncGeostatisticsDeclustering
    experimental_variogram: _SyncGeostatisticsExperimentalVariogram
    idw: _SyncGeostatisticsIdw
    knn: _SyncGeostatisticsKnn
    kriging: _SyncGeostatisticsKriging
    location_wise: _SyncGeostatisticsLocationWise
    loss_calculation: _SyncGeostatisticsLossCalculation
    normal_score: _SyncGeostatisticsNormalScore
    profit_calculation: _SyncGeostatisticsProfitCalculation
    simulation_report: _SyncGeostatisticsSimulationReport
    def __getattr__(self, name: str) -> _SyncUnknownTask: ...

class _SyncGeotechTasks:
    """Tasks published under the ``geotech`` topic."""

    add_to_intersected_geometry: _SyncGeotechAddToIntersectedGeometry
    calculate_mesh_quality_histogram: _SyncGeotechCalculateMeshQualityHistogram
    check_triangle_orientation: _SyncGeotechCheckTriangleOrientation
    copy_geological_model_meshes: _SyncGeotechCopyGeologicalModelMeshes
    create_cross_section: _SyncGeotechCreateCrossSection
    create_geological_model_meshes: _SyncGeotechCreateGeologicalModelMeshes
    detect_degenerate_triangles: _SyncGeotechDetectDegenerateTriangles
    detect_duplicate_points: _SyncGeotechDetectDuplicatePoints
    detect_duplicate_triangles: _SyncGeotechDetectDuplicateTriangles
    detect_fins: _SyncGeotechDetectFins
    detect_holes: _SyncGeotechDetectHoles
    detect_non_manifold_edges: _SyncGeotechDetectNonManifoldEdges
    detect_non_manifold_vertices: _SyncGeotechDetectNonManifoldVertices
    detect_non_partitioning_surfaces: _SyncGeotechDetectNonPartitioningSurfaces
    detect_self_intersections: _SyncGeotechDetectSelfIntersections
    fill_holes: _SyncGeotechFillHoles
    fix_self_intersections: _SyncGeotechFixSelfIntersections
    init_aggregate_geom_gmm: _SyncGeotechInitAggregateGeomGmm
    local_remesh: _SyncGeotechLocalRemesh
    make_gmm_into_intersected_geometry: _SyncGeotechMakeGmmIntoIntersectedGeometry
    make_into_model_input: _SyncGeotechMakeIntoModelInput
    mark_volumes_for_removal: _SyncGeotechMarkVolumesForRemoval
    parameterize_volumes: _SyncGeotechParameterizeVolumes
    remesh: _SyncGeotechRemesh
    remove_triangles: _SyncGeotechRemoveTriangles
    subsume_embedded_volume_in_gmm: _SyncGeotechSubsumeEmbeddedVolumeInGmm
    validate_gmm_as_intersected_geometry: _SyncGeotechValidateGmmAsIntersectedGeometry
    def __getattr__(self, name: str) -> _SyncUnknownTask: ...

class _SyncGtmTasks:
    """Tasks published under the ``gtm`` topic."""

    add_to_intersected_geometry: _SyncGtmAddToIntersectedGeometry
    clip: _SyncGtmClip
    create_cross_section: _SyncGtmCreateCrossSection
    create_geological_model_meshes: _SyncGtmCreateGeologicalModelMeshes
    detect_degenerate_tris: _SyncGtmDetectDegenerateTris
    detect_duplicate_points: _SyncGtmDetectDuplicatePoints
    detect_duplicate_tris: _SyncGtmDetectDuplicateTris
    detect_fins: _SyncGtmDetectFins
    detect_holes: _SyncGtmDetectHoles
    detect_inconsistently_oriented_tris: _SyncGtmDetectInconsistentlyOrientedTris
    detect_non_manifold_edges: _SyncGtmDetectNonManifoldEdges
    detect_non_manifold_vertices: _SyncGtmDetectNonManifoldVertices
    detect_redundant_surfaces: _SyncGtmDetectRedundantSurfaces
    detect_self_intersections: _SyncGtmDetectSelfIntersections
    fill_holes: _SyncGtmFillHoles
    fix_self_intersections: _SyncGtmFixSelfIntersections
    init_aggregate_geom: _SyncGtmInitAggregateGeom
    intersect: _SyncGtmIntersect
    local_remesh: _SyncGtmLocalRemesh
    make_gmm_into_intersected_geometry: _SyncGtmMakeGmmIntoIntersectedGeometry
    make_into_model_input: _SyncGtmMakeIntoModelInput
    merge_points: _SyncGtmMergePoints
    orient_tris: _SyncGtmOrientTris
    parameterize_volumes: _SyncGtmParameterizeVolumes
    remesh: _SyncGtmRemesh
    remove_degenerate_tris: _SyncGtmRemoveDegenerateTris
    remove_tris: _SyncGtmRemoveTris
    remove_unused_points: _SyncGtmRemoveUnusedPoints
    rotate: _SyncGtmRotate
    split_components: _SyncGtmSplitComponents
    subsume_embedded_volume_in_gmm: _SyncGtmSubsumeEmbeddedVolumeInGmm
    translate: _SyncGtmTranslate
    upload_mesh_data: _SyncGtmUploadMeshData
    validate_gmm_as_intersected_geometry: _SyncGtmValidateGmmAsIntersectedGeometry
    def __getattr__(self, name: str) -> _SyncUnknownTask: ...

class _SyncIsosurfacingTasks:
    """Tasks published under the ``isosurfacing`` topic."""

    category: _SyncIsosurfacingCategory
    numeric: _SyncIsosurfacingNumeric
    def __getattr__(self, name: str) -> _SyncUnknownTask: ...

class _SyncUtilitiesTasks:
    """Tasks published under the ``utilities`` topic."""

    compress: _SyncUtilitiesCompress
    def __getattr__(self, name: str) -> _SyncUnknownTask: ...

class _SyncVisServiceTasks:
    """Tasks published under the ``vis-service`` topic."""

    tiling: _SyncVisServiceTiling
    def __getattr__(self, name: str) -> _SyncUnknownTask: ...

class ComputeClient:
    """
    Instance-bound asynchronous entry point to the compute task catalogue.

    The topic and task attributes below come from a point-in-time snapshot of the
    discovery catalogue. The runtime resolves them live, so a task missing from this
    stub still runs -- reach it with :meth:`arun`.

    See :class:`SyncComputeClient` for the same catalogue through the other entry point.
    """

    def __init__(
        self,
        context: IContext,
        *,
        cache_ttl_seconds: float = ...,
        validate: bool = ...,
        deep_validation: bool = ...,
        check_schemas: bool | None = ...,
    ) -> None: ...
    async def arun(
        self,
        topic: str,
        task: str,
        parameters: dict[str, Any],
        *,
        validate: bool | None = ...,
        deep_validation: bool | None = ...,
        check_schemas: bool | None = ...,
    ) -> TaskResult:
        """Run any task by name, including one this stub does not know about."""
        ...
    def __dir__(self) -> list[str]: ...
    def __repr__(self) -> str: ...
    converter: _ConverterTasks
    cpt_to_borehole: _CptToBoreholeTasks
    driver: _DriverTasks
    geophysics: _GeophysicsTasks
    geostatistics: _GeostatisticsTasks
    geotech: _GeotechTasks
    gtm: _GtmTasks
    isosurfacing: _IsosurfacingTasks
    utilities: _UtilitiesTasks
    vis_service: _VisServiceTasks
    def __getattr__(self, name: str) -> _UnknownTopic: ...

class SyncComputeClient:
    """
    Instance-bound blocking entry point to the compute task catalogue.

    The topic and task attributes below come from a point-in-time snapshot of the
    discovery catalogue. The runtime resolves them live, so a task missing from this
    stub still runs -- reach it with :meth:`run`.

    See :class:`ComputeClient` for the same catalogue through the other entry point.
    """

    def __init__(
        self,
        context: IContext,
        *,
        cache_ttl_seconds: float = ...,
        validate: bool = ...,
        deep_validation: bool = ...,
        check_schemas: bool | None = ...,
    ) -> None: ...
    def run(
        self,
        topic: str,
        task: str,
        parameters: dict[str, Any],
        *,
        validate: bool | None = ...,
        deep_validation: bool | None = ...,
        check_schemas: bool | None = ...,
    ) -> SyncTaskResult:
        """Run any task by name, including one this stub does not know about."""
        ...
    def __dir__(self) -> list[str]: ...
    def __repr__(self) -> str: ...
    converter: _SyncConverterTasks
    cpt_to_borehole: _SyncCptToBoreholeTasks
    driver: _SyncDriverTasks
    geophysics: _SyncGeophysicsTasks
    geostatistics: _SyncGeostatisticsTasks
    geotech: _SyncGeotechTasks
    gtm: _SyncGtmTasks
    isosurfacing: _SyncIsosurfacingTasks
    utilities: _SyncUtilitiesTasks
    vis_service: _SyncVisServiceTasks
    def __getattr__(self, name: str) -> _SyncUnknownTopic: ...
